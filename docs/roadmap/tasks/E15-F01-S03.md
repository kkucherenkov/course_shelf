# E15-F01-S03 — AuthCubit + AuthHttpClient (mobile sign-in/out)

**Status:** 🔄 In progress
**Stage:** A
**Owner:** claude
**Estimated duration:** 2 days

**Epic:** E15 — Mobile foundations
**Feature:** F01 — App skeleton

## Dependencies

- [E15-F01-S02](./E15-F01-S02.md) — Wire generated Dio client with bearer interceptor
- [E04-F02-S02](./E04-F02-S02.md) — Mount Better Auth + register global guard

## Spec / Design references

- docs/design/DESIGN_BRIEF.md

## Goal

Mobile mirrors the web bearer flow. Phone-OTP is the PRIMARY sign-in path (Better Auth `phone-number/send-otp` + `verify-otp`); email `signIn` / `signUp` exist but are dev-tooling only.

## Acceptance

- `AuthCubit` emits `unauthenticated`, `authenticating`, `authenticated`, `otpSent`, `error` (note: `error`, not `Failed`; `otpSent` was omitted from the original card)
- Token persists across restarts

## Spec diff

none

## Codegen impact

no

## Design impact

no

## Tests

bloc_test on the cubit; widget test on login

## Sub-steps

- [ ] `AuthApiImpl` over Dio — primary `requestOtp` / `verifyOtp` (Better Auth `phone-number/send-otp` + `verify-otp`); email `signIn` / `signUp` are dev-tooling only
- [ ] `AuthCubit` + secure storage
- [ ] root `BlocProvider`
- [ ] minimal login screen (final design lands in E18-F03-S01)

## Notes

- Shipped reality in apps/mobile: `AuthCubit` + `SecureTokenStorage` + root `BlocProvider` + login/signup screens + `auth_cubit_test` are all present. Remaining work is the phone-OTP path polish and the final login design (E18-F03-S01).

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
