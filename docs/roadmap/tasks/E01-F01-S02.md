# E01-F01-S02 — Shared TypeScript & lint configs

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E01 — Repository foundations
**Feature:** F01 — Workspace bootstrap

## Dependencies

- [E01-F01-S01](./E01-F01-S01.md) — Initialize pnpm + Turborepo workspace

## Spec / Design references

- AGENTS.md §7.1
- AGENTS.md §10

## Goal

One source of truth for tsconfig presets and ESLint rules, including DDD layering boundaries.

## Acceptance

- `packages/tsconfig` exports `base.json`, `node.json`, `nuxt.json`
- `packages/eslint-config` exports flat-config presets for node, vue, and a `boundaries` ruleset that forbids cross-context imports
- `pnpm lint` runs across the workspace and reports zero errors

## Spec diff

none

## Codegen impact

no

## Design impact

no

## Tests

A deliberate cross-context import in a fixture file fails lint

## Sub-steps

- [x] add `tsconfig` package with strict + path-mapping presets
- [x] add `eslint-config` with Prettier integration and `eslint-plugin-boundaries`
- [x] encode the layering rules from AGENTS.md §7.2 (cross-module imports forbidden; modules can read shared/common/i18n freely) in `packages/eslint-config/nest.mjs`
- [x] flat config at the root (`eslint.config.mjs` instead of legacy `.eslintrc.cjs`); `.prettierrc.mjs` exists
- [x] root `lint` / `typecheck` / `format` scripts via Turborepo + pnpm

## Notes

- Completed: 2026-04-26
- Result: closed by T-2026-04-26-006 (boundaries plugin), T-2026-04-26-009 (prisma postinstall hook unblocks workspace-wide lint). `pnpm -r lint` reports 0 errors / 36 warnings (vue/max-attributes-per-line — formatting only).

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
