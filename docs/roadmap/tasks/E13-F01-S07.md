# E13-F01-S07 — AppBanner (info/success/warning/error), AppToast, inline alerts

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E13 — Web component catalog
**Feature:** F01 — Primitive families (from cs-foundations)

## Dependencies

- [E12-F01-S01](./E12-F01-S01.md) — Configure Storybook for Vue 3 + Nuxt UI v4 + tokens
- [E13-F01-S01](./E13-F01-S01.md) — IconCS — port the bundle's 61-icon family to Vue

## Spec / Design references

- docs/design/cs-foundations/, docs/design/shared/tokens.css

## Goal

AppBanner (info/success/warning/error), AppToast, inline alerts matching the bundle.

## Acceptance

- States in stories match the bundle's state matrix
- Uses design tokens only (no hard-coded values)

## Spec diff

none

## Codegen impact

no

## Design impact

yes

## Tests

_To be defined when picked up._

## Sub-steps

- [x] `AppBanner` with 4 variants (info/success/warning/error) + optional title + dismissible (commit `8f4b996`)
- [x] `AppToast` with colored dot indicator (success/info/error) + dismissible (commit `65790c4`)
- [x] `AppAlert` single-line inline alert + barrel exports (commit `7b24087`)
- [x] specs + stories per component

## Notes

- Completed: 2026-04-27
- Result: T-2026-04-27-039 — three commits on `feat/banners-alerts`. UI tests 410/410 (+54: 17 banner + 14 toast + 13 alert). Lint + typecheck + stylelint clean.
- All three bundle CSS aliases (`--text-loud`, `--surface-2`, `--shadow-2`) ship verbatim in `tokens.generated.css` so no token-name deviations were needed; the engineer used the semantic equivalents (`--surface-raised`, `--shadow-md`) where they were aliased anyway — same values either way.
- A11y: AppBanner / AppToast use `role="alert"` on the `error` variant and `role="status"` on the rest (assertive vs polite live region semantics). AppAlert is always `role="alert"` since it sits under form fields and announces validation feedback.
- Icon mapping: info → `info`, success → `check-circle`, warning → `alert`, error → `alert` — drawn from the IconCS family (E13-F01-S01).
- AppToast renders a colored dot (8px) + message + optional dismiss; the toast queue/container is out of scope and left for a future story.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
