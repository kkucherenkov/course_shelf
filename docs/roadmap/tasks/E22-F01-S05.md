# E22-F01-S05 — Flutter golden test job

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E22 — CI/CD
**Feature:** F01 — GH Actions

## Dependencies

- [E22-F01-S01](./E22-F01-S01.md) — Reusable setup action (Node + pnpm + Flutter cache)
- [E16-F01-S01](./E16-F01-S01.md) — widgetbook/main.dart + canary use case

## Spec / Design references

_None_

## Goal

Runs `flutter test --update-goldens=false` and fails on diffs.

## Acceptance

- Goldens stored under `test/goldens/`
- Diff artifacts uploaded on failure

## Spec diff

none

## Codegen impact

no

## Design impact

no

## Tests

The job _is_ the test: `flutter test` in both packages, with 76 committed
golden PNGs as the assertion surface.

## Sub-steps

- [x] `flutter` job in `.github/workflows/ci.yml` — analyze + test for
      `packages/ui_flutter` and `apps/mobile`
- [x] goldens run under plain `flutter test` (no `--update-goldens`), so a pixel
      diff fails the job
- [x] upload `test/**/failures/**` as the `golden-failures` artifact on failure

## Notes

- Completed: 2026-07-24
- Result: commit `c10cc64` — _ci: run the tests that exist — add Flutter job,
  stop enumerating packages_.

Marked Done retroactively (2026-08-29): the job shipped inside the broader
"run the tests that exist" CI commit rather than against this card, so nobody
ticked the box.

**Deviation — golden location.** The card says `test/goldens/`; the goldens are
colocated per component instead (`packages/ui_flutter/test/<area>/goldens/*.png`,
76 files across 19 areas). Same guarantee, and a diff points at the widget that
moved rather than at one flat directory.

**Deviation — dependency.** Carded as `⇐ E22-F01-S01` (the reusable setup
action). The job predates that action and calls `subosito/flutter-action@v2`
directly; E22-F01-S01 will fold it in.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done · ❌ Cancelled

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
