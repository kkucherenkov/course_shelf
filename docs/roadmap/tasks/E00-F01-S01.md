# E00-F01-S01 — Place the Claude Design bundle under docs/design/

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E00 — Stage the design bundle
**Feature:** F01 — Place the bundle

## Dependencies

_None_

## Spec / Design references

- AGENTS.md §6.4
- DESIGN.md §2.10

## Goal

The bundle ships as a first-class repo asset that PRs link.

## Acceptance

- `docs/design/` mirrors the bundle layout (cs-foundations, cs-components, cs-web-_, cs-mobile-_, shared)
- `docs/design/README.md` is an index keyed by project slug, with a `Status` column and a `Consumed by` column pointing at story IDs
- The brief is preserved at `docs/design/DESIGN_BRIEF.md`

## Spec diff

none

## Codegen impact

no

## Design impact

yes — establishes the canonical handoff path

## Tests

A CI job grep-checks that every UI story's `spec_refs` references an existing `docs/design/<slug>/` path

## Sub-steps

- [x] copy bundle contents into `docs/design/`
- [x] move `uploads/DESIGN_BRIEF.md` to `docs/design/DESIGN_BRIEF.md`
- [x] write `docs/design/README.md` with the prototype/status/consumed-by index
- [x] add `.gitattributes` rule marking `docs/design/**/*.{html,jsx,css}` as `linguist-vendored=true`

## Notes

- Completed: 2026-04-26
- Result: T-2026-04-26-008 (chore/design-bundle-index branch)
- The bundle slug `cs-foundations` ships as `cs-foundation` (singular) inside the actual handoff; the README documents this and uses the as-shipped name. Three mobile slugs (`cs-mobile-course-detail`, `cs-mobile-downloads`, `cs-mobile-search-settings`) and one partial (`cs-mobile-browse`) are documented as pending in the index — they unblock when the corresponding Claude Design handoffs land.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
