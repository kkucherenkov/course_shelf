# E29-F02-S01 — Quizzes generated from a transcript

**Status:** ⬜ Not started
**Stage:** B
**Owner:** claude
**Estimated duration:** 3 days

**Epic:** E29 — Learning mechanics
**Feature:** F02 — Generated quizzes

## Dependencies

- [E27-F01-S01](./E27-F01-S01.md) — Parse existing subtitle sidecars into cues

## Spec / Design references

- v2 direction: “Обучение, а не просмотр” (C4)

## Goal

Check whether a lesson actually landed, using the words it actually used.

## Acceptance

- Questions generated from a lesson's cues, each carrying the timestamp it came from
- Generation is a reviewable proposal, not an automatic write — the `IdentifyTask` propose/apply/discard pattern already models this
- The engine choice is explicit and documented, including whether any content leaves the machine

## Spec diff

openapi.yaml — quiz routes, shape to be settled by the design pre-step

## Codegen impact

yes

## Design impact

no

## Tests

Deterministic tests against a stubbed generator; **no test calls a live model.**

## Sub-steps

- [ ] Design pre-step: local model vs hosted API, and the privacy consequence of each for a self-hosted instance
- [ ] Port + adapter behind it
- [ ] Proposal aggregate reusing the identify-task shape
- [ ] OpenAPI + codegen + handlers

## Notes

- Stage B and deliberately last in F02: a self-hosted personal instance sending its course audio or transcripts to a third party is a decision the owner makes, not one the roadmap makes for them.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done · ❌ Cancelled

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
