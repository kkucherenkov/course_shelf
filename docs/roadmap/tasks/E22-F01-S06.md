# E22-F01-S06 — release.yml — build + publish API+web Docker image to GHCR

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 2 days

**Epic:** E22 — CI/CD
**Feature:** F01 — GH Actions

## Dependencies

- [E22-F01-S02](./E22-F01-S02.md) — ci.yml with parallel jobs per app/package

## Spec / Design references

_None_

## Goal

Tagged release builds and publishes versioned Docker images to a
public registry, generates a Conventional-Commits changelog, and
creates a Forgejo Release with a deploy-ready artefact bundle.

## Acceptance

- Images on GitHub Container Registry under
  `ghcr.io/<ns>/courseshelf-{backend,web}:{tag,major.minor,major,latest}`.
  Proxy + centrifugo run upstream images directly, no custom builds.
- amd64 only for v1; arm64 deferred to a follow-up story.
- Forgejo Release page populated with a Conventional-Commits-derived
  changelog (git-cliff) and a deploy-ready artefact bundle
  (`compose-release-v<semver>.yml`, `.env.release.example`, prod
  nginx config, release notes).

## Design decision — registry choice

Implementation pushes to GHCR (`ghcr.io`) rather than the homelab
Forgejo OCI endpoint (`code.homelab.local`) called out in the
original card. Rationale (mirrored verbatim from
`.forgejo/workflows/release.yml`, the file this shipped as — see Notes):

> The homelab Forgejo at code.homelab.local doesn't terminate TLS on
> its container-registry endpoint. Docker requires HTTPS by default,
> and the only workaround (insecure-registries via daemon.json +
> restart) needs systemd, which the act_runner's container init
> isn't. GHCR is reliable public infra, has proper TLS, and the
> runner already pulls FROM-images from Docker Hub fine — it has
> internet access and HTTPS works.

Moving to Forgejo OCI is deferred until the homelab registry gains a
working TLS cert; it's tracked as a follow-up rather than blocking
this card.

## Spec diff

none

## Codegen impact

no

## Design impact

no

## Tests

_To be defined when picked up._

## Sub-steps

- [x] Centrifugo configured from the environment, not from mounted hardcoded
      JSON. Shipped as an envsubst entrypoint; **since replaced** — see Notes.
- [x] `APP_VERSION` build arg wired into backend + web Dockerfiles
      (`apps/backend/Dockerfile:97`, `apps/web/Dockerfile:72`).
- [x] `docker/compose.release.yml` — image-pulling variant alongside
      the existing build-locally `compose.prod.yml`.
- [x] `cliff.toml` + `pnpm release:notes` → Conventional-Commits changelog
      (`package.json:40`).
- [x] `.env.release.example` — full env contract for the release path.
- [x] `.github/workflows/release.yml` — tag-triggered (vMAJOR.MINOR.PATCH-release).
      Shipped as `.forgejo/workflows/release.yml`; ported in `5738965`.
- [x] `docs/release.md` runbook + restructured `docs/deployment.md`
      (registry-pull becomes the primary path).

## Notes

Tag pattern: `vMAJOR.MINOR.PATCH-release` (e.g. `v0.2.0-release`).
Workflow rejects anything else.

Registry: GHCR (`ghcr.io/kkucherenkov/courseshelf-{backend,web}`).
The "Design decision" section above argues GHCR over the homelab Forgejo OCI
endpoint; that argument is now moot — the project moved to GitHub outright and
Forgejo is gone. Kept because it still records *why* the images are public.

- Completed: 2026-05-04
- Result: commit `842d3e7` — _chore(ci): release pipeline + image-pulling compose (E22-F01-S06)_.

**Do not read that commit's `(#190)` as a GitHub PR.** It is a **Forgejo** pull
request number from before the migration; GitHub's #190 is the openapi-generator
jar cache, three months and one epic away. Same trap the E02/E13/E14 cards carry.

**Bookkeeping corrected 2026-08-29.** The card was flipped to ✅ Done on
2026-05-04 with all seven sub-steps left unticked. Each was verified against the
tree and ticked above.

**Deviation — where the workflow lives.** It shipped as
`.forgejo/workflows/release.yml`. `5738965` ported it to
`.github/workflows/release.yml` and `d0218e7` deleted the `.forgejo/` tree;
nothing under `.forgejo/` is tracked today.

**Deviation — Centrifugo config, twice over.** `842d3e7` added
`docker/centrifugo/{Dockerfile,entrypoint.sh,config.template.json}` doing
envsubst at boot. `fb95376` deleted the entrypoint and template as dead code:
`compose.release.yml` points at upstream `centrifugo/centrifugo:v6` and
configures it entirely through `CENTRIFUGO_*` env vars, which is the same
guarantee with nothing to maintain. The surviving `docker/centrifugo/Dockerfile`
bakes `config.json` for the **dev/CI** stack only — see its header comment for
the DinD bind-mount failure it works around.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done · ❌ Cancelled

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
