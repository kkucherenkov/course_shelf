# E06-F03-S02 — Lesson + Material + Subtitle read model

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 2 days

**Epic:** E06 — Catalog bounded context
**Feature:** F03 — Course / Section / Lesson aggregates

## Dependencies

- [E06-F03-S01](./E06-F03-S01.md) — Course aggregate + slug uniqueness + section ordering

## Spec / Design references

- PRD.md FR-LIB-04
- PRD.md FR-PLAY-04

## Goal

`GET /lessons/{id}` returns metadata, sidecar materials, and available subtitles.

## Acceptance

- Sidecar `.srt`/`.vtt` files are listed with language guessed from filename
- Sidecar `.pdf`/`.md`/`.txt` files are listed as materials

## Spec diff

yes — `GET /lessons/{id}`, schema for materials/subtitles

## Codegen impact

yes

## Design impact

no

## Tests

_To be defined when picked up._

## Sub-steps

- [x] spec + regen (commits `e36b5ed`, `f44ce5a`)
- [x] `Lesson` aggregate with `materials` and `subtitles` collections at `apps/backend/src/modules/catalog/domain/lesson/`
- [x] discovery during scan: `stem-match.ts` normalises composite (`1.1 Title`/`1.1. Title`) + numeric (`01 - Title`) prefixes; sidecars (`.pdf`/`.md`/`.txt`/`.png`/`.jpg`/`.srt`/`.vtt`) attach to the matching `.mp4` and stop emitting `unsupported-extension` ScanErrors
- [x] `GetLessonQuery` + handler — admin shortcut, non-admin grant via `AuthorizationService.canSee({ kind: 'lesson', id, courseId, libraryId })`, `PermissionDenied` without grant
- [x] DTO mapper strips raw `path` fields (NFR-S-01) — handler test asserts no `path` anywhere

## Notes

- Completed: 2026-04-26
- Result: T-2026-04-26-018 — three commits on `feat/lesson-material`: `e36b5ed` (spec), `f44ce5a` (codegen), `1b4533f` (impl). Backend tests 355/355; lint + typecheck clean.
- `Scan.discoveredLessons[]` lives on the aggregate in-memory only — not persisted to a `discovered_lesson` table. Lesson rows are inserted by a future scan-materialise story; storing a parallel table now would duplicate state.
- Sidecars don't bump `filesAdded`/`filesUpdated` — only video files do, preserving the prior incremental-scan no-op invariant.
- The Neovim "mass ScanError" pattern (`1.1. Почему Vim.{pdf,png,txt}` next to `1.1 Почему Vim.mp4`) now produces zero `unsupported-extension` errors.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
