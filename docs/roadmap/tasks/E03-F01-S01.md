# E03-F01-S01 — Import bundle tokens.json verbatim; wire custom design-tokens pipeline

**Status:** ⬜ Not started
**Stage:** A
**Owner:** claude
**Estimated duration:** 2 days

**Epic:** E03 — Design tokens
**Feature:** F01 — Token source

## Dependencies

- [E00-F01-S01](./E00-F01-S01.md) — Place the Claude Design bundle under docs/design/
- [E01-F01-S02](./E01-F01-S02.md) — Shared TypeScript & lint configs

## Spec / Design references

- docs/design/shared/tokens.json
- AGENTS.md §7.9

## Goal

One token source, three outputs (CSS + TS + Dart), all faithful to the bundle.

## Acceptance

- `packages/design-tokens/tokens.json` is identical to `docs/design/shared/tokens.json`
- `pnpm design:build` produces `apps/web/assets/tokens.css`, `packages/design-tokens/src/generated/` (TS), and `apps/mobile/lib/design/tokens.dart`
- Generated files include a banner: "Generated from packages/design-tokens — do not edit by hand"

## Spec diff

none

## Codegen impact

no

## Design impact

yes — palette + type + spacing + radius + elevation + motion

## Tests

snapshot of generated outputs; failure if tokens.json is edited without re-running the generator

## Sub-steps

- [ ] copy `docs/design/shared/tokens.json` to `packages/design-tokens/tokens.json`
- [ ] write `packages/design-tokens/src/load.ts` — reads tokens.json
- [ ] write `packages/design-tokens/src/parse.ts` — normalises token tree
- [ ] write `packages/design-tokens/src/emit-scss.ts` — emits `apps/web/assets/tokens.css` with `:root, [data-mode="dark"]` + `[data-mode="light"]` blocks
- [ ] include the IBM Plex font import line at the top of `tokens.css`
- [ ] write `packages/design-tokens/src/emit-typescript.ts` — emits typed constants to `packages/design-tokens/src/generated/`
- [ ] write `packages/design-tokens/src/emit-dart.ts` — emits `Tokens.color.surface`, `Tokens.space.s4`, plus `Tokens.theme.dark|light` `ThemeData` factory
- [ ] add `design:build` npm script; commit generated outputs

## Notes

_Empty initially; agent fills in during work._

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
