# E22-F01-S06 — release.yml — build + publish API+web Docker image to Forgejo registry

**Status:** 🔄 In progress
**Stage:** A
**Owner:** claude
**Estimated duration:** 2 days

**Epic:** E22 — CI/CD
**Feature:** F01 — GH Actions

## Dependencies

- [E22-F01-S02](./E22-F01-S02.md) — ci.yml with parallel jobs per app/package

## Spec / Design references

_None_

## Goal

Tagged release builds and publishes a multi-arch image.

## Acceptance

- Images on the Forgejo OCI registry under
  `code.homelab.local/<ns>/courseshelf-{backend,web,proxy,centrifugo}:{tag,major.minor,major,latest}`.
- amd64 only for v1; arm64 deferred to a follow-up story.
- Forgejo Release page populated with a Conventional-Commits-derived
  changelog and a deploy-ready artefact bundle.

## Spec diff

none

## Codegen impact

no

## Design impact

no

## Tests

_To be defined when picked up._

## Sub-steps

- [ ] Templated Centrifugo config — envsubst at boot from
      `CENTRIFUGO_ALLOWED_ORIGINS` / `CENTRIFUGO_API_KEY` /
      `CENTRIFUGO_TOKEN_HMAC_SECRET` instead of mounting hardcoded JSON.
- [ ] `APP_VERSION` build arg wired into backend + web Dockerfiles.
- [ ] `docker/compose.release.yml` — image-pulling variant alongside
      the existing build-locally `compose.prod.yml`.
- [ ] `cliff.toml` + `pnpm release:notes` → Conventional-Commits changelog.
- [ ] `.env.release.example` — full env contract for the release path.
- [ ] `.forgejo/workflows/release.yml` — tag-triggered (vMAJOR.MINOR.PATCH-release).
- [ ] `docs/release.md` runbook + restructured `docs/deployment.md`
      (registry-pull becomes the primary path).

## Notes

Tag pattern: `vMAJOR.MINOR.PATCH-release` (e.g. `v0.2.0-release`).
Workflow rejects anything else.

Registry: Forgejo's built-in OCI registry at `code.homelab.local`.
GH Container Registry path is intentionally deferred — no Docker Hub
or GitHub credentials are required.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
