# T-2026-09-18-r3-events — carry filesUpdated on the scan lifecycle channel

- Created: 2026-09-18
- Owner: claude
- Spec: tuxedo 192
- Goal: `ScanLifecycleNotifier` stops showing a hardcoded `updated=0` — the
  `scans:user:{userId}` wire carries the `filesUpdated` count the domain
  already computes, the same way it already carries `filesAdded`.
- Acceptance:
  - A running or finished scan card in the floating notifier shows the real
    updated-files count, not always 0.
  - `ScanLifecycleProgress` and `ScanLifecycleFinished` both require
    `filesUpdated: integer, minimum 0`, shaped like the existing `filesAdded`.
- Spec diff: `packages/specs/asyncapi/centrifugo.yaml` —
  `ScanLifecycleProgress` and `ScanLifecycleFinished` gain required
  `filesUpdated`. Patch bump (0.3.0 → 0.3.1).
- Codegen impact: yes — `pnpm spec:codegen` step 4/4 regenerates
  `packages/api-client-ts/src/realtime/channels.ts`.
- Design impact: none.
- Tests: unit only — existing `run-scan.handler.spec.ts` publish-shape casts
  already tolerate the new field; existing `scanLifecycle.spec.ts` /
  `ScanLifecycleNotifier.spec.ts` call `applyEvent` with untyped literals so
  they stay green without edits.
- Sub-steps:
  - [x] `centrifugo.yaml`: add `filesUpdated` to both schemas, bump version
  - [x] `run-scan.handler.ts`: publish `filesUpdated: scan.filesUpdated` at
        both `progress` and `finished` call sites
  - [x] `apps/web/app/stores/scanLifecycle.ts`: thread `filesUpdated` through
        `ActiveScan` and `applyEvent`
  - [x] `ScanLifecycleNotifier.vue`: bind `:updated="card.filesUpdated"`
  - [x] `pnpm spec:codegen` (with fvm PATH), commit generated diff separately
  - [x] `pnpm --filter @app/backend test run-scan.handler.spec.ts` — 72 passed
  - [x] `pnpm --filter @app/web test scanLifecycle` — 22 passed
  - [ ] Open PR, wait for CI, move card to `done/`
- Status: in-progress
- Blockers: —
