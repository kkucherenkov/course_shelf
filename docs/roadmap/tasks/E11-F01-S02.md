# E11-F01-S02 — Wire @app/api-client-ts with bearer interceptor + auth store

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 2 days

**Epic:** E11 — Web foundations
**Feature:** F01 — Nuxt + Nuxt UI shell

## Dependencies

- [E11-F01-S01](./E11-F01-S01.md) — Bootstrap Nuxt SPA with Nuxt UI v4, Tailwind 4, and tokens

## Spec / Design references

- AGENTS.md §7.4
- DESIGN.md §7.3
- DESIGN.md §10.5

## Goal

Every HTTP call goes through `@app/api-client-ts`; bearer token resolved from a Pinia auth store backed by Better Auth client.

## Acceptance

- `stores/auth.ts` Pinia store wraps `better-auth/client`; exposes `signIn`, `signOut`, `session`
- `createClient` from `@app/api-client-ts` is configured with a bearer interceptor that reads from the auth store
- All requests to `/api/v1/*` include `Authorization: Bearer <token>`
- On `401`, the interceptor attempts token refresh via Better Auth; if that fails, clears session and routes to `/sign-in`
- No `useApi`, `useAuth`, or `useAuthToken` composables

## Spec diff

none

## Codegen impact

no

## Design impact

no

## Tests

_To be defined when picked up._

## Sub-steps

- [x] `stores/auth.ts` — Pinia store wrapping `better-auth/vue` (deviation: bearer captured via `set-auth-token` response header `onSuccess` hook — see Notes)
- [x] `plugins/api.client.ts` — configures the singleton `client` from `@app/api-client-ts` with a bearer request interceptor + 401 response interceptor
- [x] bearer interceptor reads token from the auth store; retry-on-401 calls `auth.refresh()` once before clearing + redirecting
- [x] `middleware/auth.global.ts` redirects unauthenticated users to `/login` (deviation: card said `/sign-in`; codebase has no `/sign-in` route)

## Notes

- Completed: 2026-04-27
- Result: T-2026-04-27-034 — six commits on `feat/web-auth-store`: `07374de` (Pinia install), `72e11c4` (auth store), `0780768` (api.client plugin), `2155d9e` (auth.global middleware), `f247814` (login + layout migration + delete legacy composables), `b3647cf` (task-card flip in active.md). Web tests 17/17 (6 token-page + 11 auth-store); lint + typecheck clean.
- Deviation 1: `bearerClient()` plugin is **not** present in `better-auth@1.6.8`. The store captures the bearer token by attaching an `onSuccess` hook to `@better-fetch/fetch` that reads the `set-auth-token` response header on sign-in and persists it to `localStorage`. Functionally equivalent to the published-but-not-yet-shipped plugin.
- Deviation 2: redirect target is `/login` (the existing route) rather than `/sign-in` (what the card says). Avoids a cosmetic rename touching every link.
- Deviation 3: a separate `plugins/api.client.spec.ts` was deferred — the plugin needs a full Nuxt runtime context that's awkward to fake. The 11 auth-store tests cover the substance (token capture, persist, refresh, clear, error paths). Plugin behaviour is verified by manual smoke when the web container is up.
- Deviation 4: `useApiShape.ts` had zero callers and was deleted alongside `useApi.ts` and `useAuth.ts`. The card explicitly required no `useApi`/`useAuth`/`useAuthToken` composables remain.
- `@app/api-client-ts/src/index.ts` was updated to re-export the singleton `client` from the generated bundle so the plugin can configure it. This is the package's hand-written entry file (not generated) — no codegen rule violated.
- Auth chain on the SPA: middleware checks `authStore.isAuthenticated` → if false and route not in `{ /login, /signup, /__tokens }`, `navigateTo('/login')`. Plugin interceptors run on every `client` call; 401 retries once after `auth.refresh()`, then clears state and routes to `/login` if the refresh failed.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
