# E17-F02-S10 — AppBookmarkList (Flutter) — bookmark list with hover edit/delete

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E17 — Mobile widget catalog
**Feature:** F02 — Domain components (from cs-components)

## Dependencies

- [E17-F02-S04](./E17-F02-S04.md) — Bookmark, BookmarkAdd, NoteEditor (Flutter equivalents)

## Spec / Design references

- docs/design/cs-components/

## Goal

Flutter equivalent of the web `AppBookmarkList` — a bookmark list with hover edit/delete (E17-F02-S04 ports the Bookmark/NoteEditor widgets but omits the list).

## Acceptance

- Matches the bundle's AppBookmarkList spec + states
- Uses design tokens only (no hard-coded values)
- Widgetbook use case + colocated golden/widget test

## Spec diff

none

## Codegen impact

no

## Design impact

yes

## Tests

golden of the widget states

## Sub-steps

- [x] port the AppBookmarkList widget from the @app/ui equivalent
- [x] Widgetbook use case covering its states
- [x] colocated golden/widget test

## Notes

Coverage-gap story — the web `@app/ui` component exists but had no mobile card. Added 2026-07-14.

- Completed: 2026-07-17
- Result: https://github.com/kkucherenkov/course_shelf/pull/151

Composes the already-shipped `AppBookmark` / `AppBookmarkAdd` / `AppEmptyState`
rather than rebuilding them. The empty state is suppressed while an add row is
in flight, mirroring the web's `v-else-if="addTime === undefined"`.
`BookmarkEntry.label` is non-nullable (`''` default) where the web has
`label?: string` — absent and blank render identically through `AppBookmark`.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
