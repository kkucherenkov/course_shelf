# E09-F02-S02 — Notes endpoints (one per user+lesson)

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E09 — Learning
**Feature:** F02 — Bookmarks & notes

## Dependencies

- [E04-F02-S03](./E04-F02-S03.md) — Mount express-openapi-validator for /api/v1/\*

## Spec / Design references

- PRD.md FR-NOTE-02

## Goal

Free-form Markdown note per (user, lesson).

## Acceptance

- `PUT /notes` upserts the single note for a (user, lesson)
- `GET /notes/{lessonId}` returns the user's note

## Spec diff

yes

## Codegen impact

yes

## Design impact

no

## Tests

_To be defined when picked up._

## Sub-steps

- [x] spec + regen (commits `a0fd960`, `4ff5f71`)
- [x] handlers and controller — `NotesController` in `LearningModule`; PUT/GET/DELETE; idempotent delete; GET + DELETE follow no-oracle rule (missing lesson → 403)

## Notes

- Completed: 2026-04-26
- Result: T-2026-04-26-024 — three commits on `feat/notes`: `a0fd960` (spec), `4ff5f71` (codegen), `ef7f86f` (impl). Backend tests 591/591; lint + typecheck clean.
- DELETE for missing lesson → `PermissionDenied` (no-oracle rule), consistent with GET. UPSERT keeps `LessonNotFoundError` because actor must have proven existence to write content.
- `deleteByUserAndLesson` uses `prisma.deleteMany` (returns count) instead of `delete` (which throws on missing) — clean idempotency without try/catch on P2025.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
