# E07-F01-S01 — AccessGrant aggregate + admin endpoints

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 2 days

**Epic:** E07 — Access control
**Feature:** F01 — Grants & enforcement

## Dependencies

- [E06-F01-S01](./E06-F01-S01.md) — Library aggregate + repository port

## Spec / Design references

- PRD.md FR-AUTH-04
- PRD.md FR-AUTH-05

## Goal

Admins can grant a user READ access to a library or to a specific course.

## Acceptance

- `POST /access/grants` (admin) creates a grant
- `DELETE /access/grants/{id}` revokes
- `GET /access/grants?userId=` lists a user's grants

## Spec diff

yes

## Codegen impact

yes

## Design impact

no

## Tests

_To be defined when picked up._

## Sub-steps

- [x] spec + regen (commits `d82bceb`, `cba2ae2`)
- [x] aggregate + repo (`apps/backend/src/modules/access/{domain,infra}/`)
- [x] commands and queries (`RegisterGrantCommand`, `RevokeGrantCommand`, `ListUserGrantsQuery`, plus a `GetGrantQuery` for the post-write re-read)
- [x] admin guard via Better Auth session role check (`apps/backend/src/common/auth/admin.guard.ts`)

## Notes

- Completed: 2026-04-26
- Result: T-2026-04-26-013 — three commits on `feat/access-grant`: `d82bceb` (spec), `cba2ae2` (codegen), `9315210` (impl). Tests 102/102; lint + typecheck clean.
- Targets are a discriminated union (`library` | `course`); v1 only issues READ. Course grants are accepted now even though the Course aggregate (E06-F03-S01) hasn't landed — keeps the contract stable for the admin UI.
- Idempotency on `(userId, targetKind, libraryId, courseId)` via Prisma composite unique; P2002 surfaces as `GrantAlreadyExistsError` (409).
- `AdminGuard` is a fresh standalone guard (no admin precedent in the repo); `RoleGuard` mixin in `auth.guard.ts` had a TODO stub but no real role check, so a dedicated guard was the cleaner path.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
