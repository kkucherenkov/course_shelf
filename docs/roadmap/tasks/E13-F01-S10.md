# E13-F01-S10 — AppEmptyState, AppErrorState, AppNoPermission

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E13 — Web component catalog
**Feature:** F01 — Primitive families (from cs-foundations)

## Dependencies

- [E12-F01-S01](./E12-F01-S01.md) — Configure Storybook for Vue 3 + Nuxt UI v4 + tokens
- [E13-F01-S01](./E13-F01-S01.md) — IconCS — port the bundle's 61-icon family to Vue
- [E13-F01-S02](./E13-F01-S02.md) — AppButton + AppIconButton (matches .btn classes)

## Spec / Design references

- docs/design/cs-foundations/, docs/design/shared/tokens.css

## Goal

AppEmptyState, AppErrorState, AppNoPermission matching the bundle.

## Acceptance

- States in stories match the bundle's state matrix
- Uses design tokens only (no hard-coded values)

## Spec diff

none

## Codegen impact

no

## Design impact

yes

## Tests

_To be defined when picked up._

## Sub-steps

- [x] AppEmptyState (icon `folder` default, neutral palette)
- [x] AppErrorState (icon `alert` default, `--status-error-fg` palette)
- [x] AppNoPermission (icon `lock` default, `--status-warning-fg` palette)

## Notes

- Completed: 2026-04-27
- Result: T-2026-04-27-040 — commit `d8b9d58` on `feat/primitives-batch-a`. Part of primitives batch A.
- Three components share the same shape (illustration / title / body / action slots) but with different default icons and palettes. Three near-duplicate templates rather than an `AppStateBase` abstraction — easier to read, unlikely to drift.
- All three carry `role="status"` (polite live region).

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
