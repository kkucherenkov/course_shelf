# E30-F01-S02 — Scraper inventory in the admin UI

**Status:** ⬜ Not started
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E30 — Scraper plugins
**Feature:** F01 — Configuration-defined scrapers

## Dependencies

- [E30-F01-S01](./E30-F01-S01.md) — Declarative scraper definitions

## Spec / Design references

- v2 direction: “Плагины скраперов” (P)

## Goal

See which scrapers loaded, which were rejected, and why — without reading container logs.

## Acceptance

- `GET /api/v1/admin/scrapers` reports every configured scraper, its origin (built-in or definition file), and any load error
- An admin screen lists them with the rejection reason where present
- Strings via `t()` in `en` and `ru`

## Spec diff

openapi.yaml — extend the existing `/admin/scrapers` response

## Codegen impact

yes

## Design impact

no

## Tests

Handler spec including a rejected definition; component spec for the list.

## Sub-steps

- [ ] OpenAPI + codegen
- [ ] Handler
- [ ] Admin screen + specs

## Notes

- `/api/v1/admin/scrapers` already exists; this widens its response rather than adding a route.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done · ❌ Cancelled

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
