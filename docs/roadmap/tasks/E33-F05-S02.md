# E33-F05-S02 — Admin block leaves the main navigation

**Status:** ⬜ Not started
**Stage:** A
**Owner:** claude
**Estimated duration:** 0.5 days

**Epic:** E33 — Ownership and taxonomy
**Feature:** F05 — Admin navigation

## Dependencies

- [E33-F05-S01](./E33-F05-S01.md) — `/admin` becomes a hub

## Spec / Design references

- [ADR-0013](../../adr/0013-ownership-taxonomy-and-ratings.md) — the 2.0 scope
  this epic implements; this card carries none of its five decisions

## Goal

Give the main navigation back to the library. Six admin rows in it make the
product look like an admin panel that happens to play video.

## Acceptance

- The main nav is Home and Browse
- Admin is reached from the avatar menu, beside Settings, and only for an admin
- The active-item logic no longer has to disambiguate six `/admin/*` prefixes
- Strings via `t()` in `en` and `ru`; `pnpm check:i18n` green

## Spec diff

none

## Codegen impact

no

## Design impact

no

## Tests

Layout specs: the nav for a non-admin, the nav for an admin, and the admin
entry present in the avatar menu for an admin only.

## Sub-steps

- [ ] Move the entry in `layouts/default.vue` + specs
- [ ] Locale keys in both languages

## Notes

- Opened from `tuxedo` 228 (second half).
- Cost accepted by the maintainer: an admin page is two clicks away instead of
  one.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done · ❌ Cancelled

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
