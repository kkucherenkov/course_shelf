# E02-F02-S04 — `pnpm spec:codegen` aggregate + contract-test scaffold

**Status:** ⬜ Not started
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E02 — API contracts as the spine
**Feature:** F02 — Codegen pipeline

## Dependencies

- [E02-F02-S01](./E02-F02-S01.md) — Generate `@app/api-client-ts` from the spec
- [E02-F02-S02](./E02-F02-S02.md) — Generate NestJS DTOs as `@app/api-client-ts/server` subpath
- [E02-F02-S03](./E02-F02-S03.md) — Generate Dart Dio client as `@app/api-client-dart`

## Spec / Design references

- AGENTS.md §8

## Goal

One command regenerates all three clients; a Dredd/Prism contract-test scaffold validates the bundled spec against the running backend.

## Acceptance

- `pnpm spec:codegen` runs `spec:validate` → `spec:bundle` → all three generators in order
- `pnpm spec:contract-test` exists (Dredd/Prism); for v1 it asserts every operationId has a matching test file
- `apps/backend/test/contract/` runner loads `dist/openapi.json`

## Spec diff

none

## Codegen impact

yes

## Design impact

no

## Tests

_To be defined when picked up._

## Sub-steps

- [ ] add `spec:codegen` script chaining sub-tasks
- [ ] add `apps/backend/test/contract/` with a runner that loads `dist/openapi.json`
- [ ] add `spec:contract-test` npm script wrapping Dredd/Prism
- [ ] CI gate: error if `paths.*` count > test files count

## Notes

_Empty initially; agent fills in during work._

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
