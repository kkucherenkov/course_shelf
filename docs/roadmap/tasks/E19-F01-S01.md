# E19-F01-S01 — DownloadsBloc with resumable encrypted downloads

**Status:** ⬜ Not started
**Stage:** A
**Owner:** claude
**Estimated duration:** 4 days

**Epic:** E19 — Mobile offline
**Feature:** F01 — Download manager

## Dependencies

- [E18-F02-S01](./E18-F02-S01.md) — Stage A · Mobile player BLoC + portrait/landscape lesson screen
- [E15-F02-S01](./E15-F02-S01.md) — Drift schema + DAOs

## Spec / Design references

- docs/design/DESIGN_BRIEF.md

## Goal

BLoC-managed queue with byte-range resumption and AES-GCM encryption at rest.

## Acceptance

- Pause + resume + retry
- Survives app kill and OS reboot (subject to platform background limits)
- Per-item progress emitted as state

## Spec diff

none

## Codegen impact

no

## Design impact

no

## Tests

bloc_test + integration_test simulating a flaky network

## Sub-steps

- [ ] events: `EnqueueLesson`, `EnqueueCourse`, `Pause`, `Resume`, `Cancel`, `Retry`
- [ ] file writer with byte-range continuation
- [ ] device-bound key in secure storage
- [ ] background scheduling via `workmanager` (Android), `BGTaskScheduler` (iOS)

## Notes

Byte source exists (`/lessons/{id}/stream-url`, materials `/download-url`) but the spec does NOT contract `Range`/`Accept-Ranges` — confirm the stream server honours Range before committing to resumable. Signed URLs default TTL 5 min → a pause/resume that outlives the token must re-mint the URL (add token-refresh-on-resume). AES-GCM at-rest + `workmanager`/`connectivity_plus` are net-new client deps (not in pubspec yet).

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
