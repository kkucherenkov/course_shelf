# E10-F01-S01 — CourseProgressProjector + read model table

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 2 days

**Epic:** E10 — Cross-context coupling
**Feature:** F01 — Course progress read model

## Dependencies

- [E06-F03-S02](./E06-F03-S02.md) — Lesson + Material + Subtitle read model
- [E09-F01-S01](./E09-F01-S01.md) — LessonProgress aggregate + record endpoint

## Spec / Design references

- DESIGN.md §4.1
- DESIGN.md §10.7

## Goal

A denormalized per-(user, course) row keeps completion ratio fast at read time.

## Acceptance

- `LessonCompleted` triggers an update to `CourseProgressReadModel`
- `GET /home/continue-watching` reads from the projection only

## Spec diff

yes — `GET /home/continue-watching`

## Codegen impact

yes

## Design impact

no

## Tests

_To be defined when picked up._

## Sub-steps

- [x] add Prisma model `CourseProgressReadModel` (composite unique on `(userId, courseId)`, index on `(userId, lastSeenAt DESC)`)
- [x] event handlers in `apps/backend/src/modules/catalog/application/event-handlers/` — `LessonCompletedHandler` + `LessonProgressRecordedHandler` (new event added to Learning so the projection tracks in-progress views, not just completions)
- [x] backfill command at `apps/backend/scripts/rebuild-projections.ts` exposed via `pnpm --filter @app/backend rebuild-projections` — idempotent, safe after schema migrations
- [x] spec + regen for `GET /api/v1/home/continue-watching` (commits `6befc08`, `4e367fa`)
- [x] Course/Lesson DTO mappers updated to read from the projection (single bulk lookup for list, single keyed read for get) — v1 zero-placeholder progress fields are gone

## Notes

- Completed: 2026-04-26
- Result: T-2026-04-26-022 — three commits on `feat/progress-projector`: `6befc08` (spec), `4e367fa` (codegen), `3c0e35e` (impl) + `99b84c0` (task archive). Backend tests 486/486; lint + typecheck clean.
- New `HomeController` in `catalog/home.controller.ts` keeps `/home/*` routes separate from `CoursesController` — anticipates more home-row queries in future stories.
- Totals counted eagerly inside event handlers (one extra count per event) so the read path stays a single index scan.
- `librarySlug` omitted from the response — `Library` aggregate carries `name` but no `slug` field. Optional in the spec; documented in handler.
- `LessonProgressRecorded` is a net-new event class, not in the original card. Without it, "Continue watching" would only ever surface fully-completed courses — wrong UX.
- New `common/learning-progress/` DI module gives Catalog access to `LESSON_PROGRESS_REPOSITORY` without a hard `LearningModule` import (boundaries-config compliant).

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
