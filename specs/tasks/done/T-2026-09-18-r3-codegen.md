## T-2026-09-18-r3-codegen — guard the Dart SDK pin in codegen, drop a dead build_runner flag

- Created: 2026-09-18
- Owner: claude
- Spec: tuxedo 246, tuxedo 89 (issue #492)
- Goal: stop `pnpm spec:codegen` from producing a plausible Dart-client diff on
  machines whose Dart SDK doesn't match CI's, and remove a build_runner flag
  that no longer does anything.
- Acceptance:
  - `pnpm spec:codegen` fails with a clear message (expected version, where
    the pin lives, that CI regenerates regardless) when local `dart --version`
    doesn't match the SDK bundled with the Flutter version pinned in
    `.github/actions/setup-cs/action.yml`.
  - `dart run build_runner build` no longer passes
    `--delete-conflicting-outputs`.
  - Codegen drift guard in CI still passes — generated output is unchanged by
    dropping the dead flag.
- Spec diff: none (script change, not a wire contract)
- Codegen impact: no — no bundle/schema change, nothing to regenerate
- Design impact: none
- Tests: guard's version-mismatch branch exercised manually against the
  local Dart SDK (3.13.2, mismatches the 3.12.2 pin as expected); CI's
  Codegen drift guard job is the proof for the flag removal, since the
  version guard now blocks the dart-\* steps from running locally.
- Sub-steps:
  - [x] Confirm Dart SDK bundled with Flutter 3.44.4 (3.12.2, via
        `releases_linux.json` — not a guess)
  - [x] Add `EXPECTED_DART_VERSION` + `assertDartVersion()` guard before the
        dart-\* steps in `packages/specs/scripts/codegen.ts`
  - [x] Drop `--delete-conflicting-outputs` from the build_runner invocation
  - [x] Point `.github/actions/setup-cs/action.yml`'s bump-here comment at
        the new guard constant
  - [x] Open PR, wait for CI (Codegen drift guard proves the flag removal is
        a no-op)
- Status: done
- Blockers: —
- Completed: 2026-09-18
- Result: https://github.com/kkucherenkov/course_shelf/pull/740
