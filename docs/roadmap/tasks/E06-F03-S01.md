# E06-F03-S01 — Course aggregate + slug uniqueness + section ordering

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 2 days

**Epic:** E06 — Catalog bounded context
**Feature:** F03 — Course / Section / Lesson aggregates

## Dependencies

- [E06-F02-S01](./E06-F02-S01.md) — Scan aggregate, FsAdapter, incremental scan

## Spec / Design references

- PRD.md FR-LIB-02

## Goal

Courses encode the structural invariants of the catalog.

## Acceptance

- Slug is unique within a library
- Sections have unique `position` within a course
- Lessons have unique `position` within a section
- `course.addSection`, `course.reorderSection` enforce invariants

## Spec diff

yes — `GET /courses`, `GET /courses/{id}`, `PATCH /courses/{id}`

## Codegen impact

yes

## Design impact

no

## Tests

_To be defined when picked up._

## Sub-steps

- [x] spec + regen (commits `acd4b97`, `bd2251f`)
- [x] aggregate + value objects (`Slug`, `Title`, `Position`) at `apps/backend/src/modules/catalog/domain/course/`
- [x] Prisma `Course` + `Section` models with `(libraryId, slug)` + `(courseId, position)` unique; manual migration SQL
- [x] `UpdateCourseMetadataCommand` (admin-only PATCH; `CourseSlugAlreadyTakenError` 409 on P2002)
- [x] `ListCoursesQuery` (filters via `AuthorizationService.canSee` for non-admins), `GetCourseQuery` (`PermissionDenied` without grant)
- [x] read-side DTOs include `progress: { percent: 0, lessonsCompleted: 0, lessonsTotal: 0 }` placeholder (filled in E10-F01-S01)

## Notes

- Completed: 2026-04-26
- Result: T-2026-04-26-017 — three commits on `feat/course-aggregate`: `acd4b97` (spec), `bd2251f` (codegen), `ab0854d` (impl). Backend tests 273/273; lint + typecheck clean.
- Section is composed inside Course (no separate `SectionRepository`); persistence uses `prisma.$transaction` + delete-and-recreate sections — diff-based upsert was deemed overkill for v1.
- Lessons + Material + Subtitle land in **E06-F03-S02**.
- `CoursesController` is a separate file alongside `ScansController` to keep route ownership clean.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
