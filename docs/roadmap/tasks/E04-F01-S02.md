# E04-F01-S02 — `/healthz` and `/readyz` outside the OpenAPI spec

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E04 — Backend skeleton + Better Auth
**Feature:** F01 — Nest application bootstrap

## Dependencies

- [E04-F01-S01](./E04-F01-S01.md) — Bootstrap apps/backend with Prisma, modules skeleton, and shared kernel

## Spec / Design references

- PRD.md FR-OPS-01

## Goal

Always-public liveness/readiness probes that bypass auth and the spec validator.

## Acceptance

- `GET /healthz` always 200
- `GET /readyz` 200 only when DB connection succeeds
- Both routes are exempted from the validator and the global auth guard
- Runs as part of the Docker Compose stack; tested via supertest as well

## Spec diff

none

## Codegen impact

no

## Design impact

no

## Tests

e2e supertest for both

## Sub-steps

- [x] add `OpsController` with `/healthz` + `/readyz`
- [x] decorate with `@AllowAnonymous()` (added in same task — F02-S02 landed alongside)
- [x] exclude probes from `setGlobalPrefix('api')` so they sit at the root, outside the validator's mount path

## Notes

- Completed: 2026-04-27
- Result: T-2026-04-27-028 — commit `f455d78` on `feat/e04-alignment`. Backend tests +3 (650 total); lint + typecheck clean.
- `/healthz` always returns `{ "status": "ok" }`; `/readyz` runs `SELECT 1` against Prisma, returns `{ "status": "ok" }` on success or `503` + `{ "status": "down" }` on failure.
- Both routes are excluded from `setGlobalPrefix('api')` (root paths, not `/api/v1/...`) so they sit outside the OpenAPI validator's `/api` mount and outside the spec.
- `@AllowAnonymous()` applied at controller level so the global `SessionGuard` (registered in `33cedf6`) skips them.
- Existing richer `GET /api/v1/health` (DB + Redis + Centrifugo dependency snapshot) preserved as the operations dashboard endpoint — `/healthz` and `/readyz` are the no-frills orchestrator probes.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
