# E15-F01-S01 — Flutter bootstrap, theme from tokens

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 2 days

**Epic:** E15 — Mobile foundations
**Feature:** F01 — App skeleton

## Dependencies

- [E03-F01-S02](./E03-F01-S02.md) — Tailwind/Nuxt UI and Flutter theme adapters + Foundations canvas

## Spec / Design references

- AGENTS.md
- docs/design/DESIGN_BRIEF.md

## Goal

Skeleton with feature-folder layout, BLoC dependencies, theme derived from generated tokens.

## Acceptance

- `flutter analyze` clean
- `flutter test` passes (single smoke test)
- Token demo screen renders all tokens

## Spec diff

none

## Codegen impact

no

## Design impact

yes

## Tests

golden of the token demo screen

## Sub-steps

- [x] `flutter create` + reorganize to layout in docs/design/DESIGN_BRIEF.md
- [x] add `flutter_bloc`, `equatable`, `dio`, `drift`, `flutter_secure_storage`, `widgetbook` — note: `drift`/`widgetbook` are deferred (they belong to E15-F02 / E16)
- [x] `app.dart` builds `ThemeData.fromTokens()`

## Notes

- Bootstrap + get_it DI + Dio+bearer + auth already shipped in apps/mobile. Remaining work = the headline: wire `packages/ui_flutter` AppTheme from `tokens.g.dart` + token-demo screen + golden. Theme-from-tokens is UNBLOCKED — `tokens.g.dart` now carries the full palette incl. the accent 50–950 scale (added 2026-07-14). main.dart still uses a placeholder `ColorScheme.fromSeed`.
- Completed: 2026-07-15
- Result: (PR pending)
- Shipped as `AppTheme.light()` / `AppTheme.dark()` in `packages/ui_flutter`,
  not `ThemeData.fromTokens()` — the card's name predates the scaffolding
  comment in `main.dart`, which already specified `AppTheme`.
- Scope grew to bundling IBM Plex on web + mobile: the tokens name the family
  but nothing loaded it on either platform, so web rendered system-ui.
  Bundling mobile-only would have made the platforms diverge.
- Token demo screen lives at `lib/src/example/token_demo_screen.dart` (inside
  `lib/` so E16's Widgetbook can import it) and mirrors the token sections of
  `apps/web/app/pages/dev/foundations.vue`.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
