# E13-F01-S09 — AppProgressLinear, AppProgressCircle, AppSpinner, AppSkeleton

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E13 — Web component catalog
**Feature:** F01 — Primitive families (from cs-foundations)

## Dependencies

- [E12-F01-S01](./E12-F01-S01.md) — Configure Storybook for Vue 3 + Nuxt UI v4 + tokens

## Spec / Design references

- docs/design/cs-foundations/, docs/design/shared/tokens.css

## Goal

AppProgressLinear, AppProgressCircle, AppSpinner, AppSkeleton matching the bundle.

## Acceptance

- States in stories match the bundle's state matrix
- Uses design tokens only (no hard-coded values)

## Spec diff

none

## Codegen impact

no

## Design impact

yes

## Tests

_To be defined when picked up._

## Sub-steps

- [x] AppProgressLinear (4px, `thin` 2px, indeterminate animation, full ARIA)
- [x] AppProgressCircle (32px default SVG, configurable `size`/`stroke`)
- [x] AppSpinner (sm/md/lg, `currentColor` for theming)
- [x] AppSkeleton (gradient pulse, `width`/`height`/`radius` props)

## Notes

- Completed: 2026-04-27
- Result: T-2026-04-27-040 — commit `6abee87` on `feat/primitives-batch-a`. Part of primitives batch A.
- ProgressLinear renders an indeterminate state when `value === undefined` (35%-wide stripe slides via translate keyframe). Determinate uses `aria-valuenow`; indeterminate omits it.
- ProgressCircle math computed inline (`circumference = 2π·r`, `offset = c·(1−value/100)`); `transform: rotate(-90deg)` so 0% starts at the top.
- Spinner factored as a reusable component but AppButton/AppIconButton's inline `::after` spinner is intentionally left untouched. Future cleanup can converge them.
- Skeleton uses `var(--skeleton-base, var(--surface-overlay))` fallback syntax — works whether the bundle's optional skeleton tokens ship or not.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
