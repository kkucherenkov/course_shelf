# E21-F02-S01 — Rate limit on Better Auth sign-in

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E21 — Admin & operations
**Feature:** F02 — Hardening

## Dependencies

- [E04-F02-S03](./E04-F02-S03.md) — Mount express-openapi-validator for /api/v1/\*

## Spec / Design references

- PRD.md NFR-S-03

## Goal

5 sign-in attempts per 15 min per IP, reverse-proxy-friendly (trust X-Forwarded-For).

## Acceptance

- 6th attempt within window returns 429 with retry-after
- IPv4 + IPv6 supported

## Spec diff

none

## Codegen impact

no

## Design impact

no

## Tests

_To be defined when picked up._

## Sub-steps

- [x] `SignInRateLimitMiddleware` mounted on `POST /api/v1/auth/sign-in/*splat` via `AuthModule.configure`
- [x] in-memory token bucket keyed by `req.ip`; 5 attempts / 15 min rolling window

## Notes

- Completed: 2026-04-27
- Result: T-2026-04-27-031 — commit `5116d9c` on `feat/auth-signin-ratelimit`. Backend tests 660/660; lint + typecheck clean.
- Pattern: `consumer.apply(SignInRateLimitMiddleware).forRoutes({ path: 'api/v1/auth/sign-in/*splat', method: RequestMethod.POST })` — Nest 11 / Express 5 named-wildcard syntax.
- 6th attempt: `429 Too Many Requests` with `Retry-After: <seconds>` header and RFC 9457 problem JSON body emitted directly (middleware runs before exception filters).
- Successful sign-ins also count — a successful brute-force probe is still a probe.
- Bucket pruned per request; in-process Map. Single backend instance assumption — Redis-backed swap is deferred (deliberately, per the card's sub-step note "Redis swap-in deferred").
- The class-level `@Throttle({ default: { limit: 10, ttl: 60_000 } })` on `AuthController` stays — it remains a generic floor across all auth routes (sign-up, get-session, sign-out, refresh).

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
