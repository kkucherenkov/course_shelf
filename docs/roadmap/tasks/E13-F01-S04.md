# E13-F01-S04 — AppCard (.card, .card-lg, .card-hover)

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E13 — Web component catalog
**Feature:** F01 — Primitive families (from cs-foundations)

## Dependencies

- [E12-F01-S01](./E12-F01-S01.md) — Configure Storybook for Vue 3 + Nuxt UI v4 + tokens

## Spec / Design references

- docs/design/cs-foundations/, docs/design/shared/tokens.css

## Goal

AppCard (.card, .card-lg, .card-hover) matching the bundle.

## Acceptance

- States in stories match the bundle's state matrix
- Uses design tokens only (no hard-coded values)

## Spec diff

none

## Codegen impact

no

## Design impact

yes

## Tests

_To be defined when picked up._

## Sub-steps

- [x] `size: 'md' | 'lg'` prop matching `.card` / `.card-lg` (`--radius-md` + 16px / `--radius-lg` + 24px)
- [x] `hoverable: boolean` prop matching `.card-hover` (border + background shift on hover, stays a `<div>`)
- [x] tests for size + hoverable; stories `Default` / `Large` / `Hoverable`

## Notes

- Completed: 2026-04-27
- Result: T-2026-04-27-038 — commit `6f0f481` on `feat/app-card-bundle`. UI tests 356/356 (+4); lint + typecheck clean.
- The card's existing `interactive` prop (focusable button mode) is preserved. `hoverable` is the visual-lift-only variant; setting both has `interactive` win (no double hover styling — verified by spec).
- Token deviation: bundle's `.card-hover` uses `--surface-2`; we use the shipped `--surface-raised`. Documented inline.
- Sectioned (header/body/footer) and flat (default-slot-only) usages share the same outer padding per the bundle's uniform-padding contract — internal dividers between sections preserved.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
