# E04-F02-S02 — Mount Better Auth + register global guard

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E04 — Backend skeleton + Better Auth
**Feature:** F02 — Better Auth integration

## Dependencies

- [E04-F02-S01](./E04-F02-S01.md) — Configure Better Auth with bearer + admin plugins

## Spec / Design references

- DESIGN.md §5.4

## Goal

Routes are authenticated by default; `@AllowAnonymous()` opts out; `@Session()` resolves the current user.

## Acceptance

- `GET /api/v1/ping` returns 401 without a token, 200 with a valid token
- `@Session()` resolves to `{ user: { id, role, ... }, session: {...} }`
- `@AllowAnonymous()` works on `/healthz` and `/readyz`

## Spec diff

none

## Codegen impact

no

## Design impact

no

## Tests

e2e covering both decorators

## Sub-steps

- [x] register `SessionGuard` globally via `APP_GUARD` in `AppModule` (deviation: native, not `AuthModule.forRootAsync`)
- [x] add `@AllowAnonymous()` decorator to opt routes out of the global guard
- [x] add `@Session()` parameter decorator returning `{ user: { id, role, displayName? } }`
- [x] write a `PingController` that uses `@Session()` to verify the auth chain end-to-end
- [x] add the `GET /api/v1/ping` operation to the spec + regenerate clients

## Notes

- Completed: 2026-04-27
- Result: T-2026-04-27-028 — commits `33cedf6` (decorators + global guard + 9-controller refactor), `66d411d` (spec), `e386476` (codegen), `50a863e` (PingController).
- `SessionGuard` reads `ALLOW_ANONYMOUS_KEY` metadata via `Reflector.getAllAndOverride([handler, class])` — the decorator works at both method and class levels.
- On success, the guard attaches a structured `req.session = { user: { id, role, displayName? } }` so `@Session()` can read it without a second `getSession` round-trip.
- `_template/_template.controller.ts` keeps the old `@UseGuards(SessionGuard)` + `resolveActor` style intentionally — it's documented as a counter-example showing the legacy pattern.
- `streaming.controller.ts` marks `getLessonStream` + `getLessonSubtitle` `@AllowAnonymous()`: those routes use a query-string stream token as auth, not a bearer session.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
