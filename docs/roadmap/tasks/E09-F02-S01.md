# E09-F02-S01 — Bookmarks endpoints

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E09 — Learning
**Feature:** F02 — Bookmarks & notes

## Dependencies

- [E04-F02-S03](./E04-F02-S03.md) — Mount express-openapi-validator for /api/v1/\*

## Spec / Design references

- PRD.md FR-NOTE-01

## Goal

Per-user, per-lesson timestamped bookmarks.

## Acceptance

- List, create, update, delete
- Owner-only access enforced

## Spec diff

yes

## Codegen impact

yes

## Design impact

no

## Tests

_To be defined when picked up._

## Sub-steps

- [x] spec + regen (commits `cdd36b7`, `195ee04`)
- [x] `Bookmark` aggregate at `apps/backend/src/modules/learning/domain/bookmark/`; trim + 1–200 char label rule; `update()` enforces at-least-one-field, `label: null` clears
- [x] handlers + controller — `BookmarksController` in `LearningModule`; admin bypass symmetric on UPDATE + DELETE; ownership check on per-id mutations

## Notes

- Completed: 2026-04-26
- Result: T-2026-04-26-023 — three commits on `feat/bookmarks`: `cdd36b7` (spec), `195ee04` (codegen), `421881d` (impl). Backend tests 547/547; lint + typecheck clean.
- Separate `bookmarks.controller.ts` because routes span two path prefixes (`lessons/:lessonId/bookmarks` + `bookmarks/:id`); merging into `ProgressController` would bloat it.
- Admin bypass is symmetric on UPDATE + DELETE so a moderator can correct a malformed position too, not just delete.
- `label: undefined` → no-op; `label: null` → clear; `label: string` → trimmed and length-validated.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
