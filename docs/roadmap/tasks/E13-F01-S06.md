# E13-F01-S06 — AppTabs and AppSegmented controls

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E13 — Web component catalog
**Feature:** F01 — Primitive families (from cs-foundations)

## Dependencies

- [E12-F01-S01](./E12-F01-S01.md) — Configure Storybook for Vue 3 + Nuxt UI v4 + tokens

## Spec / Design references

- docs/design/cs-foundations/, docs/design/shared/tokens.css

## Goal

AppTabs and AppSegmented controls matching the bundle.

## Acceptance

- States in stories match the bundle's state matrix
- Uses design tokens only (no hard-coded values)

## Spec diff

none

## Codegen impact

no

## Design impact

yes

## Tests

_To be defined when picked up._

## Sub-steps

- [x] AppTabs + AppTab with arrow-key nav (commit `89cb69a`)
- [x] AppSegmented + AppSegmentedItem (commit `c5c3bff`)

## Notes

- Completed: 2026-04-27
- Result: T-2026-04-27-040 — part of primitives batch A. Both controls use provide/inject between parent + children, generic over `T extends string | number`.
- Tabs: `[role='tablist']` + `[role='tab']` + roving `tabindex` (only the selected tab is `tabindex=0`); arrow keys cycle focus + selection via DOM-sibling traversal (matches the WCAG tablist pattern from AppRadioGroup in T-037).
- Segmented: `[role='radiogroup']` + `[role='radio']`; selected item gets `--surface-surface` background + `--shadow-xs` (the bundle's `--shadow-1` ships as that alias).

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
