openapi: 3.1.0
info:
  title: App API
  version: 0.16.0
  description: |
    Spec-first HTTP API. Paths include the versioned prefix (`/api/v1`) so
    generated clients can be used without additional base-URL rewriting.

    **Authentication endpoints** live under `/api/v1/auth/*` and follow the
    [Better Auth protocol](https://www.better-auth.com/docs). They are
    deliberately not enumerated here: Better Auth owns that wire contract and
    evolves it independently of our OpenAPI surface. The `cookieAuth` and
    `bearerAuth` security schemes documented below describe how the *rest* of
    this API accepts credentials issued by that namespace — session cookies
    (web) and bearer tokens (mobile, via the `bearer` plugin).
  contact:
    name: App Team
servers:
  - url: http://localhost:3000
    description: Local development
  - url: https://api.example.com
    description: Production (placeholder)
tags:
  - name: Access
    description: User → resource access grants. Admins issue and revoke; everyone else only ever reads their own implicit grants from session context.
  - name: Catalog
    description: Course library registration and lookup.
  - name: Learning
    description: Per-user lesson progress, bookmarks, and notes.
  - name: Ops
    description: Operational endpoints for liveness checks and self-tests.
  - name: Streaming
    description: Lesson streaming and signed URLs.
  - name: system
    description: Health and liveness endpoints
  - name: admin
    description: "Administrator-only operations: dashboard, backups, configuration."
  - name: realtime
    description: Centrifugo connection tokens
  - name: Entities
    description: Instructor, Studio, and Tag entities — browse and lookup.
  - name: Maintenance
    description: Admin-only background maintenance operations.
  - name: Scrapers
    description: Admin-only scraper registry and preview-scrape operations.

paths:
  /api/v1/access/grants:
    post:
      operationId: registerGrant
      summary: Grant a user READ access to a library or course
      description: |
        Idempotent on (userId, target). Returns 409 if the same grant already
        exists. Only admins (`session.user.role === 'admin'`) may call this;
        other authenticated users get 403.
      tags: [Access]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterGrantRequest"
            examples:
              libraryGrant:
                summary: Grant READ access to Conference Recordings
                value:
                  userId: clxvusr0000000000000000001
                  target:
                    kind: library
                    libraryId: clxvp1234567890abcdefghij
                  level: READ
      responses:
        "201":
          description: Grant created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccessGrantDto"
              examples:
                created:
                  value:
                    id: clxvgnt0000000000000000001
                    userId: clxvusr0000000000000000001
                    target:
                      kind: library
                      libraryId: clxvp1234567890abcdefghij
                    level: READ
                    createdAt: "2026-04-25T10:00:00Z"
        "400":
          description: Validation error — missing or malformed fields
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                badInput:
                  value:
                    type: about:blank
                    title: Bad Request
                    status: 400
                    detail: "target.kind must be 'library' or 'course'."
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller does not have the admin role
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: Only admins may issue access grants.
        "404":
          description: Target library or course not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No library with that id exists.
        "409":
          description: A grant for this (userId, target) pair already exists
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                conflict:
                  value:
                    type: about:blank
                    title: Conflict
                    status: 409
                    detail: "A READ grant for user clxvusr0000000000000000001 on library clxvp1234567890abcdefghij already exists."

    get:
      operationId: listGrantsByUser
      summary: List a user's access grants
      description: Returns every grant issued to the given user, both library- and course-scoped.
      tags: [Access]
      security:
        - bearerAuth: []
      parameters:
        - name: userId
          in: query
          required: true
          description: Better Auth user id whose grants should be returned.
          schema:
            type: string
            minLength: 1
      responses:
        "200":
          description: Grant list returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccessGrantListDto"
              examples:
                list:
                  value:
                    items:
                      - id: clxvgnt0000000000000000001
                        userId: clxvusr0000000000000000001
                        target:
                          kind: library
                          libraryId: clxvp1234567890abcdefghij
                        level: READ
                        createdAt: "2026-04-25T10:00:00Z"
                      - id: clxvgnt0000000000000000002
                        userId: clxvusr0000000000000000001
                        target:
                          kind: course
                          courseId: clxvcrs0000000000000000001
                        level: READ
                        createdAt: "2026-04-25T10:05:00Z"
        "400":
          description: userId query parameter is missing or empty
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                missingUserId:
                  value:
                    type: about:blank
                    title: Bad Request
                    status: 400
                    detail: "Query parameter 'userId' is required."
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller does not have the admin role
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: Only admins may list grants for arbitrary users.

  /api/v1/access/grants/{id}:
    delete:
      operationId: revokeGrant
      summary: Revoke an access grant
      description: Permanently removes the grant identified by `id`. The affected user immediately loses the access level the grant provided.
      tags: [Access]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Server-generated cuid identifying the grant to revoke.
          schema:
            type: string
      responses:
        "204":
          description: Grant revoked — no body
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller does not have the admin role
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: Only admins may revoke access grants.
        "404":
          description: Grant not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No grant with that id exists.

  /api/v1/admin/dashboard:
    get:
      operationId: getAdminDashboard
      summary: Operational snapshot for the admin dashboard
      description: |
        Returns counts of major entities (libraries, users, courses, lessons),
        a summary of the most recent scan (if any), and the count of scan
        errors collected in the last 24 hours. Read-only — no side effects.
      tags: [admin]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Dashboard snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminDashboardDto"
              examples:
                populated:
                  summary: A platform with one library, two users, mid-scan
                  value:
                    generatedAt: "2026-04-27T10:30:00Z"
                    counts:
                      libraries: 1
                      users: 2
                      courses: 7
                      lessons: 184
                    latestScan:
                      scanId: clxvscn0000000000000000001
                      libraryId: clxvlib0000000000000000001
                      status: succeeded
                      startedAt: "2026-04-27T09:55:00Z"
                      finishedAt: "2026-04-27T09:58:12Z"
                      filesScanned: 312
                      errorsCount: 2
                    errorsLast24h: 2
                empty:
                  summary: Fresh install — no scans yet
                  value:
                    generatedAt: "2026-04-27T10:30:00Z"
                    counts:
                      libraries: 0
                      users: 1
                      courses: 0
                      lessons: 0
                    latestScan: null
                    errorsLast24h: 0
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller is authenticated but not an administrator
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: Administrator role required.

  /api/v1/admin/scans:
    get:
      operationId: listAdminScans
      summary: List recent scans across every library
      description: |
        Ordered by `startedAt` descending (newest first). Used by the admin
        dashboard's "Recent scans" table. Cross-library — admin-only;
        non-admin actors see 403 even if they have READ grants on individual
        libraries. Capped at `limit` (default 20, max 100).
      tags: [admin]
      security:
        - bearerAuth: []
      parameters:
        - name: limit
          in: query
          required: false
          description: Maximum number of scans to return.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - name: libraryId
          in: query
          required: false
          description: >-
            When set, only return scans for the given library. Used by the
            admin library-detail page's scan-history table. Unknown library
            ids return an empty list (not 404 — the view is a filter, not
            a fetch).
          schema:
            type: string
      responses:
        "200":
          description: Recent-scans list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminScanListDto"
              examples:
                threeScans:
                  summary: Three scans from two libraries, one still running
                  value:
                    items:
                      - scanId: clxvscn0000000000000000003
                        libraryId: clxvlib0000000000000000001
                        libraryName: Computer Science
                        status: running
                        startedAt: "2026-04-27T10:25:00Z"
                        finishedAt: null
                        filesScanned: 412
                        coursesAdded: 2
                        errorsCount: 0
                      - scanId: clxvscn0000000000000000002
                        libraryId: clxvlib0000000000000000002
                        libraryName: Mathematics
                        status: succeeded
                        startedAt: "2026-04-27T09:55:00Z"
                        finishedAt: "2026-04-27T09:58:12Z"
                        filesScanned: 312
                        coursesAdded: 4
                        errorsCount: 2
                      - scanId: clxvscn0000000000000000001
                        libraryId: clxvlib0000000000000000001
                        libraryName: Computer Science
                        status: failed
                        startedAt: "2026-04-26T18:01:00Z"
                        finishedAt: "2026-04-26T18:01:05Z"
                        filesScanned: 0
                        coursesAdded: 0
                        errorsCount: 1
                empty:
                  summary: No scans have ever run
                  value:
                    items: []
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller is authenticated but not an administrator
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: Administrator role required.

  /api/v1/admin/libraries:
    get:
      operationId: listAdminLibraries
      summary: List every library with admin-flavoured counters
      description: |
        Returns every library in the system enriched with the counts and
        last-scan summary the admin libraries page renders per row. Admin-
        only — `listLibraries` is the user-facing endpoint and respects
        per-library grants; this one bypasses grants because admins see
        everything.

        Per row:
          - `coursesCount` / `lessonsCount` are aggregate counts from the
            catalog (`Course.libraryId == library.id`).
          - `lastScan` is the most recent scan on the library (any status),
            or `null` when no scan has ever run.
      tags: [admin]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Admin library list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminLibraryListDto"
              examples:
                twoLibraries:
                  value:
                    items:
                      - id: clxvlib0000000000000000001
                        name: Computer Science
                        rootPath: /srv/courses/computer-science
                        coursesCount: 14
                        lessonsCount: 312
                        lastScan:
                          status: succeeded
                          startedAt: "2026-04-27T09:55:00Z"
                          finishedAt: "2026-04-27T09:58:12Z"
                          errorsCount: 2
                      - id: clxvlib0000000000000000002
                        name: Mathematics
                        rootPath: /mnt/storage-2/math
                        coursesCount: 4
                        lessonsCount: 89
                        lastScan: null
                empty:
                  value:
                    items: []
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller is authenticated but not an administrator
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: Administrator role required.

  /api/v1/admin/users:
    get:
      operationId: listAdminUsers
      summary: List every user in the platform
      description: |
        Admin-only listing for the admin users page. Ordered by `createdAt`
        descending (newest first). The optional `search` filter matches
        email and `name` substrings (case-insensitive). `role` values are
        normalised to lowercase (`admin`, `user`, `guest`) regardless of
        what the auth backend stamps on the row in the database — the SPA
        only cares about the canonical lowercase form.
      tags: [admin]
      security:
        - bearerAuth: []
      parameters:
        - name: search
          in: query
          required: false
          description: Case-insensitive substring filter on `email` or `name`.
          schema:
            type: string
        - name: limit
          in: query
          required: false
          description: Maximum number of users to return.
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
      responses:
        "200":
          description: User list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminUserListDto"
              examples:
                twoUsers:
                  value:
                    items:
                      - id: 4248ce7d-a377-424b-b946-34cfd43dfffd
                        email: admin@example.com
                        name: Elena Lin
                        displayName: Elena
                        role: admin
                        banned: false
                        createdAt: "2026-04-25T09:00:00Z"
                        updatedAt: "2026-04-26T11:32:00Z"
                      - id: c0ffee00-0000-0000-0000-000000000001
                        email: user@example.com
                        name: Marco Bucci
                        displayName: null
                        role: user
                        banned: false
                        createdAt: "2026-04-26T10:00:00Z"
                        updatedAt: "2026-04-26T10:00:00Z"
                empty:
                  value:
                    items: []
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller is authenticated but not an administrator
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: Administrator role required.

  /api/v1/admin/users/{id}:
    get:
      operationId: getAdminUser
      summary: Fetch a single user by id
      description: |
        Admin-only single-user fetch — used by the admin permissions page
        to show whose grants are being edited. Returns the same
        `AdminUserListItem` shape as the listing endpoint.
      tags: [admin]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: User id (uuid).
          schema:
            type: string
      responses:
        "200":
          description: User row
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminUserListItem"
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller is authenticated but not an administrator
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: Administrator role required.
        "404":
          description: User not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No user with that id exists.
    patch:
      operationId: updateAdminUser
      summary: Patch role and/or banned flag on a user
      description: |
        Inline mutation used by the admin users page's role chip. At least
        one of `role` or `banned` must be present. The auth backend stores
        roles in upper-case; this endpoint translates the lowercase request
        to the persisted format and back.

        Banning is a soft delete from the user's perspective — sessions are
        invalidated and sign-in fails until the flag is cleared. Removing
        the user record outright is a separate, deferred operation.
      tags: [admin]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: User id (uuid).
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminUpdateUserRequest"
            examples:
              promoteToAdmin:
                value:
                  role: admin
              banAccount:
                value:
                  banned: true
      responses:
        "200":
          description: Updated user row
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminUserListItem"
        "400":
          description: Validation error — empty body or unknown role
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                emptyBody:
                  value:
                    type: about:blank
                    title: Bad Request
                    status: 400
                    detail: At least one of `role` or `banned` must be set.
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller is authenticated but not an administrator
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: Administrator role required.
        "404":
          description: User not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No user with that id exists.

  /api/v1/admin/has-users:
    get:
      operationId: getAdminHasUsers
      summary: Indicate whether any users exist in the platform
      description: |
        First-run probe. Returns `{ hasUsers: false }` when the database has
        zero users — the SPA uses this to force `/setup` (admin onboarding).
        Returns `{ hasUsers: true }` once at least one user exists, locking
        `/setup` and routing fresh visitors to `/sign-in`.

        No authentication is required: the very first request from a clean
        browser must succeed without credentials, and the response carries
        no sensitive information beyond a boolean.
      tags: [admin]
      security: []
      responses:
        "200":
          description: Boolean signalling whether at least one user exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HasUsersResponse"
              examples:
                empty:
                  summary: Fresh install — setup wizard required
                  value:
                    hasUsers: false
                populated:
                  summary: At least one user exists — setup is locked
                  value:
                    hasUsers: true

  /api/v1/admin/instance:
    get:
      operationId: getAdminInstance
      summary: Public instance configuration (self-registration, email verification, SSO providers)
      description: |
        Anonymous endpoint that exposes the three runtime toggles the auth
        UI needs to draw the right surface:

        - `selfRegistration` — when `false`, the sign-up entry points are
          hidden and `/sign-up` returns the user to `/sign-in`. Admin opts
          out via `AUTH_SELF_REGISTRATION=false`.
        - `emailVerificationRequired` — when `true`, the sign-up wizard
          renders step 2 (6-digit code). When `false`, sign-up jumps from
          step 1 (account) directly to step 3 (library setup). Mirrors
          Better Auth's `emailVerification` plugin toggle. Admin opts in
          via `AUTH_EMAIL_VERIFICATION=true`.
        - `ssoProviders` — array of OAuth/SSO providers configured for
          this instance. v1 ships `[]`; v2 lands Better Auth's
          `genericOAuth` plugin and the array starts to populate, lighting
          up the SsoBlock without UI changes.

        No authentication is required — the first GET from a clean browser
        decides whether the auth pages can even draw a sign-up CTA, and
        the response carries no sensitive information.
      tags: [admin]
      security: []
      responses:
        "200":
          description: Public instance configuration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstanceConfigDto"
              examples:
                defaults:
                  summary: Defaults — self-registration on, no email verification, no SSO
                  value:
                    selfRegistration: true
                    emailVerificationRequired: false
                    ssoProviders: []
                emailVerificationOn:
                  summary: Admin enabled email verification
                  value:
                    selfRegistration: true
                    emailVerificationRequired: true
                    ssoProviders: []
                lockedDown:
                  summary: Closed instance — sign-up disabled
                  value:
                    selfRegistration: false
                    emailVerificationRequired: false
                    ssoProviders: []

  /api/v1/admin/instructors:
    post:
      operationId: upsertInstructor
      summary: Create or update an instructor
      description: |
        Creates a new instructor record or updates an existing one (matched by
        slug or an externalId collision). Returns 409 when the provided slug
        already exists and belongs to a *different* instructor than would be
        matched by externalIds. Requires admin role.
      tags: [admin, Entities]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpsertInstructorRequest"
            examples:
              create:
                summary: Create Andrei Neagoie with a Udemy externalId
                value:
                  displayName: Andrei Neagoie
                  slug: andrei-neagoie
                  externalIds:
                    - source: udemy
                      externalId: "udemy:instructor:42"
                      url: "https://udemy.com/user/42"
      responses:
        "201":
          description: Instructor created or updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstructorDto"
              examples:
                created:
                  value:
                    id: clxvins0000000000000000001
                    slug: andrei-neagoie
                    displayName: Andrei Neagoie
                    externalIds:
                      - source: udemy
                        externalId: "udemy:instructor:42"
                        url: "https://udemy.com/user/42"
                    coursesTotal: 0
                    createdAt: "2026-05-23T10:00:00Z"
                    updatedAt: "2026-05-23T10:00:00Z"
        "400":
          description: Validation error — missing or malformed fields
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                badInput:
                  value:
                    type: about:blank
                    title: Bad Request
                    status: 400
                    detail: "displayName must not be empty."
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller does not have the admin role
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: Administrator role required.
        "409":
          description: Slug already taken by a different instructor
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                conflict:
                  value:
                    type: about:blank
                    title: Conflict
                    status: 409
                    detail: "Slug 'andrei-neagoie' is already taken by a different instructor."

  /api/v1/admin/maintenance/backfill-metadata:
    post:
      operationId: startBackfillMetadata
      summary: Trigger a background metadata backfill across the library
      description: |
        Enqueues a background job that walks every course in the specified
        library (or all libraries when `libraryId` is omitted), reads each
        course's `course.json`, and upserts instructor/studio/tag links and
        extended fields. Returns 202 immediately with a `jobId`; subscribe to
        the `maintenance:backfill:{jobId}` Centrifugo channel to track progress.
        Admin only.
      tags: [admin, Maintenance]
      security:
        - bearerAuth: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BackfillMetadataRequest"
            examples:
              allLibraries:
                summary: Backfill all libraries
                value: {}
              oneLibrary:
                summary: Backfill a specific library
                value:
                  libraryId: clxvlib0000000000000000001
      responses:
        "202":
          description: Backfill job accepted — progress via Centrifugo
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BackfillJobAccepted"
              examples:
                accepted:
                  value:
                    jobId: clxvjob0000000000000000001
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller does not have the admin role
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: Administrator role required.

  /api/v1/admin/scrapers:
    get:
      operationId: listScrapers
      summary: List available metadata scrapers
      description: Returns the scrapers configured on this instance with the invocation kinds each supports. Requires admin role.
      tags: [admin, Scrapers]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Configured scrapers
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScraperListDto"
              examples:
                configured:
                  value:
                    scrapers:
                      - id: youtube
                        supportedKinds: [url, name, fragment]
                        configured: true
                      - id: udemy
                        supportedKinds: [url, name, fragment]
                        configured: true
                      - id: json-ld
                        supportedKinds: [url, fragment]
                        configured: true
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller does not have the admin role
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: Administrator role required.

  /api/v1/admin/courses/{id}/scrape-preview:
    post:
      operationId: scrapeCoursePreview
      summary: Preview scraped metadata for a course
      description: |
        Runs the selected scraper against the given input and returns candidate
        metadata fragments. PREVIEW ONLY — nothing is persisted and scraped
        names are not resolved to existing entities. Requires admin role.
      tags: [admin, Scrapers]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Server-generated cuid identifying the course.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScrapePreviewRequest"
            examples:
              byUrl:
                summary: Scrape a URL with auto-detected source
                value:
                  kind: url
                  url: "https://www.youtube.com/playlist?list=PL123"
              byName:
                summary: Search YouTube by name
                value:
                  source: youtube
                  kind: name
                  query: "rust programming"
      responses:
        "200":
          description: Scrape candidates (possibly empty)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScrapePreviewResponse"
              examples:
                oneCandidate:
                  value:
                    candidates:
                      - source: youtube
                        sourceUrl: "https://www.youtube.com/playlist?list=PL123"
                        fragment:
                          title: Rust Course
                          studioName: Rustacean
                          externalIds:
                            - source: youtube
                              externalId: "youtube:playlist:PL123"
                empty:
                  value:
                    candidates: []
        "400":
          description: Malformed request body
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                badInput:
                  value:
                    type: about:blank
                    title: Bad Request
                    status: 400
                    detail: "kind must be one of: url, name, fragment."
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller does not have the admin role
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: Administrator role required.
        "404":
          description: Course not found or unknown scraper source
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No course with that id exists.
        "422":
          description: Scraper does not support the kind, or is not configured
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unsupported:
                  value:
                    type: about:blank
                    title: Unprocessable Entity
                    status: 422
                    detail: "Scraper 'json-ld' does not support the 'name' invocation kind."
        "502":
          description: Upstream fetch or parse failed
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                fetchFailed:
                  value:
                    type: about:blank
                    title: Bad Gateway
                    status: 502
                    detail: "Failed to fetch 'https://www.youtube.com/playlist?list=PL123' from the upstream source."

  /api/v1/admin/studios:
    post:
      operationId: upsertStudio
      summary: Create or update a studio
      description: |
        Creates a new studio record or updates an existing one (matched by slug
        or externalId collision). Returns 409 when the slug is taken by a
        different studio. Requires admin role.
      tags: [admin, Entities]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpsertStudioRequest"
            examples:
              create:
                summary: Create Zero To Mastery studio
                value:
                  displayName: Zero To Mastery
                  slug: zero-to-mastery
                  externalIds:
                    - source: udemy
                      externalId: "udemy:organization:ztm"
                      url: "https://udemy.com/organization/ztm"
      responses:
        "201":
          description: Studio created or updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StudioDto"
              examples:
                created:
                  value:
                    id: clxvstu0000000000000000001
                    slug: zero-to-mastery
                    displayName: Zero To Mastery
                    externalIds:
                      - source: udemy
                        externalId: "udemy:organization:ztm"
                        url: "https://udemy.com/organization/ztm"
                    coursesTotal: 0
                    createdAt: "2026-05-23T10:00:00Z"
                    updatedAt: "2026-05-23T10:00:00Z"
        "400":
          description: Validation error
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                badInput:
                  value:
                    type: about:blank
                    title: Bad Request
                    status: 400
                    detail: "displayName must not be empty."
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller does not have the admin role
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: Administrator role required.
        "409":
          description: Slug already taken by a different studio
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                conflict:
                  value:
                    type: about:blank
                    title: Conflict
                    status: 409
                    detail: "Slug 'zero-to-mastery' is already taken by a different studio."

  /api/v1/admin/tags:
    post:
      operationId: upsertTag
      summary: Create or update a tag
      description: |
        Creates a new tag or updates an existing one (matched by slug or
        externalId collision). Returns 409 when the slug is taken by a
        different tag. Requires admin role.
      tags: [admin, Entities]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpsertTagRequest"
            examples:
              create:
                summary: Create a JavaScript tag in the language category
                value:
                  displayName: JavaScript
                  slug: javascript
                  category: language
      responses:
        "201":
          description: Tag created or updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TagDto"
              examples:
                created:
                  value:
                    id: clxvtag0000000000000000001
                    slug: javascript
                    displayName: JavaScript
                    category: language
                    externalIds: []
                    coursesTotal: 0
                    createdAt: "2026-05-23T10:00:00Z"
                    updatedAt: "2026-05-23T10:00:00Z"
        "400":
          description: Validation error
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                badInput:
                  value:
                    type: about:blank
                    title: Bad Request
                    status: 400
                    detail: "displayName must not be empty."
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller does not have the admin role
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: Administrator role required.
        "409":
          description: Slug already taken by a different tag
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                conflict:
                  value:
                    type: about:blank
                    title: Conflict
                    status: 409
                    detail: "Slug 'javascript' is already taken by a different tag."

  /api/v1/bookmarks/{id}:
    patch:
      operationId: updateBookmark
      summary: Update a bookmark's position or label
      description: |
        Owner-only. At least one of `positionSeconds` / `label` must be present.
        Pass `label: null` to clear an existing label. The server returns 400 on
        empty patches (no fields provided).
      tags: [Learning]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Server-generated cuid identifying the bookmark to update.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateBookmarkRequest"
            examples:
              moveAndRelabel:
                summary: Move position and update label
                value:
                  positionSeconds: 210
                  label: Aggregate boundaries explained
      responses:
        "200":
          description: Bookmark updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BookmarkDto"
              examples:
                updated:
                  value:
                    id: clxvbkm0000000000000000001
                    lessonId: clxvles0000000000000000001
                    positionSeconds: 210
                    label: Aggregate boundaries explained
                    createdAt: "2026-04-25T14:00:00Z"
                    updatedAt: "2026-04-25T14:05:00Z"
        "400":
          description: Empty patch — no fields provided
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                emptyPatch:
                  value:
                    type: about:blank
                    title: Bad Request
                    status: 400
                    detail: At least one of positionSeconds or label must be present.
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller is not the bookmark owner
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: You do not own this bookmark.
        "404":
          description: Bookmark not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No bookmark with that id exists.

    delete:
      operationId: deleteBookmark
      summary: Delete a bookmark
      description: Owner-only. Admins may delete any bookmark for moderation.
      tags: [Learning]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Server-generated cuid identifying the bookmark to delete.
          schema:
            type: string
      responses:
        "204":
          description: Bookmark deleted — no body
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller is not the bookmark owner (and not an admin)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: You do not own this bookmark.
        "404":
          description: Bookmark not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No bookmark with that id exists.

  /api/v1/courses:
    get:
      operationId: listCourses
      summary: List courses (with filtering and sort)
      description: |
        Returns courses the requester can see. Non-admins see only courses inside
        libraries they have a READ AccessGrant for; admins see all.

        The `status` and `sort` query params back the Browse page (E14-F01-S02).
        `status` filters by per-user progress derived from the
        CourseProgressReadModel projection. `sort` is server-applied so the
        SPA never needs to re-sort the response.
      tags: [Catalog]
      security:
        - bearerAuth: []
      parameters:
        - name: libraryId
          in: query
          required: false
          description: Filter to a single library; omit for everything visible.
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: |
            Filter by per-user progress state.
              - `all` (default) — no filter.
              - `not-started` — `progress.percent == 0` and no `lessonsCompleted`.
              - `in-progress` — `0 < progress.percent < 100`.
              - `completed`   — `progress.percent == 100`.
          schema:
            type: string
            enum: [all, not-started, in-progress, completed]
            default: all
        - name: sort
          in: query
          required: false
          description: |
            Server-side sort. `recently-watched` (default) uses
            `updatedAt` as a proxy for last activity until a dedicated
            `lastViewedAt` field is added. `newest` is `createdAt` desc.
            `alphabetical` is title asc, locale-insensitive.
          schema:
            type: string
            enum: [recently-watched, newest, alphabetical]
            default: recently-watched
      responses:
        "200":
          description: Course list returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CourseListDto"
              examples:
                list:
                  value:
                    items:
                      - id: clxvcrs0000000000000000001
                        libraryId: clxvp1234567890abcdefghij
                        slug: pragmatic-clean-architecture
                        title: Pragmatic Clean Architecture
                        description: Milan Jovanović's deep-dive into Clean Architecture with .NET.
                        sections:
                          - id: clxvsec0000000000000000001
                            position: 1
                            title: Introduction
                          - id: clxvsec0000000000000000002
                            position: 2
                            title: Domain Layer
                        progress:
                          percent: 0
                          lessonsCompleted: 0
                          lessonsTotal: 24
                        createdAt: "2026-04-25T09:00:00Z"
                        updatedAt: "2026-04-25T09:00:00Z"
                      - id: clxvcrs0000000000000000002
                        libraryId: clxvp1234567890abcdefghij
                        slug: ddd-by-eric-evans
                        title: Domain-Driven Design by Eric Evans
                        sections:
                          - id: clxvsec0000000000000000003
                            position: 1
                            title: Putting the Domain Model to Work
                          - id: clxvsec0000000000000000004
                            position: 2
                            title: Building Blocks of a Model-Driven Design
                        progress:
                          percent: 0
                          lessonsCompleted: 0
                          lessonsTotal: 18
                        createdAt: "2026-04-25T09:05:00Z"
                        updatedAt: "2026-04-25T09:05:00Z"
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401

  /api/v1/courses/{id}:
    get:
      operationId: getCourse
      summary: Get a single course
      description: Returns the full CourseDto for one course by its server-generated cuid. Non-admins must hold a READ AccessGrant on the course's library.
      tags: [Catalog]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Server-generated cuid identifying the course.
          schema:
            type: string
      responses:
        "200":
          description: Course found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CourseDto"
              examples:
                found:
                  value:
                    id: clxvcrs0000000000000000001
                    libraryId: clxvp1234567890abcdefghij
                    slug: pragmatic-clean-architecture
                    title: Pragmatic Clean Architecture
                    description: Milan Jovanović's deep-dive into Clean Architecture with .NET.
                    sections:
                      - id: clxvsec0000000000000000001
                        position: 1
                        title: Introduction
                      - id: clxvsec0000000000000000002
                        position: 2
                        title: Domain Layer
                    progress:
                      percent: 0
                      lessonsCompleted: 0
                      lessonsTotal: 24
                    createdAt: "2026-04-25T09:00:00Z"
                    updatedAt: "2026-04-25T09:00:00Z"
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller does not have a READ grant for the course's library
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: You do not have access to this course.
        "404":
          description: Course not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No course with that id exists.

    patch:
      operationId: updateCourse
      summary: Update course metadata
      description: |
        Admin-only. Updates any combination of title / description / slug.
        Slug must be unique within the same library.
        At least one of `title`, `description`, or `slug` must be present
        (server-side validation rule — OpenAPI cannot express "at-least-one" natively).
      tags: [Catalog]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Server-generated cuid identifying the course to update.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateCourseRequest"
            examples:
              renameAndReslug:
                summary: Rename title and update slug
                value:
                  title: Pragmatic Clean Architecture (2nd ed.)
                  slug: pragmatic-clean-architecture-2nd-ed
      responses:
        "200":
          description: Course updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CourseDto"
              examples:
                updated:
                  value:
                    id: clxvcrs0000000000000000001
                    libraryId: clxvp1234567890abcdefghij
                    slug: pragmatic-clean-architecture-2nd-ed
                    title: Pragmatic Clean Architecture (2nd ed.)
                    description: Milan Jovanović's deep-dive into Clean Architecture with .NET.
                    sections:
                      - id: clxvsec0000000000000000001
                        position: 1
                        title: Introduction
                      - id: clxvsec0000000000000000002
                        position: 2
                        title: Domain Layer
                    progress:
                      percent: 0
                      lessonsCompleted: 0
                      lessonsTotal: 24
                    createdAt: "2026-04-25T09:00:00Z"
                    updatedAt: "2026-04-25T10:15:00Z"
        "400":
          description: Validation error — malformed fields or no fields provided
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                badInput:
                  value:
                    type: about:blank
                    title: Bad Request
                    status: 400
                    detail: At least one of title, description, or slug must be present.
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller does not have the admin role
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: Only admins may update course metadata.
        "404":
          description: Course not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No course with that id exists.
        "409":
          description: A course with the same slug already exists in this library
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                conflict:
                  value:
                    type: about:blank
                    title: Conflict
                    status: 409
                    detail: A course with slug 'pragmatic-clean-architecture-2nd-ed' already exists in this library.

  /api/v1/courses/{id}/outline:
    get:
      operationId: getCourseOutline
      summary: Full course outline — sections, lessons (lite), and aggregated materials
      description: |
        Single round-trip endpoint feeding the Course detail page. Returns
        the course summary, every section with its lesson list (lightweight:
        title, duration, hasMaterials, per-user progress), and a flat list of
        course-level materials aggregated across all lessons. The dedicated
        outline avoids N+1 page fetches and never returns full LessonDtos
        (which would inflate the payload with subtitle tracks the page
        does not render).

        Reads `Course` + `Section` + `Lesson` + `Material` + `LessonProgress`
        (filtered to the requester) + `CourseProgressReadModel` (for the
        aggregate progress percent).
      tags: [Catalog]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Server-generated cuid identifying the course.
          schema:
            type: string
      responses:
        "200":
          description: Course outline returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CourseOutlineDto"
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller does not have a READ grant for the course's library
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        "404":
          description: Course not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"

  /api/v1/courses/{id}/mark-complete:
    post:
      operationId: markCourseComplete
      summary: Mark every lesson in the course as completed for the requester
      description: |
        Bulk-marks every lesson in the course as completed for the
        requester. Idempotent — a second call is a no-op. Returns the
        refreshed `CourseOutlineDto` so the caller does not have to issue
        a separate GET.

        Implementation note: the handler upserts `LessonProgress` rows
        with `completed: true`, `completedAt: now`, and
        `positionSeconds: durationSeconds`. `CourseProgressReadModel` is
        kept in sync via the `LessonCompleted` event handler.
      tags: [Learning]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Server-generated cuid identifying the course.
          schema:
            type: string
      responses:
        "200":
          description: Every lesson marked complete; refreshed outline returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CourseOutlineDto"
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        "403":
          description: Caller does not have a READ grant for the course's library
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        "404":
          description: Course not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"

  /api/v1/courses/{id}/reset-progress:
    post:
      operationId: resetCourseProgress
      summary: Clear every progress row in the course for the requester
      description: |
        Deletes every `LessonProgress` row for (requester, course).
        Idempotent — a second call is a no-op. Returns the refreshed
        `CourseOutlineDto` so the caller does not have to issue a
        separate GET.

        `CourseProgressReadModel` is kept in sync via the
        `LessonProgressReset` event handler (or rebuilt directly when
        no events are emitted on delete).
      tags: [Learning]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Server-generated cuid identifying the course.
          schema:
            type: string
      responses:
        "200":
          description: Progress cleared; refreshed outline returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CourseOutlineDto"
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        "403":
          description: Caller does not have a READ grant for the course's library
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        "404":
          description: Course not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"

  /api/v1/catalog/instructors:
    get:
      operationId: listInstructors
      summary: List instructors with optional search
      description: |
        Returns a paginated list of instructors. The optional `search` parameter
        performs a case-insensitive substring match on `displayName`. Results are
        ordered by `displayName` ascending.
      tags: [Entities]
      security:
        - bearerAuth: []
      parameters:
        - name: offset
          in: query
          required: false
          description: Number of items to skip (zero-based).
          schema:
            type: integer
            minimum: 0
            default: 0
        - name: limit
          in: query
          required: false
          description: Maximum number of items to return.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - name: search
          in: query
          required: false
          description: Case-insensitive substring filter on `displayName`.
          schema:
            type: string
      responses:
        "200":
          description: Instructor list returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstructorListDto"
              examples:
                list:
                  value:
                    items:
                      - id: clxvins0000000000000000001
                        slug: andrei-neagoie
                        displayName: Andrei Neagoie
                        externalIds:
                          - source: udemy
                            externalId: "udemy:instructor:42"
                            url: "https://udemy.com/user/42"
                        coursesTotal: 12
                        createdAt: "2026-05-23T10:00:00Z"
                        updatedAt: "2026-05-23T10:00:00Z"
                    total: 1
                    offset: 0
                    limit: 20
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401

  /api/v1/catalog/instructors/{slug}:
    get:
      operationId: getInstructor
      summary: Get a single instructor by slug
      description: Returns the instructor details plus a list of their associated courses.
      tags: [Entities]
      security:
        - bearerAuth: []
      parameters:
        - name: slug
          in: path
          required: true
          description: URL-safe slug identifying the instructor.
          schema:
            $ref: "#/components/schemas/EntitySlug"
      responses:
        "200":
          description: Instructor found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstructorDetailDto"
              examples:
                found:
                  value:
                    instructor:
                      id: clxvins0000000000000000001
                      slug: andrei-neagoie
                      displayName: Andrei Neagoie
                      externalIds:
                        - source: udemy
                          externalId: "udemy:instructor:42"
                          url: "https://udemy.com/user/42"
                      coursesTotal: 1
                      createdAt: "2026-05-23T10:00:00Z"
                      updatedAt: "2026-05-23T10:00:00Z"
                    courses:
                      - id: clxvcrs0000000000000000001
                        libraryId: clxvp1234567890abcdefghij
                        slug: complete-web-developer-bootcamp
                        title: The Complete Web Developer in 2024
                        sections: []
                        progress:
                          percent: 0
                          lessonsCompleted: 0
                          lessonsTotal: 300
                        createdAt: "2026-05-20T08:00:00Z"
                        updatedAt: "2026-05-20T08:00:00Z"
                    coursesTotal: 1
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "404":
          description: Instructor not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No instructor with slug 'andrei-neagoie' exists.

  /api/v1/catalog/studios:
    get:
      operationId: listStudios
      summary: List studios with optional search
      description: |
        Returns a paginated list of studios. The optional `search` parameter
        performs a case-insensitive substring match on `displayName`. Results are
        ordered by `displayName` ascending.
      tags: [Entities]
      security:
        - bearerAuth: []
      parameters:
        - name: offset
          in: query
          required: false
          description: Number of items to skip (zero-based).
          schema:
            type: integer
            minimum: 0
            default: 0
        - name: limit
          in: query
          required: false
          description: Maximum number of items to return.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - name: search
          in: query
          required: false
          description: Case-insensitive substring filter on `displayName`.
          schema:
            type: string
      responses:
        "200":
          description: Studio list returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StudioListDto"
              examples:
                list:
                  value:
                    items:
                      - id: clxvstu0000000000000000001
                        slug: zero-to-mastery
                        displayName: Zero To Mastery
                        externalIds:
                          - source: udemy
                            externalId: "udemy:organization:ztm"
                            url: "https://udemy.com/organization/ztm"
                        coursesTotal: 5
                        createdAt: "2026-05-23T10:00:00Z"
                        updatedAt: "2026-05-23T10:00:00Z"
                    total: 1
                    offset: 0
                    limit: 20
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401

  /api/v1/catalog/studios/{slug}:
    get:
      operationId: getStudio
      summary: Get a single studio by slug
      description: Returns the studio details plus a list of their associated courses.
      tags: [Entities]
      security:
        - bearerAuth: []
      parameters:
        - name: slug
          in: path
          required: true
          description: URL-safe slug identifying the studio.
          schema:
            $ref: "#/components/schemas/EntitySlug"
      responses:
        "200":
          description: Studio found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StudioDetailDto"
              examples:
                found:
                  value:
                    studio:
                      id: clxvstu0000000000000000001
                      slug: zero-to-mastery
                      displayName: Zero To Mastery
                      externalIds:
                        - source: udemy
                          externalId: "udemy:organization:ztm"
                          url: "https://udemy.com/organization/ztm"
                      coursesTotal: 1
                      createdAt: "2026-05-23T10:00:00Z"
                      updatedAt: "2026-05-23T10:00:00Z"
                    courses:
                      - id: clxvcrs0000000000000000001
                        libraryId: clxvp1234567890abcdefghij
                        slug: complete-web-developer-bootcamp
                        title: The Complete Web Developer in 2024
                        sections: []
                        progress:
                          percent: 0
                          lessonsCompleted: 0
                          lessonsTotal: 300
                        createdAt: "2026-05-20T08:00:00Z"
                        updatedAt: "2026-05-20T08:00:00Z"
                    coursesTotal: 1
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "404":
          description: Studio not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No studio with slug 'zero-to-mastery' exists.

  /api/v1/catalog/tags:
    get:
      operationId: listTags
      summary: List tags with optional search and category filter
      description: |
        Returns a paginated list of tags. The optional `search` parameter
        performs a case-insensitive substring match on `displayName`. The
        optional `category` parameter filters by exact category value.
        Results are ordered by `displayName` ascending.
      tags: [Entities]
      security:
        - bearerAuth: []
      parameters:
        - name: offset
          in: query
          required: false
          description: Number of items to skip (zero-based).
          schema:
            type: integer
            minimum: 0
            default: 0
        - name: limit
          in: query
          required: false
          description: Maximum number of items to return.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - name: search
          in: query
          required: false
          description: Case-insensitive substring filter on `displayName`.
          schema:
            type: string
        - name: category
          in: query
          required: false
          description: Exact match filter on `category`. Omit to return tags from all categories.
          schema:
            type: string
      responses:
        "200":
          description: Tag list returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TagListDto"
              examples:
                list:
                  value:
                    items:
                      - id: clxvtag0000000000000000001
                        slug: javascript
                        displayName: JavaScript
                        category: language
                        externalIds: []
                        coursesTotal: 8
                        createdAt: "2026-05-23T10:00:00Z"
                        updatedAt: "2026-05-23T10:00:00Z"
                    total: 1
                    offset: 0
                    limit: 20
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401

  /api/v1/catalog/tags/{slug}:
    get:
      operationId: getTag
      summary: Get a single tag by slug
      description: Returns the tag details plus a list of associated courses.
      tags: [Entities]
      security:
        - bearerAuth: []
      parameters:
        - name: slug
          in: path
          required: true
          description: URL-safe slug identifying the tag.
          schema:
            $ref: "#/components/schemas/EntitySlug"
      responses:
        "200":
          description: Tag found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TagDetailDto"
              examples:
                found:
                  value:
                    tag:
                      id: clxvtag0000000000000000001
                      slug: javascript
                      displayName: JavaScript
                      category: language
                      externalIds: []
                      coursesTotal: 1
                      createdAt: "2026-05-23T10:00:00Z"
                      updatedAt: "2026-05-23T10:00:00Z"
                    courses:
                      - id: clxvcrs0000000000000000001
                        libraryId: clxvp1234567890abcdefghij
                        slug: complete-web-developer-bootcamp
                        title: The Complete Web Developer in 2024
                        sections: []
                        progress:
                          percent: 0
                          lessonsCompleted: 0
                          lessonsTotal: 300
                        createdAt: "2026-05-20T08:00:00Z"
                        updatedAt: "2026-05-20T08:00:00Z"
                    coursesTotal: 1
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "404":
          description: Tag not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No tag with slug 'javascript' exists.

  /api/v1/home/continue-watching:
    get:
      operationId: getContinueWatching
      summary: List courses the requester is in the middle of
      description: |
        Returns the requester's courses ordered by recency (most-recently-watched
        first), capped by `limit`. Reads from a denormalised
        `CourseProgressReadModel` projection that's updated by `LessonCompleted`
        and `LessonProgressRecorded` events. Empty array for new users.
      tags: [Catalog]
      security:
        - bearerAuth: []
      parameters:
        - name: limit
          in: query
          required: false
          description: How many items the home row needs.
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 10
      responses:
        "200":
          description: Continue-watching list returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContinueWatchingDto"
              examples:
                twoInProgress:
                  summary: Two courses in progress
                  value:
                    items:
                      - courseId: clxvcrs0000000000000000001
                        courseTitle: Pragmatic Clean Architecture
                        librarySlug: conference-recordings
                        percent: 15.79
                        lessonsCompleted: 12
                        lessonsTotal: 76
                        lastSeenAt: "2026-04-25T14:32:00Z"
                        lastSeenLessonId: clxvles0000000000000000042
                      - courseId: clxvcrs0000000000000000002
                        courseTitle: Domain-Driven Design by Eric Evans
                        percent: 33.33
                        lessonsCompleted: 6
                        lessonsTotal: 18
                        lastSeenAt: "2026-04-24T10:10:00Z"
                        lastSeenLessonId: clxvles0000000000000000007
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401

  /api/v1/home/recently-added:
    get:
      operationId: getRecentlyAdded
      summary: Courses recently added to the requester's libraries
      description: |
        Returns courses ordered by `createdAt` (most recent first), capped by
        `limit`. Sourced from the `Course` table directly — no completion
        filter is applied (a brand-new user sees their library's recent
        intake even before any progress events).
      tags: [Catalog]
      security:
        - bearerAuth: []
      parameters:
        - name: limit
          in: query
          required: false
          description: How many items the home row needs.
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 10
      responses:
        "200":
          description: Recently-added list returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecentlyAddedDto"
              examples:
                twoCourses:
                  summary: Two courses added recently
                  value:
                    items:
                      - courseId: clxvcrs0000000000000000010
                        courseTitle: Implementing Domain-Driven Design
                        librarySlug: conference-recordings
                        lessonCount: 24
                        totalDurationSeconds: 14400
                        createdAt: "2026-04-26T08:14:00Z"
                      - courseId: clxvcrs0000000000000000011
                        courseTitle: Streaming Systems Concepts
                        lessonCount: 12
                        totalDurationSeconds: 6300
                        createdAt: "2026-04-25T17:02:00Z"
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401

  /api/v1/home/recently-completed:
    get:
      operationId: getRecentlyCompleted
      summary: Courses the requester finished most recently
      description: |
        Returns courses where the requester completed the last lesson
        (`lessonsCompleted == lessonsTotal`), ordered by `lastSeenAt DESC`
        (which equals completion time for finished courses). Reads from the
        `CourseProgressReadModel` projection.
      tags: [Catalog]
      security:
        - bearerAuth: []
      parameters:
        - name: limit
          in: query
          required: false
          description: How many items the home row needs.
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 10
      responses:
        "200":
          description: Recently-completed list returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecentlyCompletedDto"
              examples:
                oneCompleted:
                  summary: One completed course
                  value:
                    items:
                      - courseId: clxvcrs0000000000000000020
                        courseTitle: Distributed Systems Foundations
                        librarySlug: conference-recordings
                        lessonsTotal: 18
                        completedAt: "2026-04-23T20:45:00Z"
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401

  /api/v1/home/your-week:
    get:
      operationId: getYourWeek
      summary: Roll-up of the requester's last seven days
      description: |
        Total minutes watched and lessons completed by the requester over
        the trailing seven days. `range.from` is `now - 7d`, `range.to` is
        `now`, both ISO-8601 with offset. Both counters are zero for new
        users. Sourced from `LessonProgress` (sum of completion-time
        contributions) and `CourseProgressReadModel.lessonsCompleted`.
      tags: [Catalog]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Weekly roll-up returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/YourWeekDto"
              examples:
                activeWeek:
                  summary: Active week
                  value:
                    minutesWatched: 312
                    lessonsCompleted: 8
                    range:
                      from: "2026-04-20T22:00:00Z"
                      to: "2026-04-27T22:00:00Z"
                emptyWeek:
                  summary: New user with no progress yet
                  value:
                    minutesWatched: 0
                    lessonsCompleted: 0
                    range:
                      from: "2026-04-20T22:00:00Z"
                      to: "2026-04-27T22:00:00Z"
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401

  /api/v1/lessons/{id}:
    get:
      operationId: getLesson
      summary: Get a lesson with its materials and subtitles
      description: |
        Returns lesson metadata, sidecar materials (PDF / Markdown / text / image),
        and available subtitle tracks. Raw filesystem paths are intentionally absent
        from the response (NFR-S-01); the player obtains a signed stream token for
        the lesson video and the material/subtitle blobs separately.
      tags: [Catalog]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Server-generated cuid identifying the lesson.
          schema:
            type: string
      responses:
        "200":
          description: Lesson found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LessonDto"
              examples:
                found:
                  value:
                    id: clxvles0000000000000000001
                    courseId: clxvcrs0000000000000000001
                    sectionId: clxvsec0000000000000000002
                    position: 3
                    title: Aggregates
                    durationSeconds: 2847
                    materials:
                      - id: clxvmat0000000000000000001
                        kind: doc
                        label: 03 - Aggregates slides
                        sizeBytes: 204800
                    subtitles:
                      - id: clxvsub0000000000000000001
                        language: en
                        label: English
                    progress:
                      percent: 0
                      completed: false
                      lastSeenAtSeconds: 0
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Requester has no READ grant covering the parent library or course
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: You do not have access to this lesson.
        "404":
          description: Lesson not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No lesson with that id exists.

  /api/v1/lessons/{id}/stream-url:
    get:
      operationId: issueStreamUrl
      summary: Mint a short-lived signed URL for a lesson video
      description: |
        Returns a URL pointing at `/api/v1/stream/lessons/{id}` with a signed
        query-param token bound to `(userId, lessonId, expiresAt)`. The token
        is HMAC-signed; the streaming endpoint verifies it without a database
        lookup. Default TTL is 15 minutes (configurable server-side). Clients
        must request a fresh URL when the previous one expires — refresh policy
        is up to the player.
      tags: [Streaming]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Server-generated cuid identifying the lesson.
          schema:
            type: string
      responses:
        "200":
          description: Signed stream URL issued
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StreamUrlDto"
              examples:
                issued:
                  value:
                    url: "/api/v1/stream/lessons/clxvles0000000000000000001?token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VySWQiOiJjbHh2dXNyMDAwMDAwMDAwMDAwMDAwMDAxIiwibGVzc29uSWQiOiJjbHh2bGVzMDAwMDAwMDAwMDAwMDAwMDAxIiwiZXhwIjoxNzQ1NTc0NDAwfQ.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c"
                    token: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VySWQiOiJjbHh2dXNyMDAwMDAwMDAwMDAwMDAwMDAxIiwibGVzc29uSWQiOiJjbHh2bGVzMDAwMDAwMDAwMDAwMDAwMDAxIiwiZXhwIjoxNzQ1NTc0NDAwfQ.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c"
                    expiresAt: "2026-04-25T10:15:00Z"
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Requester has no READ grant on the parent library or course
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: You do not have access to this lesson.
        "404":
          description: Lesson not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No lesson with that id exists.

  /api/v1/lessons/{lessonId}/materials/{materialId}/download-url:
    get:
      operationId: issueMaterialDownloadUrl
      summary: Mint a short-lived signed URL to download a lesson material
      description: |
        Returns a URL pointing at `/api/v1/stream/materials/{materialId}` with a
        signed query-param token bound to `(userId, materialId, expiresAt)`.
        The token is HMAC-signed with a different scope than the video stream
        token so a video token cannot be re-used to fetch a material (and vice
        versa). The streaming endpoint verifies the token without a database
        lookup. Default TTL is 5 minutes — short because clicking a download
        link should resolve immediately.

        Authorization mirrors `issueStreamUrl`: the requester must have a READ
        grant on the parent library or course, or be an admin.
      tags: [Streaming]
      security:
        - bearerAuth: []
      parameters:
        - name: lessonId
          in: path
          required: true
          description: Server-generated cuid identifying the parent lesson.
          schema:
            type: string
        - name: materialId
          in: path
          required: true
          description: Server-generated cuid identifying the material.
          schema:
            type: string
      responses:
        "200":
          description: Signed download URL issued
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MaterialDownloadUrlDto"
              examples:
                issued:
                  value:
                    url: "/api/v1/stream/materials/clxvmat0000000000000000001?token=eyJhbGciOiJIUzI1NiIsInR5cCI6IlNUSyIsInYiOjF9.eyJzdWIiOiJjbHh2dXNyMDAwMDAwMDAwMDAwMDAwMDAxIiwibWlkIjoiY2x4dm1hdDAwMDAwMDAwMDAwMDAwMDAwMSIsInNjcCI6Im1hdGVyaWFsIiwiZXhwIjoxNzQ1NTc0NDAwLCJpYXQiOjE3NDU1NzQxMDB9.k9sQfxCq8wL2pT4N3LKzr8B5jM0hVzG4Yf9X2N1tQp8"
                    token: "eyJhbGciOiJIUzI1NiIsInR5cCI6IlNUSyIsInYiOjF9.eyJzdWIiOiJjbHh2dXNyMDAwMDAwMDAwMDAwMDAwMDAxIiwibWlkIjoiY2x4dm1hdDAwMDAwMDAwMDAwMDAwMDAwMSIsInNjcCI6Im1hdGVyaWFsIiwiZXhwIjoxNzQ1NTc0NDAwLCJpYXQiOjE3NDU1NzQxMDB9.k9sQfxCq8wL2pT4N3LKzr8B5jM0hVzG4Yf9X2N1tQp8"
                    expiresAt: "2026-04-25T10:05:00Z"
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Requester has no READ grant on the parent library or course
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: You do not have access to this material.
        "404":
          description: Material or parent lesson not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No material with that id exists on this lesson.

  /api/v1/lessons/{lessonId}/bookmarks:
    get:
      operationId: listLessonBookmarks
      summary: List the requester's bookmarks for a lesson
      description: >-
        Returns all bookmarks the authenticated user has created for the given
        lesson, sorted ascending by `positionSeconds`. An empty `items` array is
        returned when no bookmarks exist yet.
      tags: [Learning]
      security:
        - bearerAuth: []
      parameters:
        - name: lessonId
          in: path
          required: true
          description: Server-generated cuid identifying the lesson.
          schema:
            type: string
      responses:
        "200":
          description: Bookmark list returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BookmarkListDto"
              examples:
                twoBookmarks:
                  summary: Two bookmarks sorted by positionSeconds
                  value:
                    items:
                      - id: clxvbkm0000000000000000001
                        lessonId: clxvles0000000000000000001
                        positionSeconds: 187
                        label: Aggregates intro
                        createdAt: "2026-04-25T14:00:00Z"
                        updatedAt: "2026-04-25T14:00:00Z"
                      - id: clxvbkm0000000000000000002
                        lessonId: clxvles0000000000000000001
                        positionSeconds: 742
                        label: Invariants section
                        createdAt: "2026-04-25T14:12:00Z"
                        updatedAt: "2026-04-25T14:12:00Z"
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: No READ grant on the parent library or course
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: You do not have access to this lesson.
        "404":
          description: Lesson not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No lesson with that id exists.

    post:
      operationId: createBookmark
      summary: Create a bookmark on a lesson
      description: |
        Bookmarks are personal — even your own admin role does not surface them
        in listings for other users. The body carries `positionSeconds` and an
        optional `label`.
      tags: [Learning]
      security:
        - bearerAuth: []
      parameters:
        - name: lessonId
          in: path
          required: true
          description: Server-generated cuid identifying the lesson.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateBookmarkRequest"
            examples:
              withLabel:
                summary: Bookmark with a label
                value:
                  positionSeconds: 187
                  label: Aggregates intro
      responses:
        "201":
          description: Bookmark created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BookmarkDto"
              examples:
                created:
                  value:
                    id: clxvbkm0000000000000000001
                    lessonId: clxvles0000000000000000001
                    positionSeconds: 187
                    label: Aggregates intro
                    createdAt: "2026-04-25T14:00:00Z"
                    updatedAt: "2026-04-25T14:00:00Z"
        "400":
          description: Validation error — missing or malformed fields
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                badInput:
                  value:
                    type: about:blank
                    title: Bad Request
                    status: 400
                    detail: "positionSeconds must be >= 0."
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: No READ grant on the parent library or course
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: You do not have access to this lesson.
        "404":
          description: Lesson not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No lesson with that id exists.

  /api/v1/libraries:
    post:
      operationId: registerLibrary
      summary: Register a new library (or share an existing path)
      description: |
        Persists a library pointing at an absolute filesystem path.

        **Idempotent on `rootPath`.** When a library with the same path
        already exists, the call returns the existing library and grants
        the calling user READ access to it instead of creating a duplicate
        row. The response body matches what `GET /libraries/{id}` would
        return for that library — the original `name`/`createdAt` are
        preserved (the new `name` you submitted is ignored).

        For brand-new libraries the controller chains an initial
        `runLibraryScan` so courses become visible shortly after the
        response. No initial scan is fired when the path already existed
        (the existing library is presumed already scanned).
      tags: [Catalog]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterLibraryRequest"
            examples:
              conference:
                value:
                  name: Conference Recordings
                  rootPath: /srv/courses/conference
      responses:
        "201":
          description: Library registered successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LibraryDto"
              examples:
                created:
                  value:
                    id: clxvp1234567890abcdefghij
                    name: Conference Recordings
                    rootPath: /srv/courses/conference
                    createdAt: "2026-04-25T09:00:00Z"
                    updatedAt: "2026-04-25T09:00:00Z"
        "400":
          description: Validation error — missing or malformed fields
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                badInput:
                  value:
                    type: about:blank
                    title: Bad Request
                    status: 400
                    detail: "rootPath must be an absolute POSIX or Windows path."
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller does not have the Owner-Admin role
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: Only Owner-Admins may register libraries.
    get:
      operationId: listLibraries
      summary: List all registered libraries
      description: |
        Returns all libraries the requester has READ access to. Admins see
        everything.
      tags: [Catalog]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Library list returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LibraryListDto"
              examples:
                list:
                  value:
                    items:
                      - id: clxvp1234567890abcdefghij
                        name: Conference Recordings
                        rootPath: /srv/courses/conference
                        createdAt: "2026-04-25T09:00:00Z"
                        updatedAt: "2026-04-25T09:00:00Z"
                      - id: clxvp0987654321zyxwvutsrq
                        name: Tutorial Screencasts
                        rootPath: /srv/courses/tutorials
                        createdAt: "2026-04-20T14:30:00Z"
                        updatedAt: "2026-04-22T11:00:00Z"
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401

  /api/v1/libraries/{id}:
    get:
      operationId: getLibrary
      summary: Get a library by id
      description: Returns a single library by its server-generated identifier.
      tags: [Catalog]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Server-generated cuid identifying the library.
          schema:
            type: string
      responses:
        "200":
          description: Library found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LibraryDto"
              examples:
                found:
                  value:
                    id: clxvp1234567890abcdefghij
                    name: Conference Recordings
                    rootPath: /srv/courses/conference
                    createdAt: "2026-04-25T09:00:00Z"
                    updatedAt: "2026-04-25T09:00:00Z"
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller does not have access to this library
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: You do not have access to this library.
        "404":
          description: Library not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No library with that id exists.
    patch:
      operationId: updateLibrary
      summary: Rename a library
      description: |
        Admin-only mutation. Currently only the `name` field is mutable —
        changing `rootPath` would invalidate every scan and break stream
        URLs minted before the change, so it is intentionally not exposed
        here. Re-create the library if the disk path needs to change.
      tags: [Catalog]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Server-generated cuid identifying the library.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateLibraryRequest"
            examples:
              rename:
                value:
                  name: Conference Recordings (2026)
      responses:
        "200":
          description: Updated library
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LibraryDto"
        "400":
          description: Validation error — empty body or invalid name
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                emptyBody:
                  value:
                    type: about:blank
                    title: Bad Request
                    status: 400
                    detail: At least one of `name` must be set.
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller is authenticated but not an administrator
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: Administrator role required.
        "404":
          description: Library not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No library with that id exists.
    delete:
      operationId: removeLibrary
      summary: Hard-delete a library and every dependent row
      description: |
        Admin-only destructive operation. Cascades through scans, courses
        (with sections/lessons/materials/subtitles), per-user progress,
        bookmarks, notes, and access grants. Files on disk are NOT touched
        — the library only exists in the DB; deletion just unlinks the
        folder from CourseShelf.

        The cascade lives in a single Prisma `$transaction` so partial
        failures roll back. Idempotent: deleting an already-deleted id
        returns 404.
      tags: [Catalog]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Server-generated cuid identifying the library.
          schema:
            type: string
      responses:
        "204":
          description: Library deleted
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller is authenticated but not an administrator
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: Administrator role required.
        "404":
          description: Library not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No library with that id exists.

  /api/v1/libraries/{id}/scans:
    post:
      operationId: runLibraryScan
      summary: Trigger a scan of a library
      description: |
        Walks the library tree, recognises Course / Section / Lesson layout,
        and records discoveries on a Scan aggregate. Returns 202 immediately
        with `status: running`; clients poll
        `GET /libraries/{id}/scans/latest`. A second scan with no filesystem
        changes is observably a no-op (`filesAdded` and `filesUpdated` are
        zero).
      tags: [Catalog]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Server-generated cuid identifying the library to scan.
          schema:
            type: string
      responses:
        "202":
          description: Scan accepted and running
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScanDto"
              examples:
                accepted:
                  value:
                    id: clxvscn0000000000000000001
                    libraryId: clxvp1234567890abcdefghij
                    status: running
                    startedAt: "2026-04-25T10:30:00Z"
                    filesScanned: 0
                    filesAdded: 0
                    filesUpdated: 0
                    coursesDiscovered: 0
                    errors: []
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller does not have the admin role
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: Only admins may trigger library scans.
        "404":
          description: Library not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No library with that id exists.
        "409":
          description: A scan is already running for this library
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                conflict:
                  value:
                    type: about:blank
                    title: Conflict
                    status: 409
                    detail: A scan is already running for library clxvp1234567890abcdefghij.

  /api/v1/libraries/{id}/scans/latest:
    get:
      operationId: getLatestLibraryScan
      summary: Get the most recent scan for a library
      description: >-
        Returns the latest scan record regardless of status
        (running, succeeded, failed, cancelled).
      tags: [Catalog]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Server-generated cuid identifying the library.
          schema:
            type: string
      responses:
        "200":
          description: Latest scan returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScanDto"
              examples:
                succeeded:
                  value:
                    id: clxvscn0000000000000000001
                    libraryId: clxvp1234567890abcdefghij
                    status: succeeded
                    startedAt: "2026-04-25T10:30:00Z"
                    finishedAt: "2026-04-25T10:31:42Z"
                    filesScanned: 314
                    filesAdded: 12
                    filesUpdated: 3
                    coursesDiscovered: 7
                    errors:
                      - path: "02 - Patterns/05 - Repositories.mp4"
                        message: "File is not readable — permission denied."
                        code: unreadable-file
                      - path: "04 - Bounded Contexts/course.json"
                        message: "JSON schema validation failed: missing required field 'title'."
                        code: course-json-invalid
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Caller does not have the admin role
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: Only admins may view scan records.
        "404":
          description: Library not found or no scan has been run yet
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No scan record found for this library.

  /api/v1/notes:
    put:
      operationId: upsertNote
      summary: "Upsert the requester's note for a lesson"
      description: |
        Exactly one note exists per `(userId, lessonId)`. PUT semantics: replaces
        the existing note's body if any, otherwise creates a new one. Markdown is
        stored verbatim — the server does not render or sanitise.
      tags: [Learning]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpsertNoteRequest"
            examples:
              upsert:
                summary: Upsert a Markdown note for a lesson
                value:
                  lessonId: clxvles0000000000000000001
                  body: "## Aggregates\n\nKey insight: aggregates enforce invariants across their boundary."
      responses:
        "200":
          description: Note upserted — current state returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NoteDto"
              examples:
                upserted:
                  value:
                    id: clxvnot0000000000000000001
                    lessonId: clxvles0000000000000000001
                    body: "## Aggregates\n\nKey insight: aggregates enforce invariants across their boundary."
                    createdAt: "2026-04-25T15:00:00Z"
                    updatedAt: "2026-04-25T15:05:00Z"
        "400":
          description: Validation error — missing fields or post-trim body is empty
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                badInput:
                  value:
                    type: about:blank
                    title: Bad Request
                    status: 400
                    detail: "body must not be empty after trimming whitespace."
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Requester has no READ grant on the parent library or course
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: You do not have access to this lesson.
        "404":
          description: Lesson not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No lesson with that id exists.

  /api/v1/notes/{lessonId}:
    get:
      operationId: getNote
      summary: "Get the requester's note for a lesson"
      description: |
        Returns the authenticated user's note for the given lesson. 403 is
        returned both when the requester has no READ grant covering the lesson
        and when the lesson does not exist — preventing existence leakage.
        404 is returned only when the lesson exists but no note has been written yet.
      tags: [Learning]
      security:
        - bearerAuth: []
      parameters:
        - name: lessonId
          in: path
          required: true
          description: Server-generated cuid identifying the lesson.
          schema:
            type: string
      responses:
        "200":
          description: Note returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NoteDto"
              examples:
                found:
                  value:
                    id: clxvnot0000000000000000001
                    lessonId: clxvles0000000000000000001
                    body: "## Aggregates\n\nKey insight: aggregates enforce invariants across their boundary."
                    createdAt: "2026-04-25T15:00:00Z"
                    updatedAt: "2026-04-25T15:05:00Z"
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: >-
            No READ grant on the parent library or course — also covers
            "lesson missing + no grant" to avoid leaking existence.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: You do not have access to this lesson.
        "404":
          description: Lesson exists but the requester has not yet created a note
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                noNote:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No note found for this lesson.

    delete:
      operationId: deleteNote
      summary: "Clear the requester's note for a lesson"
      description: |
        Idempotent: returns 204 even when no note exists. Owner-only — there is
        no concept of admin moderation for notes (notes are personal).
      tags: [Learning]
      security:
        - bearerAuth: []
      parameters:
        - name: lessonId
          in: path
          required: true
          description: Server-generated cuid identifying the lesson.
          schema:
            type: string
      responses:
        "204":
          description: Note deleted — no body
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Requester has no READ grant on the parent library or course
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: You do not have access to this lesson.
        "404":
          description: Lesson not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No lesson with that id exists.

  /api/v1/health:
    get:
      operationId: getHealth
      summary: Service health probe
      description: |
        Reports combined health status of the service and its runtime
        dependencies (database, cache, realtime bus).
      tags: [system]
      responses:
        "200":
          description: Service and all dependencies are operational
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
              examples:
                ok:
                  value:
                    status: ok
                    version: 0.1.0
                    uptimeSeconds: 42
                    dependencies:
                      db: ok
                      redis: ok
                      centrifugo: ok
        "503":
          description: One or more dependencies are degraded or down
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
              examples:
                degraded:
                  value:
                    status: degraded
                    version: 0.1.0
                    uptimeSeconds: 42
                    dependencies:
                      db: ok
                      redis: degraded
                      centrifugo: ok

  /api/v1/ping:
    get:
      operationId: ping
      summary: Verify the bearer token resolves a session
      description: |
        Smoke-test endpoint for the authentication chain. Returns the
        requesting user's identity if the bearer token is valid; returns
        `401 Unauthorized` otherwise. Useful for clients that want to
        confirm their stored token is still good without making a real
        domain call.
      tags: [Ops]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Authenticated identity echoed back
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PingResponse"
              examples:
                authenticated:
                  value:
                    id: 0193a1b2-c3d4-7e5f-8a90-1234567890ab
                    role: USER
                    displayName: Sam Reader
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401

  /api/v1/progress:
    post:
      operationId: recordLessonProgress
      summary: Record (upsert) the requester's progress on a lesson
      description: |
        Last-write-wins on `clientUpdatedAt`: out-of-order writes (older
        timestamp than the current state) are silently accepted with the prior
        state echoed back. The first write that crosses 90 % completion sets
        `completed: true` and stamps `completedAt`; subsequent writes do not
        re-emit completion. Always returns the post-merge state — clients can
        use it to detect whether their write was the one that bumped the counter.
      tags: [Learning]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordProgressRequest"
            examples:
              watchingLesson:
                summary: User watched 18 min of a 30-min lesson
                value:
                  lessonId: clxvles0000000000000000001
                  positionSeconds: 1080
                  durationSeconds: 1800
                  clientUpdatedAt: "2026-04-25T14:32:00Z"
      responses:
        "200":
          description: Post-merge progress state returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LessonProgressDto"
              examples:
                merged:
                  value:
                    lessonId: clxvles0000000000000000001
                    positionSeconds: 1080
                    durationSeconds: 1800
                    percent: 60
                    completed: false
                    lastSeenAt: "2026-04-25T14:32:00Z"
        "400":
          description: Validation error — missing or malformed fields
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                badInput:
                  value:
                    type: about:blank
                    title: Bad Request
                    status: 400
                    detail: "positionSeconds must be >= 0."
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: Requester has no READ grant covering the parent library or course
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: You do not have access to this lesson.
        "404":
          description: Lesson not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                notFound:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No lesson with that id exists.

  /api/v1/progress/{lessonId}:
    get:
      operationId: getLessonProgress
      summary: Get the requester's progress on a lesson
      description: |
        Returns the current progress record for the requesting user on the
        given lesson. 403 is returned both when the requester has no READ grant
        covering the lesson **and** when the lesson does not exist — preventing
        existence leakage. 404 is returned only when the lesson exists but the
        requester has not yet recorded any progress.
      tags: [Learning]
      security:
        - bearerAuth: []
      parameters:
        - name: lessonId
          in: path
          required: true
          description: Server-generated cuid identifying the lesson.
          schema:
            type: string
      responses:
        "200":
          description: Progress record returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LessonProgressDto"
              examples:
                inProgress:
                  value:
                    lessonId: clxvles0000000000000000001
                    positionSeconds: 1080
                    durationSeconds: 1800
                    percent: 60
                    completed: false
                    lastSeenAt: "2026-04-25T14:32:00Z"
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401
        "403":
          description: No READ grant — also covers "lesson missing + no grant" to avoid leaking existence
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                forbidden:
                  value:
                    type: about:blank
                    title: Forbidden
                    status: 403
                    detail: You do not have access to this lesson.
        "404":
          description: Lesson exists but the requester has not yet recorded any progress
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                noProgress:
                  value:
                    type: about:blank
                    title: Not Found
                    status: 404
                    detail: No progress record found for this lesson.

  /api/v1/progress/batch:
    post:
      operationId: recordLessonProgressBatch
      summary: Record up to 200 progress updates in a single call
      description: |
        Sync endpoint for offline-first clients. Per-item failures do **not**
        abort the batch — every item gets its own status in the same order
        as the input. Conflict detection: if the server's `lastSeenAt` for a
        lesson is newer than the client's `clientUpdatedAt`, the item's
        status is `stale` and `state` carries the server's view so the
        client can overwrite local cache. Otherwise the status is `accepted`.
        `forbidden` covers both "no READ grant" and "lesson does not exist"
        (no-oracle rule, consistent with `POST /progress`).
      tags: [Learning]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BatchProgressRequest"
            examples:
              mixedBatch:
                summary: Three writes — one fresh, one already-newer-on-server, one no-grant
                value:
                  items:
                    - lessonId: clxvles0000000000000000001
                      positionSeconds: 600
                      durationSeconds: 1800
                      clientUpdatedAt: "2026-04-25T14:00:00Z"
                    - lessonId: clxvles0000000000000000002
                      positionSeconds: 30
                      durationSeconds: 1200
                      clientUpdatedAt: "2026-04-20T08:00:00Z"
                    - lessonId: clxvles0000000000000000099
                      positionSeconds: 0
                      durationSeconds: 600
                      clientUpdatedAt: "2026-04-25T14:05:00Z"
      responses:
        "200":
          description: Per-item results, same order and length as input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BatchProgressResponse"
              examples:
                mixedBatch:
                  value:
                    results:
                      - status: accepted
                        state:
                          lessonId: clxvles0000000000000000001
                          positionSeconds: 600
                          durationSeconds: 1800
                          percent: 33
                          completed: false
                          lastSeenAt: "2026-04-25T14:00:00Z"
                      - status: stale
                        state:
                          lessonId: clxvles0000000000000000002
                          positionSeconds: 900
                          durationSeconds: 1200
                          percent: 75
                          completed: false
                          lastSeenAt: "2026-04-22T10:00:00Z"
                      - status: forbidden
                        lessonId: clxvles0000000000000000099
        "400":
          description: Request body is empty (`items.length === 0`) or too large (>200)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                tooMany:
                  value:
                    type: about:blank
                    title: Bad Request
                    status: 400
                    detail: items must contain between 1 and 200 entries.
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401

  /api/v1/me:
    patch:
      operationId: updateMe
      summary: Patch the calling user's own profile
      description: |
        Currently only `displayName` is mutable through this endpoint. Email
        change and avatar upload are handled separately (and are not yet
        wired). At least one field must be present in the body.
      tags: [Account]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateMeRequest"
            examples:
              renameDisplay:
                value:
                  displayName: Elena
      responses:
        "200":
          description: Updated profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeDto"
        "400":
          description: Validation error — empty body or invalid field
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                emptyBody:
                  value:
                    type: about:blank
                    title: Bad Request
                    status: 400
                    detail: At least one of `displayName` must be set.
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401

  /api/v1/me/sign-out-others:
    post:
      operationId: signOutOtherSessions
      summary: Sign out from every device EXCEPT the current session
      description: |
        Revokes every session row for the calling user other than the one
        attached to the request. Useful from the settings page's "Sign out
        from all devices" affordance — the user stays signed in on the
        device they just clicked from. Returns 204.
      tags: [Account]
      security:
        - bearerAuth: []
      responses:
        "204":
          description: Other sessions revoked
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401

  /api/v1/search:
    get:
      operationId: searchCatalogue
      summary: Search the catalogue (courses + lessons)
      description: |
        Case-insensitive substring search across course titles, section
        titles (matched into their courses), and lesson titles. Returns
        two result lists: courses and lessons. Each list is capped at
        `limit` (default 20, max 100). Results are sorted by best match
        (exact-prefix > word-prefix > substring) within each list.

        Authorisation mirrors the listing endpoints — non-admin actors
        only see courses / lessons they have a READ grant on (via the
        course's library); admins see everything.

        Empty `q` returns empty lists (no expensive full-table scan).
        Trimmed length must be ≥ 2 to avoid pathologically broad
        substring matches; shorter queries return empty lists too.
      tags: [Catalog]
      security:
        - bearerAuth: []
      parameters:
        - name: q
          in: query
          required: true
          description: Substring to match. Trimmed; case-insensitive.
          schema:
            type: string
        - name: limit
          in: query
          required: false
          description: Maximum number of results PER kind (courses + lessons).
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
      responses:
        "200":
          description: Search hits
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SearchResultDto"
              examples:
                hits:
                  value:
                    query: archi
                    courses:
                      - id: clxvcrs0000000000000000001
                        libraryId: clxvlib0000000000000000001
                        title: Pragmatic Clean Architecture
                        slug: pragmatic-clean-architecture
                        lessonsTotal: 18
                    lessons:
                      - id: clxvles0000000000000000001
                        courseId: clxvcrs0000000000000000001
                        courseTitle: Pragmatic Clean Architecture
                        sectionTitle: Domain Layer
                        title: Architectural drivers
                        position: 3
                empty:
                  value:
                    query: zzz
                    courses: []
                    lessons: []
        "401":
          description: Missing or invalid bearer token
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
              examples:
                unauthorized:
                  value:
                    type: about:blank
                    title: Unauthorized
                    status: 401

  /api/v1/realtime/token:
    post:
      operationId: issueRealtimeToken
      summary: Issue a short-lived Centrifugo connection token
      description: |
        Mints a short-lived HMAC-signed JWT that the client can use to connect
        to Centrifugo. Requires an active Better Auth session (cookie on web,
        bearer token on mobile).
      tags: [realtime]
      security:
        - cookieAuth: []
        - bearerAuth: []
      responses:
        "200":
          description: Token issued
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RealtimeToken"
              examples:
                ok:
                  value:
                    token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
                    expiresAt: "2026-04-18T12:05:00Z"
        "401":
          description: No active session
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"

components:
  securitySchemes:
    cookieAuth:
      type: apiKey
      in: cookie
      name: better-auth.session_token
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: Better Auth bearer token
  schemas:
    BackfillJobAccepted:
      type: object
      required: [jobId]
      description: Confirmation that a backfill job has been accepted and is running in the background.
      properties:
        jobId:
          type: string
          description: Unique cuid identifying the background job. Subscribe to `maintenance:backfill:{jobId}` on Centrifugo for progress events.
      example:
        jobId: clxvjob0000000000000000001

    BackfillMetadataRequest:
      type: object
      description: |
        Optional scope for a metadata backfill job. When both fields are
        omitted the backfill runs across all libraries.
      properties:
        libraryId:
          type: string
          description: cuid of the library to restrict the backfill to. Omit to backfill all libraries.
      example:
        libraryId: clxvlib0000000000000000001

    CourseLevel:
      type: string
      enum: [beginner, intermediate, advanced, expert, all_levels]
      description: >-
        Difficulty level of a course. `all_levels` is used when the course
        is suitable for all experience levels regardless of background.
      example: intermediate

    EntitySlug:
      type: string
      pattern: "^[a-z0-9](?:[a-z0-9-]{0,98}[a-z0-9])?$"
      minLength: 1
      maxLength: 100
      description: >-
        URL-safe slug. 1–100 chars, lowercase ASCII letters, digits, and hyphens;
        cannot start or end with a hyphen. Shared by Instructor, Studio, and Tag aggregates.
      example: andrei-neagoie

    ExternalIdRef:
      type: object
      required: [source, externalId]
      description: A reference to an entity in an external system (e.g. Udemy, YouTube).
      properties:
        source:
          type: string
          minLength: 1
          description: >-
            Namespace identifying the external system (e.g. `udemy`,
            `youtube`). Scrapers are responsible for namespacing their ids
            (e.g. `youtube:playlist:PLxxx` vs `youtube:channel:UCyyy`).
        externalId:
          type: string
          minLength: 1
          description: Identifier within the source system.
        url:
          type: string
          format: uri
          description: Optional canonical URL of the entity on the source platform.
      examples:
        - source: udemy
          externalId: "udemy:instructor:42"
          url: "https://udemy.com/user/42"
        - source: youtube
          externalId: "youtube:channel:UCnUYZLuoy1rq1aVMwx4aTzw"

    ScrapeCandidateDto:
      type: object
      required: [source, fragment]
      description: A single candidate produced by a scraper run.
      properties:
        source:
          type: string
          description: Id of the scraper that produced this candidate (e.g. `youtube`, `json-ld`).
          example: youtube
        sourceUrl:
          type: string
          format: uri
          description: URL of the upstream resource that was fetched, if applicable.
          example: "https://www.youtube.com/playlist?list=PL123"
        confidence:
          type: number
          format: float
          minimum: 0
          maximum: 1
          description: Optional confidence score in the range [0, 1].
          example: 0.9
        fragment:
          $ref: "#/components/schemas/ScrapedCourseFragmentDto"
      example:
        source: youtube
        sourceUrl: "https://www.youtube.com/playlist?list=PL123"
        fragment:
          title: Rust Course
          studioName: Rustacean
          externalIds:
            - source: youtube
              externalId: "youtube:playlist:PL123"

    ScrapedCourseFragmentDto:
      type: object
      description: >-
        Raw scraped metadata. Names are not resolved to entities (Stage 4 does that).
        All fields are optional because any given scraper may return a partial set.
      properties:
        title:
          type: string
          description: Raw course title as returned by the scraper.
          example: Rust Course
        description:
          type: string
          description: Raw course description.
        instructorNames:
          type: array
          items:
            type: string
          description: Instructor names as scraped (not resolved to Instructor entities).
          example: ["Jane Doe"]
        studioName:
          type: string
          description: Studio or channel name as scraped (not resolved to a Studio entity).
          example: Rustacean
        tags:
          type: array
          items:
            type: string
          description: Raw tags or topic labels.
        level:
          $ref: "#/components/schemas/CourseLevel"
        language:
          type: string
          description: BCP-47 language code (e.g. `en`, `de`).
          example: en
        releaseDate:
          type: string
          format: date
          description: Original release date of the course in ISO 8601 date format.
          example: "2024-03-15"
        posterUrl:
          type: string
          format: uri
          description: URL of the course thumbnail / poster image.
          example: "https://i.ytimg.com/vi/example/hqdefault.jpg"
        externalIds:
          type: array
          items:
            $ref: "#/components/schemas/ExternalIdRef"
          description: External system references detected during scraping.
        ratingAverage:
          type: number
          format: float
          minimum: 0
          maximum: 5
          description: Aggregate rating value in the range [0, 5].
          example: 4.7
        ratingCount:
          type: integer
          minimum: 0
          description: Number of ratings that make up the aggregate.
          example: 1234
      example:
        title: Rust Course
        studioName: Rustacean
        language: en
        ratingAverage: 4.7
        ratingCount: 1234
        externalIds:
          - source: youtube
            externalId: "youtube:playlist:PL123"

    ScrapePreviewRequest:
      type: object
      required: [kind]
      description: >-
        Input for the scrape-preview endpoint. The `kind` field determines
        which additional fields are required: `url` for kind=url, `query`
        for kind=name, `fragment` for kind=fragment. `source` pins the
        scraper explicitly; when omitted the registry auto-detects from the
        URL (kind=url) or falls back to `json-ld` (kind=fragment). Required
        for kind=name.
      properties:
        source:
          type: string
          description: >-
            Explicit scraper id (e.g. `youtube`, `udemy`, `json-ld`). Omit
            to auto-detect (kind=url) or default to json-ld (kind=fragment).
            Required for kind=name.
          example: youtube
        kind:
          $ref: "#/components/schemas/ScraperKind"
        url:
          type: string
          format: uri
          description: Required when kind=url.
          example: "https://www.youtube.com/playlist?list=PL123"
        query:
          type: string
          description: Required when kind=name.
          example: "rust programming"
        fragment:
          type: string
          description: Required when kind=fragment (raw HTML or JSON-LD string).
      allOf:
        - if:
            properties:
              kind: { const: url }
          then:
            required: [url]
        - if:
            properties:
              kind: { const: name }
          then:
            required: [query, source]
        - if:
            properties:
              kind: { const: fragment }
          then:
            required: [fragment]
      example:
        kind: url
        url: "https://www.youtube.com/playlist?list=PL123"

    ScrapePreviewResponse:
      type: object
      required: [candidates]
      description: Zero or more scrape candidates returned by the preview endpoint.
      properties:
        candidates:
          type: array
          items:
            $ref: "#/components/schemas/ScrapeCandidateDto"
          description: Ordered list of scrape candidates; may be empty.
      example:
        candidates:
          - source: youtube
            sourceUrl: "https://www.youtube.com/playlist?list=PL123"
            fragment:
              title: Rust Course
              studioName: Rustacean
              externalIds:
                - source: youtube
                  externalId: "youtube:playlist:PL123"

    ScraperInfoDto:
      type: object
      required: [id, supportedKinds, configured]
      description: Metadata about a single registered scraper.
      properties:
        id:
          type: string
          description: Stable scraper identifier used as the `source` field in requests.
          example: youtube
        supportedKinds:
          type: array
          items:
            $ref: "#/components/schemas/ScraperKind"
          description: Invocation kinds this scraper handles.
          example: [url, name, fragment]
        configured:
          type: boolean
          description: >-
            True when all required credentials / config are present on this
            instance (e.g. YouTube requires an API key). Unconfigured scrapers
            are omitted from the registry entirely — this flag is always true
            for listed scrapers.
          example: true
      example:
        id: youtube
        supportedKinds: [url, name, fragment]
        configured: true

    ScraperKind:
      type: string
      enum: [url, name, fragment]
      description: >-
        Invocation kind for the scrape-preview endpoint.
        `url` — fetch and parse a remote URL;
        `name` — search the source by course title;
        `fragment` — parse a raw HTML or JSON-LD string supplied by the caller.
      example: url

    ScraperListDto:
      type: object
      required: [scrapers]
      description: List of scrapers configured on this instance.
      properties:
        scrapers:
          type: array
          items:
            $ref: "#/components/schemas/ScraperInfoDto"
          description: All configured scrapers in registration order.
      example:
        scrapers:
          - id: youtube
            supportedKinds: [url, name, fragment]
            configured: true
          - id: json-ld
            supportedKinds: [url, fragment]
            configured: true

    InstructorDetailDto:
      type: object
      required: [instructor, courses, coursesTotal]
      description: Full instructor view including their associated courses (paginated, up to 20).
      properties:
        instructor:
          $ref: "#/components/schemas/InstructorDto"
        courses:
          type: array
          items:
            $ref: "#/components/schemas/CourseDto"
          description: Courses associated with this instructor, up to 20, sorted by title.
        coursesTotal:
          type: integer
          minimum: 0
          description: Total number of courses associated with this instructor (may exceed `courses.length`).
      example:
        instructor:
          id: clxvins0000000000000000001
          slug: andrei-neagoie
          displayName: Andrei Neagoie
          externalIds:
            - source: udemy
              externalId: "udemy:instructor:42"
              url: "https://udemy.com/user/42"
          coursesTotal: 1
          createdAt: "2026-05-23T10:00:00Z"
          updatedAt: "2026-05-23T10:00:00Z"
        courses: []
        coursesTotal: 1

    InstructorDto:
      type: object
      required: [id, slug, displayName, externalIds, coursesTotal, createdAt, updatedAt]
      description: Full representation of an Instructor aggregate.
      properties:
        id:
          type: string
          description: Server-generated cuid.
        slug:
          $ref: "#/components/schemas/EntitySlug"
        displayName:
          type: string
          minLength: 1
          maxLength: 200
          description: Human-readable instructor name.
        externalIds:
          type: array
          items:
            $ref: "#/components/schemas/ExternalIdRef"
          description: External system references for this instructor.
        coursesTotal:
          type: integer
          minimum: 0
          description: Total number of courses linked to this instructor.
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      example:
        id: clxvins0000000000000000001
        slug: andrei-neagoie
        displayName: Andrei Neagoie
        externalIds:
          - source: udemy
            externalId: "udemy:instructor:42"
            url: "https://udemy.com/user/42"
        coursesTotal: 12
        createdAt: "2026-05-23T10:00:00Z"
        updatedAt: "2026-05-23T10:00:00Z"

    InstructorListDto:
      type: object
      required: [items, total, offset, limit]
      description: Paginated list of instructors.
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/InstructorDto"
        total:
          type: integer
          minimum: 0
          description: Total number of instructors matching the filter (before pagination).
        offset:
          type: integer
          minimum: 0
          description: Number of items skipped.
        limit:
          type: integer
          minimum: 1
          description: Maximum items returned per page.
      example:
        items:
          - id: clxvins0000000000000000001
            slug: andrei-neagoie
            displayName: Andrei Neagoie
            externalIds:
              - source: udemy
                externalId: "udemy:instructor:42"
                url: "https://udemy.com/user/42"
            coursesTotal: 12
            createdAt: "2026-05-23T10:00:00Z"
            updatedAt: "2026-05-23T10:00:00Z"
        total: 1
        offset: 0
        limit: 20

    InstructorRef:
      type: object
      required: [id, slug, displayName]
      description: Lightweight reference to an instructor, embedded in CourseDto.
      properties:
        id:
          type: string
          description: Server-generated cuid of the instructor.
        slug:
          $ref: "#/components/schemas/EntitySlug"
        displayName:
          type: string
          minLength: 1
          maxLength: 200
      example:
        id: clxvins0000000000000000001
        slug: andrei-neagoie
        displayName: Andrei Neagoie

    StudioDetailDto:
      type: object
      required: [studio, courses, coursesTotal]
      description: Full studio view including their associated courses (paginated, up to 20).
      properties:
        studio:
          $ref: "#/components/schemas/StudioDto"
        courses:
          type: array
          items:
            $ref: "#/components/schemas/CourseDto"
          description: Courses associated with this studio, up to 20, sorted by title.
        coursesTotal:
          type: integer
          minimum: 0
          description: Total number of courses associated with this studio (may exceed `courses.length`).
      example:
        studio:
          id: clxvstu0000000000000000001
          slug: zero-to-mastery
          displayName: Zero To Mastery
          externalIds:
            - source: udemy
              externalId: "udemy:organization:ztm"
              url: "https://udemy.com/organization/ztm"
          coursesTotal: 5
          createdAt: "2026-05-23T10:00:00Z"
          updatedAt: "2026-05-23T10:00:00Z"
        courses: []
        coursesTotal: 5

    StudioDto:
      type: object
      required: [id, slug, displayName, externalIds, coursesTotal, createdAt, updatedAt]
      description: Full representation of a Studio aggregate.
      properties:
        id:
          type: string
          description: Server-generated cuid.
        slug:
          $ref: "#/components/schemas/EntitySlug"
        displayName:
          type: string
          minLength: 1
          maxLength: 200
          description: Human-readable studio name.
        externalIds:
          type: array
          items:
            $ref: "#/components/schemas/ExternalIdRef"
          description: External system references for this studio.
        coursesTotal:
          type: integer
          minimum: 0
          description: Total number of courses linked to this studio.
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      example:
        id: clxvstu0000000000000000001
        slug: zero-to-mastery
        displayName: Zero To Mastery
        externalIds:
          - source: udemy
            externalId: "udemy:organization:ztm"
            url: "https://udemy.com/organization/ztm"
        coursesTotal: 5
        createdAt: "2026-05-23T10:00:00Z"
        updatedAt: "2026-05-23T10:00:00Z"

    StudioListDto:
      type: object
      required: [items, total, offset, limit]
      description: Paginated list of studios.
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/StudioDto"
        total:
          type: integer
          minimum: 0
          description: Total number of studios matching the filter (before pagination).
        offset:
          type: integer
          minimum: 0
          description: Number of items skipped.
        limit:
          type: integer
          minimum: 1
          description: Maximum items returned per page.
      example:
        items:
          - id: clxvstu0000000000000000001
            slug: zero-to-mastery
            displayName: Zero To Mastery
            externalIds:
              - source: udemy
                externalId: "udemy:organization:ztm"
                url: "https://udemy.com/organization/ztm"
            coursesTotal: 5
            createdAt: "2026-05-23T10:00:00Z"
            updatedAt: "2026-05-23T10:00:00Z"
        total: 1
        offset: 0
        limit: 20

    StudioRef:
      type: object
      required: [id, slug, displayName]
      description: Lightweight reference to a studio, embedded in CourseDto.
      properties:
        id:
          type: string
          description: Server-generated cuid of the studio.
        slug:
          $ref: "#/components/schemas/EntitySlug"
        displayName:
          type: string
          minLength: 1
          maxLength: 200
      example:
        id: clxvstu0000000000000000001
        slug: zero-to-mastery
        displayName: Zero To Mastery

    TagDetailDto:
      type: object
      required: [tag, courses, coursesTotal]
      description: Full tag view including associated courses (paginated, up to 20).
      properties:
        tag:
          $ref: "#/components/schemas/TagDto"
        courses:
          type: array
          items:
            $ref: "#/components/schemas/CourseDto"
          description: Courses associated with this tag, up to 20, sorted by title.
        coursesTotal:
          type: integer
          minimum: 0
          description: Total number of courses associated with this tag (may exceed `courses.length`).
      example:
        tag:
          id: clxvtag0000000000000000001
          slug: javascript
          displayName: JavaScript
          category: language
          externalIds: []
          coursesTotal: 8
          createdAt: "2026-05-23T10:00:00Z"
          updatedAt: "2026-05-23T10:00:00Z"
        courses: []
        coursesTotal: 8

    TagDto:
      type: object
      required: [id, slug, displayName, externalIds, coursesTotal, createdAt, updatedAt]
      description: Full representation of a Tag aggregate.
      properties:
        id:
          type: string
          description: Server-generated cuid.
        slug:
          $ref: "#/components/schemas/EntitySlug"
        displayName:
          type: string
          minLength: 1
          maxLength: 200
          description: Human-readable tag name.
        category:
          oneOf:
            - type: string
              maxLength: 64
            - type: "null"
          description: Free-form category label (e.g. `language`, `framework`, `topic`). Null when uncategorised.
        externalIds:
          type: array
          items:
            $ref: "#/components/schemas/ExternalIdRef"
          description: External system references for this tag.
        coursesTotal:
          type: integer
          minimum: 0
          description: Total number of courses linked to this tag.
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      example:
        id: clxvtag0000000000000000001
        slug: javascript
        displayName: JavaScript
        category: language
        externalIds: []
        coursesTotal: 8
        createdAt: "2026-05-23T10:00:00Z"
        updatedAt: "2026-05-23T10:00:00Z"

    TagListDto:
      type: object
      required: [items, total, offset, limit]
      description: Paginated list of tags.
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/TagDto"
        total:
          type: integer
          minimum: 0
          description: Total number of tags matching the filter (before pagination).
        offset:
          type: integer
          minimum: 0
          description: Number of items skipped.
        limit:
          type: integer
          minimum: 1
          description: Maximum items returned per page.
      example:
        items:
          - id: clxvtag0000000000000000001
            slug: javascript
            displayName: JavaScript
            category: language
            externalIds: []
            coursesTotal: 8
            createdAt: "2026-05-23T10:00:00Z"
            updatedAt: "2026-05-23T10:00:00Z"
        total: 1
        offset: 0
        limit: 20

    TagRef:
      type: object
      required: [id, slug, displayName]
      description: Lightweight reference to a tag, embedded in CourseDto.
      properties:
        id:
          type: string
          description: Server-generated cuid of the tag.
        slug:
          $ref: "#/components/schemas/EntitySlug"
        displayName:
          type: string
          minLength: 1
          maxLength: 200
        category:
          oneOf:
            - type: string
              maxLength: 64
            - type: "null"
          description: Tag category, or null when uncategorised.
      example:
        id: clxvtag0000000000000000001
        slug: javascript
        displayName: JavaScript
        category: language

    UpsertInstructorRequest:
      type: object
      required: [displayName]
      description: Payload for creating or updating an instructor.
      properties:
        displayName:
          type: string
          minLength: 1
          maxLength: 200
          description: Human-readable name displayed in the UI.
        slug:
          $ref: "#/components/schemas/EntitySlug"
          description: URL-safe slug. Auto-generated from `displayName` when omitted.
        externalIds:
          type: array
          items:
            $ref: "#/components/schemas/ExternalIdRef"
          description: External system references for this instructor.
      example:
        displayName: Andrei Neagoie
        slug: andrei-neagoie
        externalIds:
          - source: udemy
            externalId: "udemy:instructor:42"
            url: "https://udemy.com/user/42"

    UpsertStudioRequest:
      type: object
      required: [displayName]
      description: Payload for creating or updating a studio.
      properties:
        displayName:
          type: string
          minLength: 1
          maxLength: 200
          description: Human-readable studio name.
        slug:
          $ref: "#/components/schemas/EntitySlug"
          description: URL-safe slug. Auto-generated from `displayName` when omitted.
        externalIds:
          type: array
          items:
            $ref: "#/components/schemas/ExternalIdRef"
          description: External system references for this studio.
      example:
        displayName: Zero To Mastery
        slug: zero-to-mastery
        externalIds:
          - source: udemy
            externalId: "udemy:organization:ztm"
            url: "https://udemy.com/organization/ztm"

    UpsertTagRequest:
      type: object
      required: [displayName]
      description: Payload for creating or updating a tag.
      properties:
        displayName:
          type: string
          minLength: 1
          maxLength: 200
          description: Human-readable tag name.
        slug:
          $ref: "#/components/schemas/EntitySlug"
          description: URL-safe slug. Auto-generated from `displayName` when omitted.
        category:
          oneOf:
            - type: string
              maxLength: 64
            - type: "null"
          description: Free-form category label (e.g. `language`, `framework`, `topic`). Pass `null` to clear.
        externalIds:
          type: array
          items:
            $ref: "#/components/schemas/ExternalIdRef"
          description: External system references for this tag.
      example:
        displayName: JavaScript
        slug: javascript
        category: language

    AccessGrantDto:
      type: object
      required: [id, userId, target, level, createdAt]
      description: A single access grant issued by an admin.
      properties:
        id:
          type: string
          description: Server-generated cuid identifying this grant.
        userId:
          type: string
          description: Better Auth user id of the grantee.
        target:
          $ref: "#/components/schemas/GrantTarget"
        level:
          $ref: "#/components/schemas/GrantLevel"
        createdAt:
          type: string
          format: date-time
          description: ISO-8601 instant when the grant was created.
      examples:
        - id: clxvgnt0000000000000000001
          userId: clxvusr0000000000000000001
          target:
            kind: library
            libraryId: clxvp1234567890abcdefghij
          level: READ
          createdAt: "2026-04-25T10:00:00Z"
        - id: clxvgnt0000000000000000002
          userId: clxvusr0000000000000000001
          target:
            kind: course
            courseId: clxvcrs0000000000000000001
          level: READ
          createdAt: "2026-04-25T10:05:00Z"

    AccessGrantListDto:
      type: object
      required: [items]
      description: Paginated list of access grants for a given user.
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/AccessGrantDto"
      example:
        items:
          - id: clxvgnt0000000000000000001
            userId: clxvusr0000000000000000001
            target:
              kind: library
              libraryId: clxvp1234567890abcdefghij
            level: READ
            createdAt: "2026-04-25T10:00:00Z"
          - id: clxvgnt0000000000000000002
            userId: clxvusr0000000000000000001
            target:
              kind: course
              courseId: clxvcrs0000000000000000001
            level: READ
            createdAt: "2026-04-25T10:05:00Z"

    AdminDashboardDto:
      type: object
      required: [generatedAt, counts, latestScan, errorsLast24h]
      additionalProperties: false
      properties:
        generatedAt:
          type: string
          format: date-time
          description: ISO-8601 instant when the snapshot was assembled (server clock).
        counts:
          type: object
          required: [libraries, users, courses, lessons]
          additionalProperties: false
          properties:
            libraries:
              type: integer
              minimum: 0
            users:
              type: integer
              minimum: 0
            courses:
              type: integer
              minimum: 0
            lessons:
              type: integer
              minimum: 0
        latestScan:
          oneOf:
            - $ref: "#/components/schemas/AdminDashboardLatestScan"
            - type: "null"
          description: |
            Most recent scan across all libraries (highest `startedAt`), or
            `null` when no scan has ever run.
        errorsLast24h:
          type: integer
          minimum: 0
          description: |
            Count of `ScanErrorRecord` rows whose parent scan started within
            the last 24 hours (rolling window from `generatedAt`). Uses the
            parent scan's `startedAt` because the error record itself has no
            timestamp; works because no scan is expected to outlive a single
            24-hour window.

    AdminDashboardLatestScan:
      type: object
      required: [scanId, libraryId, status, startedAt, finishedAt, filesScanned, errorsCount]
      additionalProperties: false
      properties:
        scanId:
          type: string
          description: cuid identifying the scan.
        libraryId:
          type: string
          description: cuid of the library this scan ran against.
        status:
          $ref: "#/components/schemas/ScanStatus"
        startedAt:
          type: string
          format: date-time
        finishedAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
          description: null while still `running`.
        filesScanned:
          type: integer
          minimum: 0
        errorsCount:
          type: integer
          minimum: 0
          description: Number of error records attached to this scan.

    AdminLibraryListDto:
      type: object
      required: [items]
      additionalProperties: false
      description: >-
        Admin-only listing of every library with the counters and last-scan
        summary the admin libraries page renders per row.
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/AdminLibraryListItem"

    AdminLibraryListItem:
      type: object
      required: [id, name, rootPath, coursesCount, lessonsCount, lastScan]
      additionalProperties: false
      description: >-
        One row in the admin libraries list. Same backbone as `LibraryDto`
        plus computed `coursesCount`, `lessonsCount` and an embedded
        `lastScan` summary (or `null` when no scan has ever run).
      properties:
        id:
          type: string
        name:
          type: string
          minLength: 1
          maxLength: 200
        rootPath:
          type: string
          minLength: 1
        coursesCount:
          type: integer
          minimum: 0
        lessonsCount:
          type: integer
          minimum: 0
        lastScan:
          oneOf:
            - $ref: "#/components/schemas/AdminLibraryListItemScan"
            - type: "null"
          description: Most recent scan summary, or `null` when no scan has ever run.

    AdminLibraryListItemScan:
      type: object
      required: [status, startedAt, finishedAt, errorsCount]
      additionalProperties: false
      description: >-
        Compact scan summary embedded in `AdminLibraryListItem`. Excludes
        scanId / filesScanned / coursesAdded — the libraries-list row only
        renders status + age + errors. The detail page fetches the full
        scan history via `listAdminScans?libraryId=…`.
      properties:
        status:
          $ref: "#/components/schemas/ScanStatus"
        startedAt:
          type: string
          format: date-time
        finishedAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
          description: null while still `running`.
        errorsCount:
          type: integer
          minimum: 0

    AdminUserListDto:
      type: object
      required: [items]
      additionalProperties: false
      description: >-
        Admin-only listing of every user in the platform. The admin users
        page renders one `AdminUserListItem` per row.
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/AdminUserListItem"

    AdminUserListItem:
      type: object
      required: [id, email, name, displayName, role, banned, createdAt, updatedAt]
      additionalProperties: false
      description: One row in the admin users list.
      properties:
        id:
          type: string
        email:
          type: string
          format: email
        name:
          type: string
          minLength: 1
        displayName:
          oneOf:
            - type: string
              minLength: 1
            - type: "null"
          description: Optional user-facing handle separate from the auth name.
        role:
          $ref: "#/components/schemas/AdminUserRole"
        banned:
          type: boolean
          description: >-
            Soft-delete flag. When `true`, sign-in fails and existing sessions
            are invalidated by Better Auth's admin plugin.
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    AdminUserRole:
      type: string
      enum: [admin, user, guest]
      description: >-
        Canonical lowercase role used by the SPA. The auth backend stamps
        upper-case values (`'ADMIN'`, `'USER'`) in the database; this DTO
        normalises to lowercase at the boundary.

    AdminUpdateUserRequest:
      type: object
      additionalProperties: false
      description: >-
        Patch body for `PATCH /admin/users/{id}`. At least one field must
        be set — the handler returns 400 on an empty body.
      properties:
        role:
          $ref: "#/components/schemas/AdminUserRole"
        banned:
          type: boolean

    AdminScanListDto:
      type: object
      required: [items]
      additionalProperties: false
      description: >-
        Page of recent scans across every library, ordered by `startedAt`
        descending. The dashboard's "Recent scans" table consumes this.
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/AdminScanListItem"

    AdminScanListItem:
      type: object
      required:
        - scanId
        - libraryId
        - libraryName
        - status
        - startedAt
        - finishedAt
        - filesScanned
        - coursesAdded
        - errorsCount
      additionalProperties: false
      description: >-
        One row in the dashboard's recent-scans table. Same backbone as
        `AdminDashboardLatestScan` plus `libraryName` (so the table can
        render the library label without a second round-trip) and
        `coursesAdded` (highlighted as a "+N" badge in the design).
      properties:
        scanId:
          type: string
        libraryId:
          type: string
        libraryName:
          type: string
          minLength: 1
        status:
          $ref: "#/components/schemas/ScanStatus"
        startedAt:
          type: string
          format: date-time
        finishedAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
          description: null while still `running`.
        filesScanned:
          type: integer
          minimum: 0
        coursesAdded:
          type: integer
          minimum: 0
          description: Net new course rows persisted by this scan.
        errorsCount:
          type: integer
          minimum: 0

    BookmarkDto:
      type: object
      required: [id, lessonId, positionSeconds, createdAt, updatedAt]
      description: A single user-owned bookmark pinned to a position within a lesson.
      properties:
        id:
          type: string
          description: Server-generated cuid identifying this bookmark.
        lessonId:
          type: string
          description: cuid of the lesson this bookmark belongs to.
        positionSeconds:
          type: integer
          minimum: 0
          description: Playback position in seconds where the bookmark is pinned.
        label:
          type: string
          minLength: 1
          maxLength: 200
          description: >-
            Free-form label. Trimmed server-side; absent means the bookmark has
            no label.
        createdAt:
          type: string
          format: date-time
          description: ISO-8601 instant when the bookmark was created.
        updatedAt:
          type: string
          format: date-time
          description: ISO-8601 instant when the bookmark was last updated.
      example:
        id: clxvbkm0000000000000000001
        lessonId: clxvles0000000000000000001
        positionSeconds: 187
        label: Aggregates intro
        createdAt: "2026-04-25T14:00:00Z"
        updatedAt: "2026-04-25T14:00:00Z"

    BookmarkListDto:
      type: object
      required: [items]
      description: >-
        The requesting user's bookmarks for a single lesson, sorted ascending by
        `positionSeconds`.
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/BookmarkDto"
      example:
        items:
          - id: clxvbkm0000000000000000001
            lessonId: clxvles0000000000000000001
            positionSeconds: 187
            label: Aggregates intro
            createdAt: "2026-04-25T14:00:00Z"
            updatedAt: "2026-04-25T14:00:00Z"
          - id: clxvbkm0000000000000000002
            lessonId: clxvles0000000000000000001
            positionSeconds: 742
            label: Invariants section
            createdAt: "2026-04-25T14:12:00Z"
            updatedAt: "2026-04-25T14:12:00Z"

    CreateBookmarkRequest:
      type: object
      required: [positionSeconds]
      description: Payload for creating a new bookmark on a lesson.
      properties:
        positionSeconds:
          type: integer
          minimum: 0
          description: Playback position in seconds to pin the bookmark at.
        label:
          type: string
          minLength: 1
          maxLength: 200
          description: Optional free-form label. Trimmed server-side.
      example:
        positionSeconds: 187
        label: Aggregates intro

    ContinueWatchingDto:
      type: object
      required: [items]
      description: >-
        Ordered list of courses the requester is in the middle of, most-recently-
        watched first. Empty array for new users.
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/ContinueWatchingItem"
      example:
        items:
          - courseId: clxvcrs0000000000000000001
            courseTitle: Pragmatic Clean Architecture
            librarySlug: conference-recordings
            percent: 15.79
            lessonsCompleted: 12
            lessonsTotal: 76
            lastSeenAt: "2026-04-25T14:32:00Z"
            lastSeenLessonId: clxvles0000000000000000042
          - courseId: clxvcrs0000000000000000002
            courseTitle: Domain-Driven Design by Eric Evans
            percent: 33.33
            lessonsCompleted: 6
            lessonsTotal: 18
            lastSeenAt: "2026-04-24T10:10:00Z"
            lastSeenLessonId: clxvles0000000000000000007

    ContinueWatchingItem:
      type: object
      required:
        - courseId
        - courseTitle
        - percent
        - lessonsCompleted
        - lessonsTotal
        - lastSeenAt
        - lastSeenLessonId
      description: >-
        A single entry in the continue-watching row, sourced from the
        `CourseProgressReadModel` projection.
      properties:
        courseId:
          type: string
          description: Server-generated cuid identifying the course.
        courseTitle:
          type: string
          minLength: 1
          maxLength: 200
          description: Display title of the course.
        librarySlug:
          type: string
          description: >-
            Slug of the parent library, included for the URL builder.
            Optional because not every layout exposes a per-library slug yet.
        percent:
          type: number
          minimum: 0
          maximum: 100
          description: "Course completion = `lessonsCompleted / lessonsTotal * 100`."
        lessonsCompleted:
          type: integer
          minimum: 0
          description: Number of lessons the user has completed in this course.
        lessonsTotal:
          type: integer
          minimum: 0
          description: Total number of lessons in the course.
        lastSeenAt:
          type: string
          format: date-time
          description: >-
            Most recent moment any lesson in this course was watched
            (completed or not).
        lastSeenLessonId:
          type: string
          description: >-
            The lesson the player last reported a position on, used to wire
            the 'Resume' CTA.
      example:
        courseId: clxvcrs0000000000000000001
        courseTitle: Pragmatic Clean Architecture
        librarySlug: conference-recordings
        percent: 15.79
        lessonsCompleted: 12
        lessonsTotal: 76
        lastSeenAt: "2026-04-25T14:32:00Z"
        lastSeenLessonId: clxvles0000000000000000042

    RecentlyAddedDto:
      type: object
      required: [items]
      description: >-
        Courses added to the requester's libraries, most recent first.
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/RecentlyAddedItem"
      example:
        items:
          - courseId: clxvcrs0000000000000000010
            courseTitle: Implementing Domain-Driven Design
            librarySlug: conference-recordings
            lessonCount: 24
            totalDurationSeconds: 14400
            createdAt: "2026-04-26T08:14:00Z"

    RecentlyAddedItem:
      type: object
      required:
        - courseId
        - courseTitle
        - lessonCount
        - totalDurationSeconds
        - createdAt
      description: A course freshly added to one of the requester's libraries.
      properties:
        courseId:
          type: string
          description: Server-generated cuid identifying the course.
        courseTitle:
          type: string
          minLength: 1
          maxLength: 200
          description: Display title of the course.
        librarySlug:
          type: string
          description: >-
            Slug of the parent library, included for the URL builder.
            Optional because not every layout exposes a per-library slug yet.
        lessonCount:
          type: integer
          minimum: 0
          description: Number of lessons in the course at intake time.
        totalDurationSeconds:
          type: integer
          minimum: 0
          description: Sum of `Lesson.duration` across the course, in whole seconds.
        createdAt:
          type: string
          format: date-time
          description: Moment the course was added to its library.
      example:
        courseId: clxvcrs0000000000000000010
        courseTitle: Implementing Domain-Driven Design
        librarySlug: conference-recordings
        lessonCount: 24
        totalDurationSeconds: 14400
        createdAt: "2026-04-26T08:14:00Z"

    RecentlyCompletedDto:
      type: object
      required: [items]
      description: >-
        Courses the requester finished most recently, most-recent first.
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/RecentlyCompletedItem"
      example:
        items:
          - courseId: clxvcrs0000000000000000020
            courseTitle: Distributed Systems Foundations
            librarySlug: conference-recordings
            lessonsTotal: 18
            completedAt: "2026-04-23T20:45:00Z"

    RecentlyCompletedItem:
      type: object
      required:
        - courseId
        - courseTitle
        - lessonsTotal
        - completedAt
      description: >-
        A course where the requester completed every lesson. Sourced from
        the `CourseProgressReadModel` projection (`lessonsCompleted ==
        lessonsTotal`).
      properties:
        courseId:
          type: string
          description: Server-generated cuid identifying the course.
        courseTitle:
          type: string
          minLength: 1
          maxLength: 200
          description: Display title of the course.
        librarySlug:
          type: string
          description: Slug of the parent library, included for the URL builder.
        lessonsTotal:
          type: integer
          minimum: 0
          description: Total lessons in the course (== lessons completed for this row).
        completedAt:
          type: string
          format: date-time
          description: >-
            Time the requester finished the last lesson —
            `CourseProgressReadModel.lastSeenAt` at the moment percent hit 100.
      example:
        courseId: clxvcrs0000000000000000020
        courseTitle: Distributed Systems Foundations
        librarySlug: conference-recordings
        lessonsTotal: 18
        completedAt: "2026-04-23T20:45:00Z"

    YourWeekDto:
      type: object
      required: [minutesWatched, lessonsCompleted, range]
      description: >-
        Roll-up of the requester's activity over the trailing seven days.
      properties:
        minutesWatched:
          type: integer
          minimum: 0
          description: >-
            Total whole minutes watched by the requester in the window.
            Computed by summing duration across `LessonProgress` rows whose
            `updatedAt` falls inside `range`.
        lessonsCompleted:
          type: integer
          minimum: 0
          description: >-
            Number of lessons the requester completed during the window.
            Counted from `LessonProgress.completedAt`.
        range:
          $ref: "#/components/schemas/DateRange"
      example:
        minutesWatched: 312
        lessonsCompleted: 8
        range:
          from: "2026-04-20T22:00:00Z"
          to: "2026-04-27T22:00:00Z"

    DateRange:
      type: object
      required: [from, to]
      description: Half-open interval `[from, to)`, both ISO-8601 with offset.
      properties:
        from:
          type: string
          format: date-time
          description: Inclusive lower bound.
        to:
          type: string
          format: date-time
          description: Exclusive upper bound.

    CourseDto:
      type: object
      required: [id, libraryId, slug, title, sections, progress, createdAt, updatedAt]
      description: Full representation of a Course aggregate.
      properties:
        id:
          type: string
          description: Server-generated cuid identifying this course.
        libraryId:
          type: string
          description: cuid of the library this course belongs to.
        slug:
          $ref: "#/components/schemas/CourseSlug"
        title:
          type: string
          minLength: 1
          maxLength: 200
        description:
          type: string
          maxLength: 4000
        sections:
          type: array
          items:
            $ref: "#/components/schemas/SectionDto"
          description: Sections sorted ascending by position.
        progress:
          $ref: "#/components/schemas/CourseProgress"
        instructors:
          type: array
          items:
            $ref: "#/components/schemas/InstructorRef"
          default: []
          description: Instructors associated with this course. Empty array when none linked.
        studios:
          type: array
          items:
            $ref: "#/components/schemas/StudioRef"
          default: []
          description: Studios associated with this course. Empty array when none linked.
        tags:
          type: array
          items:
            $ref: "#/components/schemas/TagRef"
          default: []
          description: Tags associated with this course. Empty array when none linked.
        level:
          oneOf:
            - $ref: "#/components/schemas/CourseLevel"
            - type: "null"
          description: Difficulty level of the course, or null when not set.
        language:
          oneOf:
            - type: string
            - type: "null"
          description: >-
            BCP-47 language tag for the course primary language (e.g. `en`,
            `en-US`). Validated server-side; null when not set.
        releaseDate:
          oneOf:
            - type: string
              format: date
            - type: "null"
          description: ISO-8601 date when the course was originally released. Null when not set.
        posterUrl:
          oneOf:
            - type: string
              format: uri
            - type: "null"
          description: URL of the course poster image. Null when not set.
        ratingAverage:
          oneOf:
            - type: number
              multipleOf: 0.01
              minimum: 0
              maximum: 5
            - type: "null"
          description: Average rating on a 0–5 scale (two decimal places). Null when not set.
        ratingCount:
          oneOf:
            - type: integer
              minimum: 0
            - type: "null"
          description: Number of ratings contributing to `ratingAverage`. Null when not set.
        externalIds:
          type: array
          items:
            $ref: "#/components/schemas/ExternalIdRef"
          default: []
          description: External system references for this course. Empty array when none.
        sourceUpdatedAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
          description: Timestamp from the upstream source when this course was last updated. Null when not set.
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      example:
        id: clxvcrs0000000000000000001
        libraryId: clxvp1234567890abcdefghij
        slug: pragmatic-clean-architecture
        title: Pragmatic Clean Architecture
        description: Milan Jovanović's deep-dive into Clean Architecture with .NET.
        sections:
          - id: clxvsec0000000000000000001
            position: 1
            title: Introduction
          - id: clxvsec0000000000000000002
            position: 2
            title: Domain Layer
        progress:
          percent: 0
          lessonsCompleted: 0
          lessonsTotal: 24
        instructors: []
        studios: []
        tags: []
        level: null
        language: null
        releaseDate: null
        posterUrl: null
        ratingAverage: null
        ratingCount: null
        externalIds: []
        sourceUpdatedAt: null
        createdAt: "2026-04-25T09:00:00Z"
        updatedAt: "2026-04-25T09:00:00Z"

    CourseListDto:
      type: object
      required: [items]
      description: List of courses visible to the requester.
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/CourseDto"
      example:
        items:
          - id: clxvcrs0000000000000000001
            libraryId: clxvp1234567890abcdefghij
            slug: pragmatic-clean-architecture
            title: Pragmatic Clean Architecture
            description: Milan Jovanović's deep-dive into Clean Architecture with .NET.
            sections:
              - id: clxvsec0000000000000000001
                position: 1
                title: Introduction
              - id: clxvsec0000000000000000002
                position: 2
                title: Domain Layer
            progress:
              percent: 0
              lessonsCompleted: 0
              lessonsTotal: 24
            createdAt: "2026-04-25T09:00:00Z"
            updatedAt: "2026-04-25T09:00:00Z"
          - id: clxvcrs0000000000000000002
            libraryId: clxvp1234567890abcdefghij
            slug: ddd-by-eric-evans
            title: Domain-Driven Design by Eric Evans
            sections:
              - id: clxvsec0000000000000000003
                position: 1
                title: Putting the Domain Model to Work
              - id: clxvsec0000000000000000004
                position: 2
                title: Building Blocks of a Model-Driven Design
            progress:
              percent: 0
              lessonsCompleted: 0
              lessonsTotal: 18
            createdAt: "2026-04-25T09:05:00Z"
            updatedAt: "2026-04-25T09:05:00Z"

    CourseOutlineDto:
      type: object
      required: [course, sections, materials]
      description: >-
        Page-shaped projection of a course — enough data to render the Course
        detail screen in one round-trip. Aggregates sections, lessons (lite),
        and course-level materials with the requester's progress applied.
      properties:
        course:
          $ref: "#/components/schemas/CourseOutlineSummary"
        sections:
          type: array
          description: Sections sorted by position.
          items:
            $ref: "#/components/schemas/SectionOutline"
        materials:
          type: array
          description: >-
            Course-level materials, deduplicated and aggregated across every
            lesson in the course. Empty array when no lesson carries
            materials.
          items:
            $ref: "#/components/schemas/CourseMaterialItem"

    CourseOutlineSummary:
      type: object
      required: [id, title, lessonsTotal, totalDurationSeconds, progress, createdAt, updatedAt]
      description: Course-level fields surfaced in the page hero.
      properties:
        id:
          type: string
          description: Server-generated cuid identifying the course.
        title:
          type: string
          minLength: 1
          maxLength: 200
        slug:
          type: string
        description:
          type: ["string", "null"]
          description: Long-form description rendered under the title.
        instructor:
          type: ["string", "null"]
          description: >-
            Visible "by …" label. Optional — may be null until the catalog
            DTO grows the field.
        librarySlug:
          type: string
          description: >-
            Slug of the parent library, included for breadcrumbs. Optional
            because Library has no slug field yet (same caveat as
            ContinueWatchingItem).
        lessonsTotal:
          type: integer
          minimum: 0
        totalDurationSeconds:
          type: integer
          minimum: 0
          description: Sum of `Lesson.duration` across the course (whole seconds).
        progress:
          $ref: "#/components/schemas/CourseProgress"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    SectionOutline:
      type: object
      required: [id, position, title, totalDurationSeconds, lessons]
      description: One section in the outline, with its lesson list inline.
      properties:
        id:
          type: string
          description: Server-generated cuid identifying this section.
        position:
          type: integer
          minimum: 1
          description: 1-based position within the course.
        title:
          type: string
          minLength: 1
          maxLength: 200
        totalDurationSeconds:
          type: integer
          minimum: 0
          description: Sum of `Lesson.duration` across this section's lessons.
        lessons:
          type: array
          description: Lessons sorted by position.
          items:
            $ref: "#/components/schemas/LessonOutlineItem"

    LessonOutlineItem:
      type: object
      required:
        - id
        - position
        - title
        - durationSeconds
        - hasMaterials
        - state
        - progressPercent
      description: >-
        Lightweight per-lesson row for the Course detail outline. Maps 1:1 to
        the AppLessonRow component's props.
      properties:
        id:
          type: string
        position:
          type: integer
          minimum: 1
        title:
          type: string
          minLength: 1
          maxLength: 200
        durationSeconds:
          type: integer
          minimum: 0
        hasMaterials:
          type: boolean
          description: Whether the lesson has at least one sidecar material.
        state:
          type: string
          enum: [not-started, in-progress, completed, locked]
          description: >-
            Per-user lesson state. Derived: `completed` when the
            `LessonProgress` row has `completed: true`; `in-progress` when
            it has progress but is not complete; `locked` when the
            requester does not hold a READ grant on the course's library
            (defensive — usually the whole course 403s before this);
            `not-started` otherwise.
        progressPercent:
          type: integer
          minimum: 0
          maximum: 100
          description: 0..100 — only meaningful when `state === 'in-progress'`.

    CourseMaterialItem:
      type: object
      required: [id, lessonId, sectionId, sectionTitle, kind, label, sizeBytes]
      description: |
        One sidecar material aggregated at the course level. The
        `materials[]` list returned by `getCourseOutline` is sorted by
        `(section.position, lesson.position, material.id)` so consecutive
        items belong to the same section — the right-rail groups them by
        `sectionId` and renders a small caption per cluster.

        `sectionTitle` is included so the rail can render the caption
        without a per-item lookup against the outline's `sections[]`.
      properties:
        id:
          type: string
        lessonId:
          type: string
          description: Owning lesson id. Used by the right-rail to link to the lesson.
        sectionId:
          type: string
          description: Owning section id. Used by the rail to group items.
        sectionTitle:
          type: string
          description: >-
            Title of the owning section, denormalised so the rail can
            render its grouping caption without resolving via
            `sections[]`.
        kind:
          type: string
          enum: [doc, note, image, slide]
        label:
          type: string
        sizeBytes:
          type: integer
          minimum: 0

    CourseProgress:
      type: object
      required: [percent, lessonsCompleted, lessonsTotal]
      description: Progress summary for a course from the requester's perspective.
      properties:
        percent:
          type: number
          minimum: 0
          maximum: 100
          description: >-
            Completion percent. v1 always returns 0 — populated once the
            LessonProgress projector lands (E10-F01-S01).
        lessonsCompleted:
          type: integer
          minimum: 0
        lessonsTotal:
          type: integer
          minimum: 0
      example:
        percent: 0
        lessonsCompleted: 0
        lessonsTotal: 24

    CourseSlug:
      type: string
      pattern: "^[a-z0-9](?:[a-z0-9-]{0,98}[a-z0-9])?$"
      description: >-
        URL-safe slug. 1–100 chars, lowercase ASCII letters, digits, and hyphens;
        cannot start or end with a hyphen. Unique within a library.
      example: pragmatic-clean-architecture

    DependencyStatus:
      type: ["string", "null"]
      enum: [ok, degraded, down]

    GrantLevel:
      type: string
      enum: [READ]
      description: >-
        Access level. v1 only issues READ; the enum is open for future levels
        (e.g. ADMIN, NONE for explicit denials).

    GrantTarget:
      description: The resource to which access is granted. Discriminated by `kind`.
      oneOf:
        - title: LibraryTarget
          type: object
          required: [kind, libraryId]
          properties:
            kind:
              type: string
              enum: [library]
              description: Discriminator value indicating a library-scoped grant.
            libraryId:
              type: string
              description: Server-generated cuid of the target library.
          additionalProperties: false
          example:
            kind: library
            libraryId: clxvp1234567890abcdefghij
        - title: CourseTarget
          type: object
          required: [kind, courseId]
          properties:
            kind:
              type: string
              enum: [course]
              description: Discriminator value indicating a course-scoped grant. Accepted in v1 even though the Course aggregate (E06-F03-S01) has not landed yet — keeps the contract stable for E14-F04-S01.
            courseId:
              type: string
              description: Server-generated cuid of the target course (e.g. "DDD by Eric Evans").
          additionalProperties: false
          example:
            kind: course
            courseId: clxvcrs0000000000000000001
      discriminator:
        propertyName: kind
        mapping:
          library: "#/components/schemas/GrantTarget/oneOf/0"
          course: "#/components/schemas/GrantTarget/oneOf/1"

    HasUsersResponse:
      type: object
      required: [hasUsers]
      additionalProperties: false
      properties:
        hasUsers:
          type: boolean
          description: |
            True iff `users` table count > 0. The web SPA caches this for the
            session lifetime and re-checks only when the user explicitly
            signs out + back in.

    InstanceConfigDto:
      type: object
      required: [selfRegistration, emailVerificationRequired, ssoProviders]
      additionalProperties: false
      description: >-
        Public runtime configuration. Read once at app boot; cache for the
        session.
      properties:
        selfRegistration:
          type: boolean
          description: When false, sign-up CTAs are hidden and /sign-up redirects to /sign-in.
        emailVerificationRequired:
          type: boolean
          description: >-
            When true, sign-up wizard renders the 6-digit-code step between
            account creation and library setup.
        ssoProviders:
          type: array
          description: >-
            Configured OAuth / SSO providers. Empty array in v1 — Better
            Auth's `genericOAuth` plugin lands in v2.
          items:
            $ref: "#/components/schemas/SsoProviderConfig"

    SsoProviderConfig:
      type: object
      required: [id, label, iconName]
      additionalProperties: false
      description: One configured SSO / OAuth provider, rendered as an SsoBlock button.
      properties:
        id:
          type: string
          minLength: 1
          description: Stable identifier (e.g. `google`, `github`, `okta-foo`). Emitted on click.
        label:
          type: string
          minLength: 1
          maxLength: 80
          description: Human-readable label (e.g. `Continue with Google`).
        iconName:
          type: string
          minLength: 1
          description: IconCS glyph name (e.g. `mail`, `github`, `key`).

    HealthStatus:
      type: object
      required: [status, version, uptimeSeconds, dependencies]
      properties:
        status:
          $ref: "#/components/schemas/DependencyStatus"
        version:
          type: ["string", "null"]
          description: Semantic version of the running backend build
        uptimeSeconds:
          type: integer
          minimum: 0
          description: Seconds since the backend process started
        dependencies:
          type: object
          required: [db, redis, centrifugo]
          properties:
            db:
              $ref: "#/components/schemas/DependencyStatus"
            redis:
              $ref: "#/components/schemas/DependencyStatus"
            centrifugo:
              $ref: "#/components/schemas/DependencyStatus"

    LibraryDto:
      type: object
      required: [id, name, rootPath, createdAt, updatedAt]
      properties:
        id:
          type: string
          description: Server-generated identifier.
        name:
          type: string
          minLength: 1
          maxLength: 200
          description: Human-readable label.
        rootPath:
          type: string
          minLength: 1
          description: >-
            Absolute filesystem path to the library root. Must start with `/`
            (POSIX) or `[A-Za-z]:\` (Windows).
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      example:
        id: clxvp1234567890abcdefghij
        name: Conference Recordings
        rootPath: /srv/courses/conference
        createdAt: "2026-04-25T09:00:00Z"
        updatedAt: "2026-04-25T09:00:00Z"

    MeDto:
      type: object
      required: [id, email, name, displayName, role]
      additionalProperties: false
      description: >-
        The calling user's own profile. Returned by `GET /me` (not yet
        implemented — for now read from `GET /auth/get-session`) and by
        `PATCH /me`.
      properties:
        id:
          type: string
        email:
          type: string
          format: email
        name:
          type: string
          minLength: 1
        displayName:
          oneOf:
            - type: string
              minLength: 1
            - type: "null"
        role:
          $ref: "#/components/schemas/AdminUserRole"

    UpdateMeRequest:
      type: object
      additionalProperties: false
      description: >-
        Patch body for `PATCH /me`. At least one field must be set; the
        handler returns 400 on an empty body. Currently only
        `displayName` is exposed for self-edit.
      properties:
        displayName:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 64
            - type: "null"
          description: >-
            Optional user-facing name separate from the Better Auth `name`
            field. Pass `null` to clear it (the topbar avatar then falls
            back to `name`).

    UpdateLibraryRequest:
      type: object
      additionalProperties: false
      description: >-
        Patch body for `PATCH /libraries/{id}`. Currently only `name` is
        mutable; changing `rootPath` is intentionally unsupported.
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200

    SearchResultDto:
      type: object
      required: [query, courses, lessons]
      additionalProperties: false
      description: >-
        Two result lists for a single search query — one of course hits
        and one of lesson hits. The shape is intentionally not unified
        because each kind needs different context fields (lesson hits
        carry their parent course/section so the SPA can show breadcrumb-
        style context).
      properties:
        query:
          type: string
          description: The trimmed query string the server matched against.
        courses:
          type: array
          items:
            $ref: "#/components/schemas/SearchCourseHit"
        lessons:
          type: array
          items:
            $ref: "#/components/schemas/SearchLessonHit"

    SearchCourseHit:
      type: object
      required: [id, libraryId, title, slug, lessonsTotal]
      additionalProperties: false
      properties:
        id:
          type: string
        libraryId:
          type: string
        title:
          type: string
        slug:
          type: string
        lessonsTotal:
          type: integer
          minimum: 0

    SearchLessonHit:
      type: object
      required: [id, courseId, courseTitle, sectionTitle, title, position]
      additionalProperties: false
      properties:
        id:
          type: string
        courseId:
          type: string
        courseTitle:
          type: string
          description: Title of the parent course — included so the SPA can show breadcrumb context.
        sectionTitle:
          type: string
          description: Title of the parent section.
        title:
          type: string
        position:
          type: integer
          minimum: 1

    LibraryListDto:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/LibraryDto"
      example:
        items:
          - id: clxvp1234567890abcdefghij
            name: Conference Recordings
            rootPath: /srv/courses/conference
            createdAt: "2026-04-25T09:00:00Z"
            updatedAt: "2026-04-25T09:00:00Z"
          - id: clxvp0987654321zyxwvutsrq
            name: Tutorial Screencasts
            rootPath: /srv/courses/tutorials
            createdAt: "2026-04-20T14:30:00Z"
            updatedAt: "2026-04-22T11:00:00Z"

    LessonDto:
      type: object
      required: [id, courseId, sectionId, position, title, materials, subtitles, progress]
      description: Full representation of a Lesson aggregate, including sidecar materials and subtitle tracks.
      properties:
        id:
          type: string
          description: Server-generated cuid identifying this lesson.
        courseId:
          type: string
          description: cuid of the course this lesson belongs to.
        sectionId:
          type: string
          description: cuid of the section this lesson belongs to.
        position:
          type: integer
          minimum: 1
          description: 1-based position within the section.
        title:
          type: string
          minLength: 1
          maxLength: 200
        durationSeconds:
          type: integer
          minimum: 0
          description: >-
            Video duration in seconds. Populated by E06-F02-S02 (ffprobe).
            `undefined` until then.
        materials:
          type: array
          items:
            $ref: "#/components/schemas/MaterialDto"
          description: Sidecar materials (PDF / Markdown / text / image). Empty array when none.
        subtitles:
          type: array
          items:
            $ref: "#/components/schemas/SubtitleDto"
          description: Available subtitle tracks. Empty array when none.
        progress:
          $ref: "#/components/schemas/LessonProgress"
      example:
        id: clxvles0000000000000000001
        courseId: clxvcrs0000000000000000001
        sectionId: clxvsec0000000000000000002
        position: 3
        title: Aggregates
        durationSeconds: 2847
        materials:
          - id: clxvmat0000000000000000001
            kind: doc
            label: 03 - Aggregates slides
            sizeBytes: 204800
        subtitles:
          - id: clxvsub0000000000000000001
            language: en
            label: English
        progress:
          percent: 0
          completed: false
          lastSeenAtSeconds: 0

    LessonProgress:
      type: object
      required: [percent, completed, lastSeenAtSeconds]
      description: Per-lesson playback progress for the requesting user.
      properties:
        percent:
          type: number
          minimum: 0
          maximum: 100
          description: >-
            Completion percent. v1 always returns 0 — populated once the
            LessonProgress projector lands (E10-F01-S01).
        completed:
          type: boolean
          description: Whether the lesson is marked as completed.
        lastSeenAtSeconds:
          type: integer
          minimum: 0
          description: >-
            Last reported watched position in seconds. v1 always returns 0 —
            populated by the LessonProgress projector (E10-F01-S01).
      example:
        percent: 0
        completed: false
        lastSeenAtSeconds: 0

    LessonProgressDto:
      type: object
      required: [lessonId, positionSeconds, durationSeconds, percent, completed, lastSeenAt]
      description: Per-user progress state for a single lesson, as persisted after a merge.
      properties:
        lessonId:
          type: string
          description: Server-generated cuid identifying the lesson.
        positionSeconds:
          type: integer
          minimum: 0
          description: Last recorded watch position in seconds.
        durationSeconds:
          type: integer
          minimum: 1
          description: Lesson video duration in seconds.
        percent:
          type: number
          minimum: 0
          maximum: 100
          description: >-
            Computed as `positionSeconds / durationSeconds * 100`.
            Clamped to 100 when position >= duration.
        completed:
          type: boolean
          description: >-
            True once the user crosses the 90 % threshold; never flips back
            to false.
        lastSeenAt:
          type: string
          format: date-time
          description: ISO-8601 instant of the last accepted progress write.
        completedAt:
          type: string
          format: date-time
          description: >-
            Set the first time `completed` flips to true. Stable across
            subsequent writes. Absent when `completed` is false.
      example:
        lessonId: clxvles0000000000000000001
        positionSeconds: 1080
        durationSeconds: 1800
        percent: 60
        completed: false
        lastSeenAt: "2026-04-25T14:32:00Z"

    MaterialDto:
      type: object
      required: [id, kind, label, sizeBytes]
      description: A sidecar material attached to a lesson.
      properties:
        id:
          type: string
          description: Server-generated cuid identifying this material.
        kind:
          $ref: "#/components/schemas/MaterialKind"
        label:
          type: string
          minLength: 1
          description: >-
            Human-readable name derived from the original filename (extension
            stripped, ordinal prefix preserved if present).
        sizeBytes:
          type: integer
          minimum: 0
          description: File size in bytes.
      example:
        id: clxvmat0000000000000000001
        kind: doc
        label: 03 - Aggregates slides
        sizeBytes: 204800

    MaterialKind:
      type: string
      enum: [doc, note, image, slide]
      description: >-
        Coarse classification of a sidecar material. `doc` is `.pdf`,
        `note` is `.md` / `.txt`, `image` is `.png` / `.jpg`,
        `slide` is reserved for future use.
      example: doc

    NoteDto:
      type: object
      required: [id, lessonId, body, createdAt, updatedAt]
      description: A user-owned Markdown note attached to a single lesson.
      properties:
        id:
          type: string
          description: Server-generated cuid identifying this note.
        lessonId:
          type: string
          description: cuid of the lesson this note belongs to.
        body:
          type: string
          minLength: 1
          maxLength: 16384
          description: Plain Markdown stored verbatim. Server does not render.
        createdAt:
          type: string
          format: date-time
          description: ISO-8601 instant when the note was first created.
        updatedAt:
          type: string
          format: date-time
          description: ISO-8601 instant when the note body was last replaced.
      example:
        id: clxvnot0000000000000000001
        lessonId: clxvles0000000000000000001
        body: "## Aggregates\n\nKey insight: aggregates enforce invariants across their boundary."
        createdAt: "2026-04-25T15:00:00Z"
        updatedAt: "2026-04-25T15:05:00Z"

    PingResponse:
      type: object
      required: [id, role]
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
          description: User id (UUID v4) — Better Auth's internal identifier.
        role:
          type: string
          description: |
            Role string from Better Auth's `additionalFields.role`. Default
            `USER` for fresh sign-ups; admin tooling may assign others.
        displayName:
          type: string
          description: Optional friendly label set by the user in their profile.

    Problem:
      type: object
      description: RFC 9457 problem details
      required: [type, title, status]
      properties:
        type:
          type: ["string", "null"]
          format: uri
          default: about:blank
        title:
          type: ["string", "null"]
        status:
          type: integer
          format: int32
        detail:
          type: ["string", "null"]
        instance:
          type: ["string", "null"]
        code:
          type: ["string", "null"]

    RealtimeToken:
      type: object
      required: [token, expiresAt]
      properties:
        token:
          type: ["string", "null"]
          description: Short-lived HMAC-signed JWT for Centrifugo
        expiresAt:
          type: ["string", "null"]
          format: date-time
          description: ISO-8601 instant when the token expires

    RecordProgressRequest:
      type: object
      required: [lessonId, positionSeconds, durationSeconds, clientUpdatedAt]
      description: Payload for upserting the requester's progress on a lesson.
      properties:
        lessonId:
          type: string
          description: Server-generated cuid identifying the lesson.
        positionSeconds:
          type: integer
          minimum: 0
          description: >-
            Last reported watch position in seconds. Clamped server-side to
            `[0, durationSeconds]`.
        durationSeconds:
          type: integer
          minimum: 1
          description: >-
            Lesson video duration in seconds. Clients pass the player's
            `duration` from the `loadedmetadata` event; it must match the
            server-side value once E06-F02-S02 (ffprobe) lands. v1 trusts
            the client value.
        clientUpdatedAt:
          type: string
          format: date-time
          description: >-
            ISO-8601 timestamp the client recorded the position. Out-of-order
            writes (older than the current `lastSeenAt`) are silently accepted
            and the response echoes the unchanged state.
      example:
        lessonId: clxvles0000000000000000001
        positionSeconds: 1080
        durationSeconds: 1800
        clientUpdatedAt: "2026-04-25T14:32:00Z"

    BatchProgressRequest:
      type: object
      required: [items]
      additionalProperties: false
      properties:
        items:
          type: array
          minItems: 1
          maxItems: 200
          items:
            $ref: "#/components/schemas/RecordProgressRequest"
          description: |
            Up to 200 progress writes. Cap exists to bound server-side work
            and to keep request bodies under the 1 MiB JSON ceiling.

    BatchProgressResponse:
      type: object
      required: [results]
      additionalProperties: false
      properties:
        results:
          type: array
          items:
            $ref: "#/components/schemas/BatchProgressItemResult"
          description: |
            Same length and order as the input `items` array. Index N in the
            response maps 1:1 to index N in the request.

    BatchProgressItemResult:
      oneOf:
        - $ref: "#/components/schemas/BatchProgressItemAccepted"
        - $ref: "#/components/schemas/BatchProgressItemStale"
        - $ref: "#/components/schemas/BatchProgressItemForbidden"
      discriminator:
        propertyName: status
        mapping:
          accepted: "#/components/schemas/BatchProgressItemAccepted"
          stale: "#/components/schemas/BatchProgressItemStale"
          forbidden: "#/components/schemas/BatchProgressItemForbidden"

    BatchProgressItemAccepted:
      type: object
      required: [status, state]
      additionalProperties: false
      properties:
        status:
          type: string
          enum: [accepted]
          description: |
            The client write was applied (or absorbed by last-write-wins
            with no resulting state change). `state` reflects the post-merge
            server state.
        state:
          $ref: "#/components/schemas/LessonProgressDto"

    BatchProgressItemStale:
      type: object
      required: [status, state]
      additionalProperties: false
      properties:
        status:
          type: string
          enum: [stale]
          description: |
            The client's `clientUpdatedAt` was older than the server's
            `lastSeenAt` for this lesson. The write was absorbed but the
            server already had newer state — the client should overwrite
            its local cache from `state`.
        state:
          $ref: "#/components/schemas/LessonProgressDto"

    BatchProgressItemForbidden:
      type: object
      required: [status, lessonId]
      additionalProperties: false
      properties:
        status:
          type: string
          enum: [forbidden]
          description: |
            Actor has no READ grant covering this lesson, OR the lesson does
            not exist. The two cases are collapsed deliberately to avoid
            existence leakage (no-oracle rule).
        lessonId:
          type: string
          description: Echoes the input `lessonId` for client correlation.

    RegisterGrantRequest:
      type: object
      required: [userId, target, level]
      description: Payload for issuing a new access grant.
      properties:
        userId:
          type: string
          description: Better Auth user id of the grantee.
        target:
          $ref: "#/components/schemas/GrantTarget"
        level:
          $ref: "#/components/schemas/GrantLevel"
      example:
        userId: clxvusr0000000000000000001
        target:
          kind: library
          libraryId: clxvp1234567890abcdefghij
        level: READ

    RegisterLibraryRequest:
      type: object
      required: [name, rootPath]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
          description: Human-readable label.
        rootPath:
          type: string
          minLength: 1
          pattern: '^(?:/[^\0]*|[A-Za-z]:\\[^\0]*)$'
          description: >-
            Absolute filesystem path to the library root. Accepts POSIX paths
            starting with `/` or Windows drive paths starting with `[A-Za-z]:\`.
            Trailing slashes are allowed.
      example:
        name: Conference Recordings
        rootPath: /srv/courses/conference

    ScanDto:
      type: object
      required:
        - id
        - libraryId
        - status
        - startedAt
        - filesScanned
        - filesAdded
        - filesUpdated
        - coursesDiscovered
        - errors
      description: A scan record for a library, produced by the Scan aggregate.
      properties:
        id:
          type: string
          description: Server-generated cuid identifying this scan.
        libraryId:
          type: string
          description: cuid of the library that was scanned.
        status:
          $ref: "#/components/schemas/ScanStatus"
        startedAt:
          type: string
          format: date-time
          description: ISO-8601 instant when the scan was started.
        finishedAt:
          type: string
          format: date-time
          description: >-
            Set on terminal status (`succeeded` / `failed` / `cancelled`).
            Absent while `status: running`.
        filesScanned:
          type: integer
          minimum: 0
          description: Total number of filesystem entries inspected.
        filesAdded:
          type: integer
          minimum: 0
          description: Files that did not exist in the catalog before this scan.
        filesUpdated:
          type: integer
          minimum: 0
          description: Files whose metadata changed since the last scan.
        coursesDiscovered:
          type: integer
          minimum: 0
          description: Course roots detected during this scan.
        errors:
          type: array
          minItems: 0
          items:
            $ref: "#/components/schemas/ScanError"
          description: Non-fatal per-file errors encountered during the scan.
      example:
        id: clxvscn0000000000000000001
        libraryId: clxvp1234567890abcdefghij
        status: succeeded
        startedAt: "2026-04-25T10:30:00Z"
        finishedAt: "2026-04-25T10:31:42Z"
        filesScanned: 314
        filesAdded: 12
        filesUpdated: 3
        coursesDiscovered: 7
        errors:
          - path: "02 - Patterns/05 - Repositories.mp4"
            message: "File is not readable — permission denied."
            code: unreadable-file
          - path: "04 - Bounded Contexts/course.json"
            message: "JSON schema validation failed: missing required field 'title'."
            code: course-json-invalid

    ScanError:
      type: object
      required: [path, message]
      description: A non-fatal error encountered while processing a single file during a scan.
      properties:
        path:
          type: string
          description: >-
            Filesystem path relative to the library root,
            e.g. `01 - Intro to DDD/03 - Aggregates.mp4`.
        message:
          type: string
          description: Human-readable description of what went wrong.
        code:
          type: string
          description: >-
            Machine-readable error key
            (e.g. `course-json-invalid`, `unreadable-file`,
            `unsupported-extension`).
      example:
        path: "02 - Patterns/05 - Repositories.mp4"
        message: "File is not readable — permission denied."
        code: unreadable-file

    ScanStatus:
      type: string
      enum: [running, succeeded, failed, cancelled]
      description: >-
        Scan lifecycle. `cancelled` is reserved for v2 admin-cancel; v1 scans
        only ever transition `running → {succeeded, failed}`.

    StreamUrlDto:
      type: object
      required: [url, token, expiresAt]
      description: Short-lived signed URL for streaming a lesson video.
      properties:
        url:
          type: string
          format: uri-reference
          description: >-
            URL the player should request. Same-origin relative path (e.g.
            `/api/v1/stream/lessons/<id>?token=…`) — the browser resolves
            it against the SPA's API origin so the backend doesn't need to
            know its public hostname. Carries the signed token as the
            `token` query parameter so existing video-element
            implementations work without an Authorization header.
        token:
          type: string
          minLength: 1
          description: >-
            Opaque signed token. Format is internal to the backend (currently a
            JWT-like compact form `header.payload.signature`); clients must
            round-trip it untouched.
        expiresAt:
          type: string
          format: date-time
          description: >-
            ISO-8601 timestamp at which the token + URL stop being accepted.
            Clients should request a fresh URL before this moment.
      example:
        url: "/api/v1/stream/lessons/clxvles0000000000000000001?token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VySWQiOiJjbHh2dXNyMDAwMDAwMDAwMDAwMDAwMDAxIiwibGVzc29uSWQiOiJjbHh2bGVzMDAwMDAwMDAwMDAwMDAwMDAxIiwiZXhwIjoxNzQ1NTc0NDAwfQ.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c"
        token: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VySWQiOiJjbHh2dXNyMDAwMDAwMDAwMDAwMDAwMDAxIiwibGVzc29uSWQiOiJjbHh2bGVzMDAwMDAwMDAwMDAwMDAwMDAxIiwiZXhwIjoxNzQ1NTc0NDAwfQ.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c"
        expiresAt: "2026-04-25T10:15:00Z"

    MaterialDownloadUrlDto:
      type: object
      required: [url, token, expiresAt]
      description: >-
        Short-lived signed URL for downloading a lesson material (PDF, MD,
        image). Same shape as `StreamUrlDto` — the only difference is the
        token's internal scope claim. Issued by `issueMaterialDownloadUrl`
        and consumed by the browser via a transient `<a href download>`.
      properties:
        url:
          type: string
          format: uri-reference
          description: >-
            URL the browser should fetch. Same-origin relative path (e.g.
            `/api/v1/stream/materials/<id>?token=…`). Carries the signed
            token as the `token` query parameter so a direct
            `<a href download>` works without an Authorization header.
        token:
          type: string
          minLength: 1
          description: >-
            Opaque signed token. Internal format is the same compact
            `header.payload.signature` shape as the video stream token, but
            the payload's scope claim differs so a video token can never be
            re-used to fetch a material (and vice versa). Round-trip
            untouched.
        expiresAt:
          type: string
          format: date-time
          description: >-
            ISO-8601 timestamp at which the token + URL stop being accepted.
            Default TTL is 5 minutes — clicking a download link should
            resolve immediately, so a long TTL is unnecessary.
      example:
        url: "/api/v1/stream/materials/clxvmat0000000000000000001?token=eyJhbGciOiJIUzI1NiIsInR5cCI6IlNUSyIsInYiOjF9.eyJzdWIiOiJjbHh2dXNyMDAwMDAwMDAwMDAwMDAwMDAxIiwibWlkIjoiY2x4dm1hdDAwMDAwMDAwMDAwMDAwMDAwMSIsInNjcCI6Im1hdGVyaWFsIiwiZXhwIjoxNzQ1NTc0NDAwLCJpYXQiOjE3NDU1NzQxMDB9.k9sQfxCq8wL2pT4N3LKzr8B5jM0hVzG4Yf9X2N1tQp8"
        token: "eyJhbGciOiJIUzI1NiIsInR5cCI6IlNUSyIsInYiOjF9.eyJzdWIiOiJjbHh2dXNyMDAwMDAwMDAwMDAwMDAwMDAxIiwibWlkIjoiY2x4dm1hdDAwMDAwMDAwMDAwMDAwMDAwMSIsInNjcCI6Im1hdGVyaWFsIiwiZXhwIjoxNzQ1NTc0NDAwLCJpYXQiOjE3NDU1NzQxMDB9.k9sQfxCq8wL2pT4N3LKzr8B5jM0hVzG4Yf9X2N1tQp8"
        expiresAt: "2026-04-25T10:05:00Z"

    SubtitleDto:
      type: object
      required: [id, language, label]
      description: A subtitle track available for a lesson.
      properties:
        id:
          type: string
          description: Server-generated cuid identifying this subtitle track.
        language:
          type: string
          pattern: "^[a-z]{2,3}$|^und$"
          description: >-
            BCP-47-ish language code parsed from the filename suffix
            (`Lesson.en.srt` → `en`). `und` when no suffix is present.
        label:
          type: string
          minLength: 1
          description: Human-readable label for the subtitle track (e.g. "English").
      example:
        id: clxvsub0000000000000000001
        language: en
        label: English

    SectionDto:
      type: object
      required: [id, position, title]
      description: A section within a course.
      properties:
        id:
          type: string
          description: Server-generated cuid identifying this section.
        position:
          type: integer
          minimum: 1
          description: 1-based position within the course. Sections are returned sorted by position.
        title:
          type: string
          minLength: 1
          maxLength: 200
      example:
        id: clxvsec0000000000000000001
        position: 1
        title: Introduction

    UpdateBookmarkRequest:
      type: object
      description: |
        Payload for updating a bookmark. All fields are optional, but at least
        one of `positionSeconds` or `label` must be present — the server returns
        400 on empty patches.
      properties:
        positionSeconds:
          type: integer
          minimum: 0
          description: New playback position in seconds.
        label:
          type: ["string", "null"]
          maxLength: 200
          description: >-
            Pass an explicit `null` to clear the label; omit to leave it
            untouched.
      example:
        positionSeconds: 210
        label: Aggregate boundaries explained

    UpdateCourseRequest:
      type: object
      description: |
        Payload for updating course metadata. All fields are optional, but at
        least one of `title`, `description`, `slug`, `instructorIds`,
        `studioIds`, `tagIds`, `posterUrl`, `level`, `language`,
        `releaseDate`, `sourceUpdatedAt`, `ratingAverage`, `ratingCount`, or
        `externalIds` must be present (server-side validation rule — OpenAPI
        does not have a native "at-least-one" constraint).

        **Set-replace semantics for relation arrays:** `null` means "leave the
        existing set alone"; `[]` (empty array) means "remove all links";
        a non-empty array replaces the full set with exactly the listed ids.
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 200
        description:
          type: string
          maxLength: 4000
        slug:
          $ref: "#/components/schemas/CourseSlug"
        instructorIds:
          oneOf:
            - type: array
              items:
                type: string
            - type: "null"
          description: >-
            Set-replace list of instructor cuids. `null` = leave unchanged;
            `[]` = remove all instructor links; `[id1, id2]` = replace with
            exactly these instructors.
        studioIds:
          oneOf:
            - type: array
              items:
                type: string
            - type: "null"
          description: >-
            Set-replace list of studio cuids. Same null/empty/array semantics
            as `instructorIds`.
        tagIds:
          oneOf:
            - type: array
              items:
                type: string
            - type: "null"
          description: >-
            Set-replace list of tag cuids. Same null/empty/array semantics as
            `instructorIds`.
        level:
          oneOf:
            - $ref: "#/components/schemas/CourseLevel"
            - type: "null"
          description: Difficulty level. Pass `null` to clear.
        language:
          oneOf:
            - type: string
            - type: "null"
          description: BCP-47 language tag (e.g. `en`, `en-US`). Pass `null` to clear.
        releaseDate:
          oneOf:
            - type: string
              format: date
            - type: "null"
          description: ISO-8601 date of original release. Pass `null` to clear.
        posterUrl:
          oneOf:
            - type: string
              format: uri
            - type: "null"
          description: Course poster image URL. Pass `null` to clear.
        ratingAverage:
          oneOf:
            - type: number
              multipleOf: 0.01
              minimum: 0
              maximum: 5
            - type: "null"
          description: Average rating 0–5. Pass `null` to clear.
        ratingCount:
          oneOf:
            - type: integer
              minimum: 0
            - type: "null"
          description: Number of ratings. Pass `null` to clear.
        externalIds:
          oneOf:
            - type: array
              items:
                $ref: "#/components/schemas/ExternalIdRef"
            - type: "null"
          description: >-
            Replaces all external id refs for this course. `null` = leave
            unchanged; `[]` = remove all; an array = full replacement.
        sourceUpdatedAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
          description: Upstream source last-updated timestamp. Pass `null` to clear.
      example:
        title: Pragmatic Clean Architecture (2nd ed.)
        slug: pragmatic-clean-architecture-2nd-ed
        level: intermediate
        language: en
        instructorIds:
          - clxvins0000000000000000001
        tagIds:
          - clxvtag0000000000000000001

    UpsertNoteRequest:
      type: object
      required: [lessonId, body]
      description: Payload for creating or replacing the requester's note on a lesson.
      properties:
        lessonId:
          type: string
          description: Server-generated cuid identifying the lesson.
        body:
          type: string
          minLength: 1
          maxLength: 16384
          description: >-
            Plain Markdown. Trimmed server-side; an empty post-trim body is
            rejected as 400.
      example:
        lessonId: clxvles0000000000000000001
        body: "## Aggregates\n\nKey insight: aggregates enforce invariants across their boundary."
