# E31-F02-S01 — Remove the SSO promise

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 0.5 day

**Epic:** E31 — Honest v1.1
**Feature:** F02 — Withdraw the promises

## Dependencies

- [E14-F02-S01](./E14-F02-S01.md) — Stage A · Sign in / sign up / forgot password

## Spec / Design references

- `docs/user-guide.md` — Known limits: “SSO”

## Goal

For a personal instance, email and password is the whole requirement. The honest move is to withdraw the promise, not to implement it.

## Acceptance

- Dead `ssoProviders` handling removed from the web client, including the fetch nothing reads
- The provider button row stays in `@app/ui` — it is a component, not a claim
- Every doc line claiming SSO is coming is removed, including the Known-limits row

## Spec diff

none — leave `ssoProviders` in the instance-config contract unless nothing else reads it; check before removing

## Codegen impact

conditional

## Design impact

no

## Tests

Existing sign-in specs stay green; no new tests for removed code.

## Sub-steps

- [x] Trace every `ssoProviders` reader
- [x] Remove the dead path
- [x] Remove the doc claims

## Notes

- Should the instance ever grow past one owner, this is a fresh story with a fresh decision — not a resurrected half-implementation.
- The card's premise was one revision stale: PR #205 had already wired
  `AppSsoBlock` into `sign-in.vue` and `sign-up.vue` through a
  `useSsoProviders` composable, so this was a live-but-always-empty surface
  rather than an unread fetch. The removal direction is unchanged.
- **The contract keeps `ssoProviders`.** The mobile client still reads it
  (`apps/mobile/lib/features/auth/…/sign_in_screen.dart`,
  `sign_up_screen.dart`), so `packages/specs` is untouched and there is no
  codegen commit. `useInstanceConfig`'s `DEFAULT_CONFIG` keeps the field only
  to satisfy `InstanceConfigDto`; a comment says so.
- `AppSsoBlock` stays exported from `@app/ui` (and `@app/ui_flutter`) with its
  story and spec, per the card.
- tuxedo task 11 ("подключить AppSsoBlock на sign-in/sign-up") contradicted
  this card and has been closed with a pointer here.
- Completed: 2026-08-30
- Result: PR_LINK_PLACEHOLDER

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done · ❌ Cancelled

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
