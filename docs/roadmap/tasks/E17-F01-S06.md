# E17-F01-S06 — AppDialog, AppBottomSheet

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E17 — Mobile widget catalog
**Feature:** F01 — Mobile primitives

## Dependencies

- [E16-F01-S01](./E16-F01-S01.md) — widgetbook/main.dart + canary use case
- [E17-F01-S02](./E17-F01-S02.md) — AppButton, AppIconButton (Flutter)

## Spec / Design references

- docs/design/cs-foundation/
- docs/design/shared/tokens.json

## Goal

`AppDialog` mirrors the web `AppDialog`. `AppBottomSheet` is a mobile-only pattern (there is no web `AppBottomSheet`).

## Acceptance

- Widgetbook use case per state matches the bundle
- Uses Tokens.\* (no hard-coded values)

## Spec diff

none

## Codegen impact

no

## Design impact

yes

## Tests

_To be defined when picked up._

## Sub-steps

- [x] _Decompose when picking this up._

## Notes

- Completed: 2026-07-17
- Result: [PR #149](https://github.com/kkucherenkov/course_shelf/pull/149) — branch `feat/e17-f01-wave-2` (E17-F01 wave 2, built by an isolated worktree subagent reusing the E17-F01-S02 recipe)
- `AppDialog` + `AppBottomSheet` in `packages/ui_flutter`, sharing an internal `OverlayActionRow` footer. `AppDialogSize.maxWidth` (480/640) is a documented locally-owned scale mirroring the web `max-width` (no spacing token for modal width). `AppBottomSheet` has no web twin — the mobile-native analogue of `AppDialog`.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
