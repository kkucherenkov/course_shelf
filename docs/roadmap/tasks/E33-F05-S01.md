# E33-F05-S01 — `/admin` becomes a hub

**Status:** ⬜ Not started
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E33 — Ownership and taxonomy
**Feature:** F05 — Admin navigation

## Dependencies

- None.

## Spec / Design references

- [ADR-0013](../../adr/0013-ownership-taxonomy-and-ratings.md) — the 2.0 scope
  this epic implements; this card carries none of its five decisions

## Goal

Make `/admin` the way into the admin area. Today it is a stats dashboard that
links nowhere.

## Acceptance

- A grid on `/admin` linking the six admin pages — dashboard, libraries,
  users, permissions, identify tasks, backups — each with a one-line
  description of what it is for
- The existing stats and the scans table stay on the page
- Strings via `t()` in `en` and `ru`; `pnpm check:i18n` green

## Spec diff

none

## Codegen impact

no

## Design impact

no

## Tests

A page spec asserting every admin route is linked — the assertion that catches
a page orphaned by a later change.

## Sub-steps

- [ ] The hub grid + spec
- [ ] Locale keys in both languages

## Notes

- Opened from `tuxedo` 228 (first half).
- The hub has to land before [E33-F05-S02](./E33-F05-S02.md) removes the admin
  block from the main navigation, or the five non-dashboard pages have no
  reachable entry point at all.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done · ❌ Cancelled

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
