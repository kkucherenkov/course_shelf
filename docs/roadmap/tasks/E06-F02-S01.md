# E06-F02-S01 — Scan aggregate, FsAdapter, incremental scan

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 3 days

**Epic:** E06 — Catalog bounded context
**Feature:** F02 — Scanner

## Dependencies

- [E06-F01-S01](./E06-F01-S01.md) — Library aggregate + repository port

## Spec / Design references

- PRD.md FR-LIB-02
- PRD.md FR-LIB-03
- PRD.md FR-LIB-06
- DESIGN.md §10.1

## Goal

A scan walks the library tree, builds Course → Section → Lesson, and only touches files whose `(mtime, size)` signature changed.

## Acceptance

- First scan creates aggregates; second scan with no changes is a no-op
- `course.json` override file (if present) supersedes folder-derived metadata
- Per-file errors are recorded on the `Scan` aggregate

## Spec diff

yes — `POST /libraries/{id}/scans`, `GET /libraries/{id}/scans/latest`

## Codegen impact

yes

## Design impact

no

## Tests

domain unit, integration with a fixture library tree on tmpfs

## Sub-steps

- [x] spec edits + regen (commits `4830409`, `11243f3`)
- [x] `Scan` aggregate with start → progress → complete/fail lifecycle (events recorded as state mutations on the aggregate; in-process consumer in v1, no event bus required for E06-F02-S01)
- [x] `FsAdapter` interface (port) + `NodeFsAdapter` (recursive `opendir`, `readUtf8`, hidden-file skip)
- [x] `course.json` v1 schema + hand-rolled parser (no zod — keeps domain dep-free); malformed files surface as `ScanError`, not 5xx
- [x] folder-name parser handles both `NN -` and `NN.` prefixes plus bare titles; lesson extension whitelist
- [x] `RunScanCommand` + handler — fire-and-forget walk after the 202 response
- [x] mtime+size signature stored on `DiscoveredFile` (per-scan in v1; will lift onto `Lesson` in E06-F03-S02)

## Notes

- Completed: 2026-04-26
- Result: T-2026-04-26-015 — three commits on `feat/library-scan`: `4830409` (spec), `11243f3` (codegen), `2ee4cf7` (impl). Backend tests 191/191; lint + typecheck clean.
- Course/Section/Lesson aggregate creation is **not** part of this story; scan only discovers and records counters + `ScannedCourse[]` value objects on the aggregate. Persisted aggregate creation lands in E06-F03-S01 / E06-F03-S02.
- v1 walk runs in-process (`Promise.resolve().then(...)`); v2 will move it to a background worker once scope demands.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
