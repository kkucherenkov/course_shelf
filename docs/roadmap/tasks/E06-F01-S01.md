# E06-F01-S01 — Library aggregate + repository port

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 2 days

**Epic:** E06 — Catalog bounded context
**Feature:** F01 — Library aggregate & registration

## Dependencies

- [E04-F02-S03](./E04-F02-S03.md) — Mount express-openapi-validator for /api/v1/\*

## Spec / Design references

- PRD.md FR-LIB-01
- DESIGN.md §4
- DESIGN.md §6.2

## Goal

Persisting libraries with their invariants (name non-empty, rootPath absolute and unique).

## Acceptance

- Domain test: cannot construct with empty name or relative path
- Roundtrip test: save → load returns equal aggregate

## Spec diff

yes — `POST /libraries`, `GET /libraries`, `GET /libraries/{id}`

## Codegen impact

yes — regenerate api-client-{ts,dart} and DTOs

## Design impact

no

## Tests

_To be defined when picked up._

## Sub-steps

- [x] edit `packages/specs/openapi/openapi.yaml` (paths for libraries + component schema)
- [x] `pnpm spec:validate && pnpm spec:bundle && pnpm spec:codegen` (TS only — Dart codegen blocked by pre-existing openapi-generator-cli env issue)
- [x] `Library` aggregate in `apps/backend/src/modules/catalog/domain/library/` (path corrected from `contexts/` to match the existing `modules/` pattern + boundaries config)
- [x] `LibraryRepository` interface in domain
- [x] `prisma-library.repository.ts` + mapper in `apps/backend/src/modules/catalog/infra/`
- [x] commands: `RegisterLibraryCommand` + handler
- [x] queries: `ListLibrariesQuery`, `GetLibraryQuery` + handlers
- [x] `CatalogController` mapping DTO ↔ command/query, `CatalogModule` registered in `app.module.ts`
- [x] tests (5 spec files: aggregate, three handlers, repository roundtrip with mocked Prisma)

## Notes

- Completed: 2026-04-26
- Result: T-2026-04-26-010 — three commits on `feat/library-aggregate`: `a710c5d` (spec), `be9ac44` (TS codegen), `0ef1607` (impl).
- Prisma migration was authored as SQL (`apps/backend/prisma/migrations/20260426000000_add_library/migration.sql`) instead of via `prisma migrate dev --create-only` because Docker was not running at edit time. The SQL is byte-identical to what Prisma would have generated.
- ID generation uses `nanoid` (already a dep) at the handler layer; the Prisma `@default(cuid())` stays as a fallback if the adapter is ever called without an id.
- Workspace `tsc` regression in `packages/api-client-ts/src/generated/core/serverSentEvents.gen.ts` (TS2379, TS2375) introduced by the codegen in `be9ac44` is **not** caused by E06 code — it's an `exactOptionalPropertyTypes` + `@hey-api/openapi-ts` mismatch. Tracked as T-2026-04-26-011 (codegen TS strictness) and not blocking E06 acceptance: backend-only `lint` is clean and `test` is 70/70 passing.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
