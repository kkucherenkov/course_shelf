## T-2026-09-16-shell-a11y — avatar button missing accessible name, h3-const empty/error/no-permission states

- Created: 2026-09-16
- Owner: claude
- Goal: audit round 4 (23/40) fixes, wave 3 — #630 `AppNavigationShell`
  desktop avatar-trigger button has no accessible name (98 axe
  `button-name` violations across the matrix); #631 `AppEmptyState`/
  `AppErrorState`/`AppNoPermission` hardcode `h3`, producing `h1 → h3`
  heading-order jumps now that every page has an `h1` (22 axe
  `heading-order` violations).
- Sub-steps:
  - [x] #630 — made `userMenuLabel` required (dropped the English
        default), wired `:aria-label="userMenuLabel"` onto the desktop
        avatar-trigger button (mobile trigger already had it at line 439);
        `layouts/default.vue` already passed the label (from #623's
        `sidebarLabel`/`rightRailLabel` wiring) — no change needed there.
        New spec tests (default + override), story arg added.
  - [x] #631 — added `headingLevel` prop (`2 | 3 | 4 | 5 | 6`, default `2`)
        to `AppEmptyState`/`AppErrorState`/`AppNoPermission`; title renders
        via `<component :is="`h${headingLevel}`">` instead of a hardcoded
        `h3`. Checked all three for #624's slot-alias drift while in
        there — all three already consistent (`action`/`actions` fallback
        alias present on all three, confirmed by reading the source, not
        assumed).
  - [x] axe baseline, live stand (`:8090`, `audit4`, `417c5cbb`,
        `audit-admin`, 1440px), one clean login (had to
        `docker restart csh-audit-backend-1` once after tripping the
        5-attempts/15min limiter during earlier throwaway debug logins):
        `/` → 0 `button-name`/`heading-order` nodes; `/search` → 1
        `heading-order` node (empty-state title, matches the ticket).
        **Honest finding, not the ticket's number:** `button-name` measured
        **0**, not 98, for this persona/route/viewport at steady state —
        traced by reading `AppAvatar.vue`: the avatar-trigger's only
        content (initials text, e.g. "AA", plus a role badge with its own
        `aria-label`) already supplies axe a non-empty accessible name via
        subtree text, for every seeded persona
        (`audit-admin`/`audit-learner`/`audit-empty` all have two-word
        `name` values → non-empty initials). Confirmed the
        transient degraded-name window `layouts/default.vue`'s own #602
        comment describes (`shellUser` empty before profile hydrates)
        does NOT reach the DOM either — `auth.global.ts`'s middleware
        blocks navigation until `getSession()` resolves, verified by
        delaying that request 3s and polling every 200ms: the shell never
        mounts with a null user. Fixed anyway — the button's accessible
        name should describe its function ("open user menu"), not
        incidentally borrow the user's initials, and #630's own diagnosis
        (English-default prop hid the gap through 4 audits) is the right
        root-cause regardless of which crawl state tripped axe. Flagging
        the count discrepancy rather than the count in the ticket.
        Isolated-stack (own worktree, fix applied, fresh sign-up admin)
        re-run: `/` and `/search` both 0/0; directly confirmed
        `aria-label="User menu"` in the live DOM and `/search`'s empty
        state rendering `h2` (was `h3`, 0 `h3` on the page).
  - [x] `pnpm --filter @app/ui test` (4 changed spec files): red before
        fix (8 failing — reverted the 4 `.vue` files via a throwaway
        patch, reran, restored), green after (100/100).
  - [x] gates: lint, stylelint, format, `turbo run lint test typecheck`
- Status: ready for PR
- Blockers: —
- PR: https://github.com/kkucherenkov/course_shelf/pull/636 (merged)
