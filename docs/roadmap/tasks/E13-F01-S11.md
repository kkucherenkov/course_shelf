# E13-F01-S11 — AppAvatar (xs/sm/default/lg/xl, with role-admin and role-guest badges)

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E13 — Web component catalog
**Feature:** F01 — Primitive families (from cs-foundations)

## Dependencies

- [E12-F01-S01](./E12-F01-S01.md) — Configure Storybook for Vue 3 + Nuxt UI v4 + tokens

## Spec / Design references

- docs/design/cs-foundations/, docs/design/shared/tokens.css

## Goal

AppAvatar (xs/sm/default/lg/xl, with role-admin and role-guest badges) matching the bundle.

## Acceptance

- States in stories match the bundle's state matrix
- Uses design tokens only (no hard-coded values)

## Spec diff

none

## Codegen impact

no

## Design impact

yes

## Tests

_To be defined when picked up._

## Sub-steps

- [x] AppAvatar with `image`/`initials`/`name` (computed initials fallback), 5 sizes (xs/sm/md/lg/xl), `role` badge for `admin` + `guest`

## Notes

- Completed: 2026-04-27
- Result: T-2026-04-27-041 — commit `cf8830b` on `feat/primitives-batch-b`. UI tests +13.
- Initials computation: explicit `initials` prop wins; otherwise derived from `name` (first letter of each word, max 2, uppercased).
- Role badge: rendered as a child element with `role="img"` + `aria-label` (`"Administrator"` / `"Guest"`) and a single-letter glyph (`A` / `G`) inside. Background is `--brand-accent` for admin and `--status-info-fg` for guest.
- Outer ring of the role badge uses `--surface-page` (the bundle's `--bg`) so it visually punches out of the avatar background.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
