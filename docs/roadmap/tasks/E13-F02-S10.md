# E13-F02-S10 — SsoBlock — third-party / SSO provider button row

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E13 — Web component catalog
**Feature:** F02 — Domain components (from cs-components)

## Dependencies

- [E13-F01-S02](./E13-F01-S02.md) — AppButton + AppIconButton (matches .btn classes)
- [E13-F01-S01](./E13-F01-S01.md) — IconCS — port the bundle's 61-icon family to Vue

## Spec / Design references

- docs/design/shared/auth.jsx §SsoBlock
- docs/design/cs-web-auth/

## Goal

Visual home for OAuth/SSO providers — rendered when the admin has configured ≥1 provider.

## Acceptance

- Props: `providers: Array<{ id: 'google' | 'github' | 'sso'; label: string; iconName: IconName }>`
- Renders one `AppButton` (secondary) per provider with the configured icon + label
- When `providers` is empty, the component renders nothing (auth pages also hide the surrounding `or` divider)
- States in stories: Default (3 providers as in bundle), GoogleOnly, GitHubOnly, SSOOnly, Empty

## Spec diff

none

## Codegen impact

no

## Design impact

yes

## Tests

_To be defined when picked up._

## Sub-steps

- [x] `AppSsoBlock.vue` — vertical AppButton stack, one per provider, empty list renders nothing (commit `203f461`)
- [x] 6 unit tests + 5 stories (Default + 3 single-provider variants + Empty)
- [x] role=group + aria-label on the wrapper

## Notes

- Completed: 2026-04-27
- Result: T-2026-04-27-047 — single feature commit `203f461` on `feat/sso-block`. UI tests 713/713 (+6); lint + typecheck clean.
- Provider list is configurable per the card — any `id`/`label`/`iconName` triple works, not just google/github/sso. The bundle's three-button default lives in the Storybook `Default` story, not the component.
- Empty array short-circuits via `v-if` so the auth page can skip the surrounding "or" divider via the same `providers.length` check.

**v1 scope.** PRD lists OAuth/OIDC/SAML at v2+. v1 ships this component with an empty `providers` list by default — the visual is in place for when v2 lands. When the admin enables Better Auth's `genericOAuth` or per-provider plugins (post-v1), the auth page populates `providers` from `GET /admin/instance` and the SsoBlock renders. Until then, the divider and SsoBlock are hidden on the auth pages.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
