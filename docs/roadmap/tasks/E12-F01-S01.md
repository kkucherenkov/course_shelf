# E12-F01-S01 — Configure Storybook for Vue 3 + Nuxt UI v4 + tokens

**Status:** ⬜ Not started
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E12 — Storybook for web
**Feature:** F01 — Storybook bootstrap

## Dependencies

- [E11-F01-S01](./E11-F01-S01.md) — Bootstrap Nuxt SPA with Nuxt UI v4, Tailwind 4, and tokens

## Spec / Design references

- AGENTS.md §7.6
- DESIGN.md §2.9

## Goal

Stories render with the same theme + tokens as the real app.

## Acceptance

- A canary `Button.stories.ts` renders Default, Hover, Disabled
- `pnpm dev:storybook` works
- `pnpm build:storybook` produces a static site

## Spec diff

none

## Codegen impact

no

## Design impact

yes

## Tests

_To be defined when picked up._

## Sub-steps

- [ ] install Storybook 8 + Vue 3 framework
- [ ] `.storybook/main.ts`, `preview.ts` (Tailwind/Nuxt UI v4 decorator, `tokens.css` import)
- [ ] add `pnpm dev:storybook`, `pnpm build:storybook`, `pnpm storybook:test`

## Notes

_Empty initially; agent fills in during work._

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
