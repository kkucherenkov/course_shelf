/**
 * WHY this file exists:
 * A Query is a plain data object representing a read-side request.
 * ListLibraries has no parameters — it always returns all libraries.
 */
export class ListLibrariesQuery {}
