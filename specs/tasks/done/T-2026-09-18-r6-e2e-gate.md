## T-2026-09-18-r6-e2e-gate — make the smoke workflow report on docs-only PRs

- Created: 2026-09-18
- Owner: claude
- Spec: tuxedo #197 — make "Playwright smoke (web + backend health)" a required branch-protection context
- Goal: `.github/workflows/e2e.yml`'s `smoke` job must report on every PR, including
  docs-only ones, so it can be turned into a required status check without
  making those PRs permanently unmergeable.
- Acceptance:
  - A PR touching only `.md` / `docs/**` / `.claude/**` / `specs/tasks/**` /
    `LICENSE` gets a green `smoke` job quickly, not a skip.
  - A PR touching code still runs the full stack + Playwright + contract test.
  - `merge_group` events still get a reporting `smoke` job.
  - Branch-protection toggle itself stays with the coordinator, done outside
    this task.
- Spec diff: none
- Codegen impact: no
- Design impact: none
- Tests: none (CI workflow change; proof is this PR's own docs-only diff
  reporting green `smoke`)
- Sub-steps:
  - [x] Read `ci.yml`'s `changes` + `flutter` job pair (the precedent named in
        the brief) and `e2e.yml`'s current `paths-ignore`.
  - [x] Drop `paths-ignore` from `e2e.yml`'s `pull_request` trigger.
  - [x] Add a `changes` job (`dorny/paths-filter`) computing a `code` output —
        true when a changed file falls outside the docs/tasks ignore list.
  - [x] Gate `smoke`'s expensive steps behind
        `needs.changes.outputs.code == 'true'`; job itself always runs.
  - [x] Confirm `merge_group` works with `dorny/paths-filter` with no explicit
        `base`/`ref` (checked the action's README: auto-detected from the
        event).
  - [x] `pnpm format` (this file is prettier-formatted markdown).
  - [x] Open PR, watch `smoke` report green on this doc/workflow-only diff.
- Status: blocked
- Blockers: waiting on coordinator to merge PR #748, then flip the
  branch-protection required contexts (out of scope for this lane).
- Completed: 2026-09-18
- Result: https://github.com/kkucherenkov/course_shelf/pull/748
