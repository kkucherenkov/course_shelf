# E02-F02-S04 — Aggregate `gen:all` and contract-test scaffold

**Status:** ⬜ Not started
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E02 — API contracts as the spine
**Feature:** F02 — Codegen pipeline

## Dependencies

- [E02-F02-S01](./E02-F02-S01.md) — Generate `@courseshelf/api-types` from the spec
- [E02-F02-S02](./E02-F02-S02.md) — Generate `@courseshelf/api-dtos-nest` with class-validator
- [E02-F02-S03](./E02-F02-S03.md) — Generate Dart Dio client

## Spec / Design references

- AGENTS.md §8

## Goal

One command regenerates everything; a stub contract test walks the bundled spec.

## Acceptance

- `pnpm gen:all` runs lint → bundle → all three generators in order
- `pnpm test:contract` exists; for v1 it asserts every operationId has a matching test file

## Spec diff

none

## Codegen impact

yes

## Design impact

no

## Tests

_To be defined when picked up._

## Sub-steps

- [ ] add `gen:all` script chaining sub-tasks
- [ ] add `apps/api/test/contract/` with a runner that loads `dist/openapi.json`
- [ ] CI gate: error if `paths.*` count > test files count

## Notes

_Empty initially; agent fills in during work._

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
