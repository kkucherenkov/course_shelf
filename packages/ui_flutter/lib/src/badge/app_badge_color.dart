/// The colour axis of [AppBadge], sharing the same union as the web
/// `AppBadge`'s `color` prop (`primary | neutral | success | warning | error
/// | info`).
enum AppBadgeColor { primary, neutral, success, warning, error, info }
