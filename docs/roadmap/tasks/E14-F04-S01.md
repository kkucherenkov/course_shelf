# E14-F04-S01 — Stage B · Admin section (precede with cs-web-admin)

**Status:** ✅ Done
**Stage:** B
**Owner:** claude
**Estimated duration:** 5 days

**Epic:** E14 — Web pages
**Feature:** F04 — Admin

## Dependencies

- [E13-F02-S07](./E13-F02-S07.md) — ScanProgress — live scan indicator with stats and current file
- [E13-F02-S08](./E13-F02-S08.md) — NavigationShell — top bar + sidebar (composes AppRow + IconCS)
- [E07-F01-S01](./E07-F01-S01.md) — AccessGrant aggregate + admin endpoints
- [E21-F01-S01](./E21-F01-S01.md) — GET /admin/dashboard aggregator

## Spec / Design references

- DESIGN_BRIEF §6.7
- docs/design/cs-web-admin/ (to be produced)

## Goal

All admin surfaces in one PR family. ScanProgress (E13-F02-S07) plugs into the libraries detail page.

## Acceptance

- Dashboard: counts + last-scan summary + recent scans table
- Libraries: list + detail (with `ScanProgress`) + add CTA
- Users: list with role chips that admins can change inline
- Permissions: per-user grants with library + per-course overrides
- Responsive: desktop-first; works at 360 (tables become row lists)

## Spec diff

yes — admin endpoints (already drafted in E07/E10) + `GET /admin/dashboard` aggregator

## Codegen impact

yes

## Design impact

yes — produce bundle first

## Tests

_To be defined when picked up._

## Sub-steps

- [x] **Design pre-step**: `cs-web-admin` bundle (`docs/design/cs-web-admin/`)
- [x] spec edits + regen — `listAdminScans`, `listAdminLibraries` (with `libraryId` filter), `listAdminUsers`, `updateAdminUser`, `getAdminUser`
- [x] handlers + projections — extended `DashboardPort` with five methods (`listRecentScans`, `listAllLibrariesWithCounts`, `listUsers`, `updateUser`, `findUserById`); reused existing `AccessGrant` for permissions
- [x] pages composition — Dashboard, Libraries (list + detail with live `AppScanProgress`), Users (with `AdminRoleChip`), Permissions (per-library + course overrides)
- [x] dependency `E13-F02-S07` (`AppScanProgress`) shipped in PR #163 along the way

## Notes

- Completed: 2026-04-29
- Result: shipped in **four chunked PRs + one rescue PR + one prep PR**:
  - PR #163 — `feat(ui): AppScanProgress` (missing dep — E13-F02-S07)
  - PR #164 — chunk 1: dashboard (counts + last-scan + recent-scans table)
  - PR #165 — chunk 2: libraries list + detail with live polling
  - PR #166 — rescue: restore spec source dropped by Forgejo's squash of #165
  - PR #167 — chunk 3: users list with inline `AdminRoleChip`
  - merge `d2ddc11` on `main` — chunk 4: permissions page (per-user library + course grants). Origin was offline at merge time, so this one is a local merge rather than a Forgejo PR.
- Bookkeeping log: T-2026-04-29-061 in `specs/tasks/done.md`.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
