# E13-F02-S01 — CourseCard — Poster, Wide, Compact variants

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 3 days

**Epic:** E13 — Web component catalog
**Feature:** F02 — Domain components (from cs-components)

## Dependencies

- [E13-F01-S01](./E13-F01-S01.md) — IconCS — port the bundle's 61-icon family to Vue
- [E13-F01-S09](./E13-F01-S09.md) — AppProgressLinear, AppProgressCircle, AppSpinner, AppSkeleton

## Spec / Design references

- docs/design/cs-components/components.jsx §CourseCard
- DESIGN_BRIEF §5.1

## Goal

Three layouts (poster, wide, compact) covered by one Vue component family with shared logic.

## Acceptance

- Props per variant exactly match the bundle (`course`, `state`, `loading`; `Wide` adds `resumeAt`)
- States rendered in stories: Default, Hover, Focus, InProgress, Completed, NotStarted, Locked, Loading (≥ 8 each)
- Cover treatment uses the bundle's `COVER` map (teal/amber/indigo/warm/coral/neutral) + initials glyph
- All variants emit `@click` and respond to Enter/Space

## Spec diff

none

## Codegen impact

no

## Design impact

yes — primary discovery surface

## Tests

Storybook interactions; visual regression

## Sub-steps

- [x] shared types + COVER map + initials/fmtTime + `useCourseProgress` composable (commit `6f72324`)
- [x] `CoursePosterCard.vue` — 3:4 cover, state badges, progress strip (commit `562f918`)
- [x] `CourseWideCard.vue` — square thumb, resumeAt meta + pct fallback (commit `675aebb`)
- [x] `CourseCompactRow.vue` — single-line list variant (commit `f85ace6`)
- [x] barrel exports + Family Storybook story showing all three side-by-side (commit `f2a9969`)

## Notes

- Completed: 2026-04-27
- Result: T-2026-04-27-045 — five commits on `feat/course-card`. UI tests 662/662 (+71); lint + typecheck clean.
- File-naming deviation: composable lives at `use-course-progress.ts` (kebab-case) per the project's `unicorn/filename-case` convention. Imports use the kebab path.
- `CoursePosterCard` loading skeleton uses `height="0"` on `AppSkeleton` plus an `aspect-ratio: 3/4` SCSS class because `AppSkeleton` doesn't take `aspect-ratio` as a prop. Tracked as a small follow-up if a future card extends `AppSkeleton`.
- `course.cover` override test uses `toContain('url(')` instead of full equality because Vue normalises inline `url()` style values (e.g. `url(/x.jpg)` → `url("/x.jpg")`).
- Storybook ships per-variant + a `CourseCardFamily` story that renders all three side by side for visual comparison and a `FamilyAllAccents` story spot-checking the COVER map.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
