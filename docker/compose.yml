name: "course-shelf"

networks:
  "course-shelf-net":
    driver: bridge

volumes:
  pgdata:
  redisdata:
  otel_lgtm_data:

services:
  postgres:
    image: postgres:18.1-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: "course-shelf"
      POSTGRES_USER: "course-shelf"
      POSTGRES_PASSWORD: "course-shelf"
      PGDATA: /var/lib/postgresql/18/docker
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql
      - ./postgres/init.sql:/docker-entrypoint-initdb.d/10-init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U course-shelf -d course-shelf"]
      interval: 5s
      timeout: 3s
      retries: 20
    networks: ["course-shelf-net"]

  redis:
    image: redis:8.6-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20
    networks: ["course-shelf-net"]

  centrifugo:
    image: centrifugo/centrifugo:v6
    restart: unless-stopped
    command: ["centrifugo", "-c", "/centrifugo/config.json"]
    ports:
      - "8000:8000"
    volumes:
      - ./centrifugo/config.json:/centrifugo/config.json:ro
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8000/health"]
      interval: 5s
      timeout: 3s
      retries: 20
    networks: ["course-shelf-net"]

  otel-lgtm:
    image: grafana/otel-lgtm:0.8.1
    restart: unless-stopped
    ports:
      - "3200:3000" # Grafana UI
      - "4317:4317" # OTLP gRPC
      - "4318:4318" # OTLP HTTP
    environment:
      GF_AUTH_ANONYMOUS_ENABLED: "true"
      GF_AUTH_ANONYMOUS_ORG_ROLE: Admin
    volumes:
      - otel_lgtm_data:/data
      - ./grafana/provisioning:/etc/grafana/provisioning
    networks: ["course-shelf-net"]

  backend:
    build:
      context: ..
      dockerfile: apps/backend/Dockerfile.dev
    restart: unless-stopped
    depends_on:
      postgres: { condition: service_healthy }
      redis: { condition: service_healthy }
      centrifugo: { condition: service_healthy }
      otel-lgtm: { condition: service_started }
    environment:
      NODE_ENV: development
      PORT: "3000"
      DATABASE_URL: "postgresql://course-shelf:course-shelf@postgres:5432/course-shelf"
      REDIS_URL: redis://redis:6379/0
      BETTER_AUTH_SECRET: dev-secret-change-me
      BETTER_AUTH_URL: http://localhost:3000
      BETTER_AUTH_BASE_PATH: /api/v1/auth
      CENTRIFUGO_API_URL: http://centrifugo:8000/api
      CENTRIFUGO_API_KEY: dev-api-key
      CENTRIFUGO_TOKEN_HMAC_SECRET: dev-hmac-secret
      CENTRIFUGO_TOKEN_TTL_SECONDS: "300"
      CORS_ORIGINS: http://localhost:3001
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-lgtm:4318
    working_dir: /workspace
    # Anonymous volumes mask host node_modules at every package boundary.
    # Without this, host's macOS-pathed pnpm symlinks shadow the container's
    # Linux install and modules resolve to nothing inside the container.
    volumes:
      - ..:/workspace
      - /workspace/node_modules
      - /workspace/apps/backend/node_modules
      - /workspace/apps/web/node_modules
      - /workspace/packages/api-client-ts/node_modules
      - /workspace/packages/specs/node_modules
      - /workspace/packages/design-tokens/node_modules
      - /workspace/packages/eslint-config/node_modules
      - /workspace/packages/tsconfig/node_modules
      - /workspace/packages/ui/node_modules
    ports:
      - "3000:3000"
    healthcheck:
      test:
        - CMD-SHELL
        - wget -qO- --server-response http://127.0.0.1:3000/api/v1/health 2>&1 | grep -E 'HTTP/1\.[01] (200|503)'
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 60s
    networks: ["course-shelf-net"]

  web:
    build:
      context: ..
      dockerfile: apps/web/Dockerfile.dev
    restart: unless-stopped
    depends_on:
      backend: { condition: service_started }
    environment:
      NODE_ENV: development
      NUXT_PUBLIC_API_BASE_URL: http://localhost:3000/api/v1
      NUXT_PUBLIC_AUTH_BASE_URL: http://localhost:3000
    working_dir: /workspace
    # Anonymous volumes for every workspace package's node_modules + Nuxt's
    # build caches. Local `.nuxt` / `.output` from a prior `pnpm dev` on the
    # host would otherwise leak into the container with stale plugin state
    # and break the `<i18n>` SFC block transform (vite:json claims the
    # request before unplugin-vue-i18n can register).
    volumes:
      - ..:/workspace
      - /workspace/node_modules
      - /workspace/apps/backend/node_modules
      - /workspace/apps/web/node_modules
      - /workspace/apps/web/.nuxt
      - /workspace/apps/web/.output
      - /workspace/packages/api-client-ts/node_modules
      - /workspace/packages/specs/node_modules
      - /workspace/packages/design-tokens/node_modules
      - /workspace/packages/eslint-config/node_modules
      - /workspace/packages/tsconfig/node_modules
      - /workspace/packages/ui/node_modules
    ports:
      - "3001:3001"
    networks: ["course-shelf-net"]

  storybook:
    build:
      context: ..
      dockerfile: packages/ui/Dockerfile.dev
    restart: unless-stopped
    environment:
      NODE_ENV: development
    working_dir: /workspace
    # Anonymous volumes for every workspace package — Storybook imports
    # `@app/ui` which transitively pulls in `@app/design-tokens` and
    # `@app/api-client-ts`. Without each having its own anon volume, host
    # symlinks shadow the container's pnpm install and modules vanish.
    volumes:
      - ..:/workspace
      - /workspace/node_modules
      - /workspace/apps/backend/node_modules
      - /workspace/apps/web/node_modules
      - /workspace/packages/api-client-ts/node_modules
      - /workspace/packages/specs/node_modules
      - /workspace/packages/design-tokens/node_modules
      - /workspace/packages/eslint-config/node_modules
      - /workspace/packages/tsconfig/node_modules
      - /workspace/packages/ui/node_modules
      - /workspace/packages/ui/storybook-static
    ports:
      - "6006:6006"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:6006/iframe.html"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 60s
    networks: ["course-shelf-net"]
