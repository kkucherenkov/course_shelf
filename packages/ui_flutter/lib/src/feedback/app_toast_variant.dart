/// Semantic tone for [AppToast] — Flutter twin of the web `AppToast.vue`'s
/// narrower `Variant` union (`'success' | 'info' | 'error'`, no `warning`).
/// See `AppFeedbackVariant` for the wider set shared by [AppAlert]/[AppBanner].
enum AppToastVariant { success, info, error }
