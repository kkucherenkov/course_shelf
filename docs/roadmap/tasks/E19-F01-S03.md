# E19-F01-S03 — Stage B · Downloads tab UI (precede with cs-mobile-downloads)

**Status:** ⬜ Not started
**Stage:** B
**Owner:** claude
**Estimated duration:** 3 days

**Epic:** E19 — Mobile offline
**Feature:** F01 — Download manager

## Dependencies

- [E19-F01-S01](./E19-F01-S01.md) — DownloadsBloc with resumable encrypted downloads
- [E17-F02-S06](./E17-F02-S06.md) — DownloadRow (mobile-only) — full state machine

## Spec / Design references

- DESIGN_BRIEF §7.7
- docs/design/cs-mobile-downloads/ (to be produced)

## Goal

Storage usage + grouped sections (In progress / Downloaded / Failed).

## Acceptance

- Top: storage usage (CourseShelf used / total available)
- Sections: In progress, Downloaded (per-course with delete-all), Failed
- Empty: "Nothing downloaded yet — open a course and tap the download icon."
- Network-aware banner when offline

## Spec diff

none

## Codegen impact

no

## Design impact

yes — produce bundle first

## Tests

integration_test

## Sub-steps

- [x] **Design pre-step**: produce `cs-mobile-downloads` bundle
- [ ] screen composes `DownloadRow` widgets
- [ ] storage usage from `path_provider` + Drift sums

## Notes

✅ Design pre-step done (2026-07-18): `docs/design/cs-mobile-downloads/` produced via Open Design (POPULATED / EMPTY / OFFLINE frames; storage bar, In-progress/Downloaded/Failed groups) and verified in Chromium. Audited for a11y: collapsible groups are a real disclosure (`role=button` + `aria-expanded` + `aria-controls` via `useId()`, Enter/Space) and the shared `DownloadRow` action controls are now focusable, labelled `<button>`s (`cs-components` fix). No API gap — downloads list is local (Drift + `path_provider`).

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
