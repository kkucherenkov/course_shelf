# E13-F02-S06 — ProgressBadge — ring, bar, pill variants

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E13 — Web component catalog
**Feature:** F02 — Domain components (from cs-components)

## Dependencies

- [E13-F01-S01](./E13-F01-S01.md) — IconCS — port the bundle's 61-icon family to Vue

## Spec / Design references

- docs/design/cs-components/components.jsx §ProgressBadge

## Goal

One component, three visual variants, four domain states.

## Acceptance

- `variant: 'ring' | 'bar' | 'pill'`
- `state: 'not-started' | 'in-progress' | 'completed' | 'locked'`
- Ring uses `conic-gradient`; renders a checkmark when completed, a lock when locked
- Pill text: `Done` / `Locked` / `—` / `4 of 12`
- Bar shows numeric pct in mono next to the bar

## Spec diff

none

## Codegen impact

no

## Design impact

yes

## Tests

_To be defined when picked up._

## Sub-steps

- [x] `AppProgressBadge.vue` covering all three variants × four states (commit `9c37d9b`)
- [x] Spec: 12 snapshots (variants × states) + 9 logic cases for percentage maths and pill text matrix
- [x] Storybook: 3×4 Variants matrix + per-state demos

## Notes

- Completed: 2026-04-27
- Result: T-2026-04-27-043 — single commit `9c37d9b` on `feat/progress-badge`. UI tests 573/573 (+21); lint + typecheck clean.
- Token mapping (bundle → shipped): `--primary` → `--brand-accent`, `--surface-3` → `--surface-overlay`, `--success` → `--status-success-fg`, `--text-muted` → `--text-secondary`. Pill `completed` variant uses `--status-success-soft` background.
- Multi-root SFC (one branch per variant via `v-if/v-else-if/v-else`) means tests must query `.find('.app-progress-badge')` to read root-level attributes — `wrapper.attributes()` doesn't traverse multi-root fragments reliably.
- Percentage maths: `completed` clamps to 100 regardless of `completed/total`; `locked`/`not-started` clamp to 0; `total ≤ 0` guard returns 0 instead of `NaN`.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
