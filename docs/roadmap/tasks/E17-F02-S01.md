# E17-F02-S01 — CoursePosterCard / CourseWideCard / CourseCompactRow (Flutter)

**Status:** ⬜ Not started
**Stage:** A
**Owner:** claude
**Estimated duration:** 3 days

**Epic:** E17 — Mobile widget catalog
**Feature:** F02 — Mobile domain widgets

## Dependencies

- [E17-F01-S01](./E17-F01-S01.md) — IconCS Flutter widget — port the 61 icons
- [E17-F01-S07](./E17-F01-S07.md) — AppProgressLinear, AppProgressCircle, AppSpinner, AppSkeleton

## Spec / Design references

- docs/design/cs-components/components.jsx §CourseCard
- DESIGN_BRIEF §5.1

## Goal

Flutter equivalents of the web CourseCard family — three distinct widgets `CoursePosterCard`, `CourseWideCard`, and `CourseCompactRow` (not one widget with a `variant` prop).

## Acceptance

- Three separate widgets (`CoursePosterCard` / `CourseWideCard` / `CourseCompactRow`), not one widget with a `variant` prop
- Same prop surface as the web components (`course`, `state`, `resumeLabel`)
- Same state matrix; Widgetbook use cases per state

## Spec diff

none

## Codegen impact

no

## Design impact

yes

## Tests

_To be defined when picked up._

## Sub-steps

- [ ] _Decompose when picking this up._

## Notes

`resumeLabel` is a PRE-TRANSLATED string, never seconds — the widget must never format a time string itself (i18n contract; mirrors the web component, which takes `resumeLabel?: string`).

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
