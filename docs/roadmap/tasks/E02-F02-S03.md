# E02-F02-S03 — Generate Dart Dio client as `@app/api-client-dart`

**Status:** ⬜ Not started
**Stage:** A
**Owner:** claude
**Estimated duration:** 2 days

**Epic:** E02 — API contracts as the spine
**Feature:** F02 — Codegen pipeline

## Dependencies

- [E02-F01-S02](./E02-F01-S02.md) — Reusable error schema, security scheme, version prefix

## Spec / Design references

- DESIGN.md §3.2
- DESIGN.md §8

## Goal

A typed Dart client compatible with Dio that the mobile app consumes for all `/api/v1/*` calls, living in `packages/api-client-dart`.

## Acceptance

- `pnpm spec:codegen` regenerates the client under `packages/api-client-dart/lib/generated/`
- Mobile app imports from the `@app/api-client-dart` package (pubspec path dep)
- Client compiles with `flutter analyze` clean

## Spec diff

none

## Codegen impact

yes

## Design impact

no

## Tests

_To be defined when picked up._

## Sub-steps

- [ ] use `openapi-generator-cli` with `dart-dio` template, pinned version
- [ ] script wraps the CLI; output path is `packages/api-client-dart/lib/generated/`
- [ ] `pnpm spec:codegen` drives this as one of its sub-tasks
- [ ] document that the directory is generated — never edit by hand

## Notes

_Empty initially; agent fills in during work._

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
