# E31-F01-S02 — Backups UI

**Status:** ⬜ Not started
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E31 — Honest v1.1
**Feature:** F01 — Close the real gaps

## Dependencies

- [E21-F01-S02](./E21-F01-S02.md) — POST /admin/backups (DB snapshot archive)

## Spec / Design references

- `docs/user-guide.md` — Known limits: “Backups have no UI”

## Goal

The API has shipped since E21. On a one-person instance, being able to take and download a backup without curl is exactly the point.

## Acceptance

- An admin screen creates a backup and downloads it via the existing signed short-lived link
- In-progress and failure states are visible
- Strings via `t()` in `en` and `ru`; `pnpm check:i18n` green
- The Known-limits row is removed from `docs/user-guide.md`

## Spec diff

none — `POST /admin/backups` and the download route already exist

## Codegen impact

no

## Design impact

no

## Tests

Component spec for the three states; the download link is asserted, not followed.

## Sub-steps

- [ ] Admin backups screen + spec
- [ ] Locale keys in both languages
- [ ] Remove the Known-limits row

## Notes

—

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done · ❌ Cancelled

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
