## T-2026-09-18-r2-text — six cheap text/docs cleanups (lane "text")

- Created: 2026-09-18
- Owner: claude
- Spec: tuxedo 193, 195, 194, 189, 188, 254 (r2-text lane brief)
- Goal: remove dead i18n keys, fix stale/misleading comments, disambiguate the
  duplicate "Libraries" nav entries, correct a false branch-protection claim
  in the architecture doc.
- Acceptance:
  - `pages.admin.libraryDetail.scanProgressCancel` and `notifiers.scan.cancel`
    gone from en.ts/ru.ts; AppScanProgress.spec.ts still makes sense.
  - `pages.admin.permissions.addGrantCta` gone from en.ts/ru.ts.
  - nuxt.config.ts:30 stale "messages live in .json" comment deleted.
  - settings.vue:12 comment says "two options", not "3-up picker".
  - default.vue: admin sees two distinguishable nav labels; `/libraries`
    entry hidden for non-admins (route already has `admin` middleware); new
    locale keys added both languages.
  - docs/architecture.md ~L724: strict-mode claim corrected to match
    `gh api .../branches/main/protection` (`strict: false`); parallel-lane
    reasoning in ~727-732 updated to match.
- Spec diff: none
- Codegen impact: no
- Design impact: none
- Tests: `pnpm --filter @app/web test`, `pnpm check:i18n`
- Sub-steps:
  - [x] 193 — remove scanProgressCancel/notifiers.scan.cancel keys
  - [x] 195 — remove addGrantCta key
  - [x] 194 — delete stale nuxt.config.ts comment
  - [x] 189 — no-op: line 12 already reads "theme (3-up picker), density
        (2-up picker)" since 01baa1d1 (2026-09-16); audit target predates
        that fix
  - [x] 188 — no-op: admin nav already reads "Manage libraries"
        (916fd9361) and the bare `/libraries` route + nav entry was already
        removed (ba74cffd6, #665), both 2026-09-16; no duplicate label and
        nothing to hide
  - [x] 254 — fix architecture.md strict-mode claim (verified `strict:
false` via `gh api .../branches/main/protection`); reworded the
        parallel-lane rationale to point at git's plain three-way merge
        instead
  - [x] lint --fix, format, check:i18n, vitest all green (592/592)
  - [ ] PR opened, CI green
- Status: in-progress
- Blockers: —
