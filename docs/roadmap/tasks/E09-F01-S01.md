# E09-F01-S01 — LessonProgress aggregate + record endpoint

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 2 days

**Epic:** E09 — Learning
**Feature:** F01 — Progress

## Dependencies

- [E04-F02-S03](./E04-F02-S03.md) — Mount express-openapi-validator for /api/v1/\*

## Spec / Design references

- PRD.md FR-PROG-01
- PRD.md FR-PROG-02
- DESIGN.md §5.1

## Goal

Position writes update progress; completion threshold raises a domain event.

## Acceptance

- `POST /progress` upserts; out-of-order `clientUpdatedAt` is silently ignored (last-write-wins)
- Crossing 90% emits `LessonCompleted` exactly once per (user, lesson)

## Spec diff

yes — `POST /progress`, `GET /progress/{lessonId}`

## Codegen impact

yes

## Design impact

no

## Tests

_To be defined when picked up._

## Sub-steps

- [x] spec + regen (commits `3bc4e58`, `68549d0`)
- [x] `LessonProgress` aggregate at `apps/backend/src/modules/learning/domain/progress/`; `record()` returns `{ aggregate, accepted, completedThisCall }` so the handler decides about `EventBus.publish(LessonCompleted)`
- [x] handler + Prisma adapter + mapper; `(userId, lessonId)` composite unique; `completedAt` undefined ↔ null at the boundary
- [x] `ProgressController` (`@Controller('progress')`) under new `LearningModule`; actor forwarded from session

## Notes

- Completed: 2026-04-26
- Result: T-2026-04-26-021 — three commits on `feat/lesson-progress`: `3bc4e58` (spec), `68549d0` (codegen), `84d0510` (impl). Backend tests 441/441; lint + typecheck clean.
- Position-clamped-to-duration (not rejected) because video players report `currentTime` slightly above `duration` at segment boundaries. Documented in code.
- GET handler treats "lesson missing" as 403 (PermissionDenied), not 404 — no-oracle rule. Only after a successful `canSee` check does a missing progress row become 404.
- `LessonCompleted` event published synchronously after `repo.save`; no subscriber yet (E10-F01-S01 lands next; Nest's `EventBus` drops un-handled events silently).
- `completedThisCall` flag inside `RecordResult` is the idempotency gate — second record above 90 % does not re-emit.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
