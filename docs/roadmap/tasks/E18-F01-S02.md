# E18-F01-S02 — Stage B · Browse tab (precede with cs-mobile-browse)

**Status:** ✅ Done
**Stage:** B
**Owner:** claude
**Estimated duration:** 3 days

**Epic:** E18 — Mobile features
**Feature:** F01 — Browsing

## Dependencies

- [E17-F02-S01](./E17-F02-S01.md) — CourseCard (poster / wide / compact in Flutter)
- [E17-F02-S07](./E17-F02-S07.md) — NavigationShell (mobile — bottom tab bar with 5 tabs)

## Spec / Design references

- DESIGN_BRIEF §7.4
- docs/design/cs-mobile-browse/ (partial — needs app.jsx)

## Goal

Grid of poster CourseCards; filter bottom sheet; sort.

## Acceptance

- 2 columns at narrow widths, 3 at tablet widths
- Filter button opens a bottom sheet (status, library)
- Empty / loading / error states

## Spec diff

none

## Codegen impact

no

## Design impact

yes — produce bundle first

## Tests

_To be defined when picked up._

## Sub-steps

- [x] **Design pre-step**: finish the partial `cs-mobile-browse` bundle (needs app.jsx)
- [x] BrowseCubit + screen

## Notes

Filters trimmed to what the API backs: `/courses` supports `libraryId`, `status`, `sort` only. `duration` + `instructor` filters are deferred pending backend (`CourseDto.totalDurationSeconds` + a `Course.instructor` field — both explicitly deferred on web). `cs-mobile-browse` bundle already exists but is partial (needs `app.jsx`).

✅ Design pre-step done (2026-07-18): hand-authored `docs/design/cs-mobile-browse/app.jsx` (OD MCP disconnected) in the established bundle style — 7 frames: grid 2-col, grid 3-col (tablet), filtered + active-rail, filter/sort bottom sheet (Sort · Status · Library, API-backed facets only), loading, empty, error. Verified render-clean in headless Chromium (7 phones / 24 posters / sheet / empty / error, no JS errors). Sheet drops the web's duration/instructor facets since `/courses` doesn't back them. Filters are single-select per the single-value API (`status`, `libraryId`).

- Completed: 2026-07-18
- Result: https://github.com/kkucherenkov/course_shelf/pull/181

BrowseCubit + repo (`GET /courses` filter/sort + `GET /libraries`) and `BrowseTabBody` (poster grid 2-col / 3-col tablet, non-scrolling inside the shell; single-select filter/sort bottom sheet; active-chip rail; loading/empty/error). Posters tap through to the E18-F01-S03 course-detail route. Filter→query mapping mirrors `apps/web` (omit-when-default). Full mobile suite 247/247. Closes #94, #95, #96.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
