# E25-F02-S01 — Transcript and transcription schema

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 0.5 day

**Epic:** E25 — Transcription (Whisper)
**Feature:** F02 — Domain

## Dependencies

- [E06-F03-S02](./E06-F03-S02.md) — Lesson + Material + Subtitle read model

## Spec / Design references

- [B4 design §5](../../superpowers/specs/2026-08-29-b4-transcription-design.md)
- [B4 plan, Task 5](../../superpowers/plans/2026-08-29-b4-transcription.md)

## Goal

Four tables and two enums: the transcript index that doubles as the skip marker, its cues, the run, and the run's per-lesson errors.

## Acceptance

- `Transcript(lessonId, language, origin, sourcePath, sourceMtime, sourceSize, derivedPath)` unique on `(lessonId, language)`
- `TranscriptCue(transcriptId, startMs, endMs, text)`
- `Transcription` + `TranscriptionErrorRecord`, shaped like `Scan` + `ScanErrorRecord`
- **No foreign key** from `transcript` to `lesson` or `subtitle`

## Spec diff

none

## Codegen impact

no

## Design impact

no

## Tests

Covered through the handler and repository specs that follow; the migration itself is verified by review and `typecheck`.

## Sub-steps

- [x] Models and enums in `schema.prisma`
- [x] Migration generated and its SQL reviewed for an accidental FK
- [x] `typecheck` green

## Notes

- Completed: 2026-08-30
- Result: https://github.com/kkucherenkov/course_shelf/pull/256
- Migration `20260830120000_add_transcription`. Generated offline with
  `prisma migrate diff --from-schema <HEAD schema> --to-schema` because
  no database was reachable from the worktree; the SQL was reviewed by
  hand.
- The SQL creates exactly two foreign keys — `transcript_cue` ->
  `transcript` and `transcription_error` -> `transcription`. Nothing
  references `lesson` or `subtitle`.

- `prisma-lesson.repository.ts` deletes and recreates every subtitle row on each lesson save, so a cascade from `subtitle` — or from `lesson` — would erase transcripts on the next ordinary scan. Cleanup is explicit instead (E25-F04-S01).

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done · ❌ Cancelled

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
