# E11-F01-S03 — Auth store + minimal sign-in/setup pages

**Status:** ⬜ Not started
**Stage:** A
**Owner:** claude
**Estimated duration:** 2 days

**Epic:** E11 — Web foundations
**Feature:** F01 — Nuxt + Nuxt UI shell

## Dependencies

- [E11-F01-S02](./E11-F01-S02.md) — Wire @app/api-client-ts with bearer interceptor + auth store

## Spec / Design references

- PRD.md FR-AUTH-01

## Goal

First-run wizard creates the admin; `/sign-in` works against the real backend via the Pinia auth store.

## Acceptance

- First run: `/setup` is forced if no users exist (checked via `GET /admin/has-users`)
- Returning users: `/sign-in` if no session, otherwise `/`

## Spec diff

none

## Codegen impact

no

## Design impact

no

## Tests

_To be defined when picked up._

## Sub-steps

- [ ] `pages/setup.vue` calls `stores/auth.ts` `signUp` then promotes role to admin
- [ ] `pages/sign-in.vue` (minimal; full design in E14-F02-S01)
- [ ] global middleware reads session from the Pinia auth store
- [ ] backend endpoint `GET /admin/has-users` added here

## Notes

The _real_ sign-in + setup design lands when `cs-web-auth` is prototyped (Stage B in E14-F02).

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
