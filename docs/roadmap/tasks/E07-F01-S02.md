# E07-F01-S02 — AuthorizationService consumed by Catalog & Streaming

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 2 days

**Epic:** E07 — Access control
**Feature:** F01 — Grants & enforcement

## Dependencies

- [E07-F01-S01](./E07-F01-S01.md) — AccessGrant aggregate + admin endpoints

## Spec / Design references

- PRD.md NFR-S-01
- PRD.md FR-AUTH-04

## Goal

A single `AuthorizationService.canSee(user, resource)` reused everywhere.

## Acceptance

- `ListCoursesQuery` filters by grants for non-admins
- Stream-token issuance refuses to mint for unauthorized lessons

## Spec diff

none

## Codegen impact

no

## Design impact

no

## Tests

_To be defined when picked up._

## Sub-steps

- [x] add `AuthorizationService` to `access` context's `application/`
- [x] consumers depend on its interface, not its implementation
- [x] caching: in-memory LRU keyed by `(userId, resourceId)` with short TTL

## Notes

- Completed: 2026-04-26
- Result: feat/authorization-service branch, single commit on top of 239b0d7.
- `AuthorizationService` + `AUTHORIZATION_SERVICE` token live in `src/common/access/`.
- `LruAuthorizationService` wraps `GRANT_REPOSITORY` via `forwardRef` circular-module break.
- Direct `invalidate(userId)` call in `RegisterGrantHandler` / `RevokeGrantHandler` (no EventBus).
- Actor passed on query DTO (`ListLibrariesQuery`, `GetLibraryQuery`); controller resolves session.
- 25 new tests; 127 total green.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
