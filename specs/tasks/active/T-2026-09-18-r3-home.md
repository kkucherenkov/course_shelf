## T-2026-09-18-r3-home — Stop faking progress on the home page

- Created: 2026-09-18
- Owner: claude
- Spec: tuxedo 182, tuxedo 200 (part of #578)
- Goal: home page shows one honest progress number per course, in both the
  "Continue watching" and "Recently added" rails, and "Continue watching"
  can show a real resume position instead of only a percentage.
- Acceptance:
  - A course appearing in "Recently added" shows its real completed-lesson
    count, not a hardcoded 0.
  - `CourseWideCard.resumeLabel` can be filled with a real
    `mm:ss` / `h:mm:ss` position when one exists, not only `NN%`.
  - No N+1: progress and position are bulk-fetched, not queried per course.
- Spec diff:
  - `RecentlyAddedItem` gains required `lessonsCompleted` (integer, min 0).
  - `ContinueWatchingItem` gains optional `resumePositionSeconds` (integer,
    min 0).
- Codegen impact: yes — regenerate api-client-{ts,dart}, own commit.
- Design impact: none — `CourseWideCard` already has `resumeLabel` prop and
  Storybook coverage for the "Resume 2:05" state; only data wiring changes.
- Tests: unit tests for the two handlers (bulk progress / position lookups
  mocked at the port).
- Sub-steps:
  - [x] Task file created
  - [x] `packages/specs/openapi/openapi.yaml`: add the two fields
  - [x] `pnpm spec:validate && pnpm spec:bundle && pnpm spec:codegen`
  - [x] backend: recently-added handler emits `lessonsCompleted` via bulk
        query
  - [x] backend: continue-watching handler emits `resumePositionSeconds` via
        bulk `LessonProgress` lookup by `(userId, lastSeenLessonId)`
  - [x] unit tests for both handlers
  - [x] web: `index.vue` uses `item.lessonsCompleted` instead of `0`
  - [x] web: continue-watching card formats `resumePositionSeconds` into
        `resumeLabel` when present
  - [x] `pnpm format`
  - [ ] PR opened, CI green (Monitor)
- Status: in-progress
- Blockers: —
