# E13-F01-S02 — AppButton + AppIconButton (matches .btn classes)

**Status:** ⬜ Not started
**Stage:** A
**Owner:** claude
**Estimated duration:** 2 days

**Epic:** E13 — Web component catalog
**Feature:** F01 — Primitive families (from cs-foundations)

## Dependencies

- [E12-F01-S01](./E12-F01-S01.md) — Configure Storybook for Vue 3 + Nuxt UI v4 + tokens
- [E13-F01-S01](./E13-F01-S01.md) — IconCS — port the bundle's 61-icon family to Vue

## Spec / Design references

- docs/design/shared/tokens.css §Buttons
- docs/design/cs-foundations/ §5

## Goal

Variants and sizes match the design exactly.

## Acceptance

- Variants: `primary`, `secondary`, `ghost`, `destructive`
- Sizes: `sm` (28px), default (36px), `lg` (44px)
- States in stories: Default, Hover, Active, Focus (keyboard), Disabled, Loading (spinner replaces label)
- `AppIconButton` is a square variant (28/36/44 wide)
- Loading state matches `data-loading="true"` in the bundle CSS

## Spec diff

none

## Codegen impact

no

## Design impact

yes

## Tests

Storybook interaction (click, keyboard activation); a11y check (focus ring visible, `aria-busy` while loading, `aria-disabled`)

## Sub-steps

- [ ] _Decompose when picking this up._

## Notes

_Empty initially; agent fills in during work._

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
