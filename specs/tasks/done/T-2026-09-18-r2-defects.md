## T-2026-09-18-r2-defects — fix six R2 defects in browse/course-detail lane

- Created: 2026-09-18
- Owner: claude
- Spec: GitHub #578 (183, 184, 185); tuxedo-sourced defects 196, 212, 213
- Goal: browse/course-detail/scan-notifier surfaces stop lying to the user —
  skeleton matches the real card, description isn't duplicated, chevron uses
  the house icon system, load errors use the shared error component, rate
  limits say so, and partial scans don't wear the success colour.
- Acceptance:
  - browse.vue skeleton cell height matches CoursePosterCard's real rendered
    height at 180px width (post-#716 progress ring).
  - CourseHero's descriptionLead no longer duplicates CourseDescription's
    first line.
  - HomeRow.vue's collapse chevron renders via IconCS, not a `'▾'` literal.
  - courses/[id].vue's non-403 load-error branch renders via AppErrorState.
  - useCoursesList surfaces the HTTP status from a failed courses fetch;
    browse.vue's banner shows distinct copy for 429 vs generic failure.
  - ScanLifecycleNotifier passes through a `partial` status distinct from
    `success`; AppScanProgress gains a 4th state with story + spec + updated
    visual baseline.
- Spec diff: none (no HTTP contract change — 212 reads status already on the
  error, doesn't add a route)
- Codegen impact: no
- Design impact: AppScanProgress (@app/ui) gains `partial` state — story +
  spec + visual baseline
- Tests: vitest unit (useCoursesList status plumbing, ScanLifecycleNotifier
  status mapping, AppScanProgress spec), Storybook story for partial state
- Sub-steps:
  - [x] 183: browse.vue skeleton now renders `<CoursePosterCard loading />` —
        can't drift from the real card's height, no px number to chase
  - [x] 184: `descriptionBody()` strips the hero's lead from what
        `CourseDescription` renders (courses/[id].vue + description-lead.ts)
  - [x] 185: HomeRow.vue chevron → `IconCS name="chevron-down"`
  - [x] 196: courses/[id].vue's non-403 error branch → `AppErrorState`
        (new key `pages.courseDetail.loadErrorTitle`)
  - [x] 212: useCoursesList throws `HttpStatusError` (mirrors
        useCourseOutline.ts's idiom) and exposes `errorStatus`; browse.vue's
        banner shows `ui.errors.rateLimitedBody` on 429
  - [x] 213: ScanLifecycleNotifier's `toScanStatus()` maps `partial`
        distinctly; AppScanProgress gained a 4th `ScanStatus` + `partialLabel`
        prop, warning-coloured dot/bar, story, spec, admin/libraries/[id].vue
        updated for the new required prop (new key
        `pages.admin.libraryDetail.scanProgressPartial`)
  - [x] lint --fix (web, ui) + stylelint:fix + format — all clean
  - [x] vitest (web 602, ui 998) + typecheck (web, ui) green; check:i18n clean
  - [x] Storybook visual baselines regenerated via CI workflow (Partial story +
        all-states) — landed as cf62e15b; needed a fix to
        `regen-snapshots.yml`'s body-less "Pull external images" step first
        (pre-existing on main, blocked `workflow_dispatch`'s static
        validation for the whole file)
  - [x] PR opened (#741); CI checks retriggering now — the baseline commit
        above landed as `github-actions[bot]` on the default `GITHUB_TOKEN`,
        which GitHub never runs checks against (anti-recursion), so the PR
        head sat at zero checks until this commit
- Status: done
- Blockers: —
- Completed: 2026-09-18
- Result: https://github.com/kkucherenkov/course_shelf/pull/741
