# E13-F01-S12 — AppChip (default/primary/success/warning/error/info; removable variant)

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E13 — Web component catalog
**Feature:** F01 — Primitive families (from cs-foundations)

## Dependencies

- [E12-F01-S01](./E12-F01-S01.md) — Configure Storybook for Vue 3 + Nuxt UI v4 + tokens
- [E13-F01-S01](./E13-F01-S01.md) — IconCS — port the bundle's 61-icon family to Vue

## Spec / Design references

- docs/design/cs-foundations/, docs/design/shared/tokens.css

## Goal

AppChip (default/primary/success/warning/error/info; removable variant) matching the bundle.

## Acceptance

- States in stories match the bundle's state matrix
- Uses design tokens only (no hard-coded values)

## Spec diff

none

## Codegen impact

no

## Design impact

yes

## Tests

_To be defined when picked up._

## Sub-steps

- [x] AppChip refactored to flat 6-variant API (`default/primary/success/warning/error/info`) matching `.chip-*` classes
- [x] Icon migrated from `AppIcon` (Iconify) to `IconCS` (typed `IconName` literal)
- [x] `dismissible` renamed to `removable` to match the bundle's `.chip-removable`
- [x] Broken `--status-danger` token replaced with `--status-error-fg`

## Notes

- Completed: 2026-04-27
- Result: T-2026-04-27-041 — commit `659c626` on `feat/primitives-batch-b`. The previous `color × variant × size` matrix collapsed to flat `variant: 'default' | 'primary' | 'success' | 'warning' | 'error' | 'info'`. Existing AppChip.spec.ts had 11 cases tied to the old prop axis; replaced with 17 covering the new variant axis + remove emit + selected/disabled/router-link mode.
- Net test delta for AppChip alone: +6 (17 new − 11 old). UI tests went 513 → 552 across the full batch B (+39 net).
- AppChip is the only existing component refactored in this slice; all callers in `apps/web/` were searched and the only hit was a doc-only string in `eslint.config.mjs` (no JSX usage). No web migration needed.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
