# E31-F01-S01 — Finish the browse filters

**Status:** ⬜ Not started
**Stage:** A
**Owner:** claude
**Estimated duration:** 2 days

**Epic:** E31 — Honest v1.1
**Feature:** F01 — Close the real gaps

## Dependencies

- [E14-F01-S02](./E14-F01-S02.md) — Stage B · Browse + Search

## Spec / Design references

- `docs/user-guide.md` — Known limits: “Browse filters incomplete”
- Design bundle `cs-web-browse-search`

## Goal

The library, duration-bucket and instructor filters and sort-by-duration were designed and never implemented. With a large library this is a daily cost.

## Acceptance

- Library, duration-bucket and instructor filters work on web
- Sort by duration works
- Filter state survives a reload via the query string
- The Known-limits row is removed from `docs/user-guide.md` in the same PR

## Spec diff

openapi.yaml only if the list endpoint lacks the parameters — check before assuming

## Codegen impact

conditional

## Design impact

no — the bundle already specifies these

## Tests

Component specs per filter; a query-string round-trip test.

## Sub-steps

- [ ] Audit which filter parameters the API already accepts
- [ ] Contract + codegen if anything is missing
- [ ] Filters and sort on the browse page
- [ ] Query-string persistence
- [ ] Remove the Known-limits row

## Notes

—

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done · ❌ Cancelled

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
