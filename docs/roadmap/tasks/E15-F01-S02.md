# E15-F01-S02 — Wire generated Dio client with bearer interceptor

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E15 — Mobile foundations
**Feature:** F01 — App skeleton

## Dependencies

- [E02-F02-S03](./E02-F02-S03.md) — Generate Dart Dio client as `@app/api-client-dart`
- [E15-F01-S01](./E15-F01-S01.md) — Flutter bootstrap, theme from tokens

## Spec / Design references

- docs/design/DESIGN_BRIEF.md

## Goal

One configured Dio + generated client used everywhere.

## Acceptance

- `Authorization: Bearer <token>` injected for `/api/v1/*`
- 401 triggers token clear via the auth cubit

## Spec diff

none

## Codegen impact

no

## Design impact

no

## Tests

_To be defined when picked up._

## Sub-steps

- [x] `core/api/dio_factory.dart`
- [x] interceptor reading from `flutter_secure_storage`
- [x] expose typed `apiClient` from a single getter

## Notes

- Shipped in apps/mobile — `shared/network/api_client.dart` (`buildDio`: injects `Authorization: Bearer <token>`, clears token on 401); single shared Dio registered in `shared/di/injector.dart`. Real filenames differ from the card's `core/api/dio_factory.dart`. 401 is cleared in the interceptor (re-auth deferred to presentation).

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
