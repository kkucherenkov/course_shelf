# E01-F01-S01 — Initialize pnpm + Turborepo workspace

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 1 day

**Epic:** E01 — Repository foundations
**Feature:** F01 — Workspace bootstrap

## Dependencies

_None_

## Spec / Design references

- AGENTS.md §2
- AGENTS.md §3

## Goal

A pnpm workspace that Turborepo can drive, with the directory layout from AGENTS.md §2 in place.

## Acceptance

- `pnpm install` runs cleanly from a fresh clone
- `turbo run build --dry` lists the expected app + package targets
- Directory layout matches AGENTS.md §2

## Spec diff

none

## Codegen impact

no

## Design impact

no

## Tests

_To be defined when picked up._

## Sub-steps

- [x] add root `package.json` with `"private": true`, pin pnpm version via `packageManager`
- [x] add `pnpm-workspace.yaml` declaring `apps/*` and `packages/*`
- [x] add `turbo.json` with `build`, `lint`, `typecheck`, `test`, `dev` pipelines
- [x] create empty `apps/{backend,web,mobile}` and `packages/{specs,api-client-ts,api-client-dart,design-tokens,eslint-config,tsconfig}`
- [x] add `.editorconfig`, root `.gitignore` (covers `node_modules`, `dist`, `.turbo`, `.env*`, `apps/mobile/build`)
- [x] add `.nvmrc` pinning Node 20 LTS

## Notes

- Completed: 2026-04-26
- Result: archive sweep — already shipped by initial scaffolding (see git log; no single PR for this story)

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
