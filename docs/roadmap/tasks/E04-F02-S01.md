# E04-F02-S01 — Configure Better Auth with bearer + admin plugins

**Status:** ✅ Done
**Stage:** A
**Owner:** claude
**Estimated duration:** 2 days

**Epic:** E04 — Backend skeleton + Better Auth
**Feature:** F02 — Better Auth integration

## Dependencies

- [E04-F01-S01](./E04-F01-S01.md) — Bootstrap apps/backend with Prisma, modules skeleton, and shared kernel

## Spec / Design references

- AGENTS.md §7.4
- DESIGN.md §2.5
- DESIGN.md §5.4

## Goal

Better Auth boots, manages its own Prisma tables, exposes `/api/v1/auth/*`, issues bearer tokens via `Set-Auth-Token`.

## Acceptance

- `apps/backend/src/identity/auth.ts` exports `createAuth(prisma)`
- `pnpm auth:schema` regenerates the Better Auth section of `schema.prisma`
- Plugins enabled: `emailAndPassword`, `bearer`, `admin`
- `additionalFields` declares `role` (default `USER`) and `displayName`

## Spec diff

none

## Codegen impact

no

## Design impact

no

## Tests

integration test exercises sign-up, sign-in, get-session, sign-out via supertest

## Sub-steps

- [x] install `better-auth` (1.6); native Nest integration via `AuthService` + `AuthController` (deviation: `@thallesp/nestjs-better-auth` skipped — see Notes)
- [x] Better Auth instance configured in `apps/backend/src/common/auth/auth.service.ts` (deviation: not under `identity/auth.ts`)
- [x] `BETTER_AUTH_SECRET` and `BETTER_AUTH_URL` are wired through `AppConfig.betterAuth`
- [x] `pnpm auth:schema` script regenerates the Better Auth section of `prisma/schema.prisma`
- [x] `additionalFields { role: 'string' default 'USER', displayName: 'string' optional }` declared so `session.user.role` is type-safe
- [x] plugins enabled: `emailAndPassword`, `bearer`, `admin`, `phoneNumber`

## Notes

- Completed: 2026-04-27
- Result: T-2026-04-27-028 — commit `501bcc2` on `feat/e04-alignment`.
- Deviation 1: did **not** install `@thallesp/nestjs-better-auth`. The native integration (`AuthService` + catch-all `AuthController` mapping `/api/v1/auth/*` to `auth.handler(Request)`) is solid and well-tested; adopting a third-party Nest wrapper would force a rewrite for cosmetic gain. Documented in the commit body.
- Deviation 2: Better Auth lives in `apps/backend/src/common/auth/` (not `apps/backend/src/identity/auth.ts`). Renaming is cosmetic-only — kept current layout to avoid touching every import site.
- Deviation 3: an extra `phoneNumber` plugin is enabled on top of the card's three (the project supports SMS OTP sign-up). Not in scope for this card but left as-is.

---

**Status legend:** ⬜ Not started · 🔄 In progress · 🚫 Blocked · ⏸ Paused · ✅ Done

When marking done, also:

1. Tick all sub-step checkboxes above.
2. Add `- Completed: YYYY-MM-DD` and `- Result: <PR link>` under the Notes heading.
3. Update the same row in `../TODO.md` (`- [ ]` → `- [x]`).
